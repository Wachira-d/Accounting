# LODGING_BOOKING_AUDIT.md — ตรวจโมดูลจองที่พักบน CMS + งานที่ต้องทำต่อ

_รอบ 126 · 2026-09-04 · ตรวจกับโค้ดจริงทุกข้อ (ไม่ใช่จากเอกสาร)_
_ขอบเขต: ลูกค้าที่เลือกเว็บประเภท "ที่พัก" ทำอะไรได้จริงบ้าง — จัดการห้อง ·
แสดงข้อมูล/รูป · แขกเช็ควันและจองเอง · แนบสลิป · payment gateway ·
หลักฐานการจองให้โหลด · ลูกค้าจัดการ add-on ตรงไหน · เส้นทาง admin ตรวจสลิป_

> **สรุปหนึ่งบรรทัด:** โครงหลังบ้านครบและถูกต้องมาก (ราคา · ห้องว่าง · มัดจำ ·
> เอกสารบัญชี · นโยบายยกเลิก) แต่ **ปลายทางที่แขกกับเจ้าของสัมผัสจริง ขาด 6 ชิ้น**
> — และ 3 ใน 6 เป็น defect class เดิมของเรพนี้: **"ของที่สร้างไว้แล้วไม่ได้ถูกเรียกใช้"**

---

## 1. เช็กลิสต์ตามคำถาม — สถานะจริง

| # | คำถาม | สถานะ | หลักฐาน |
| --- | --- | --- | --- |
| 1 | ลูกค้าจัดการ **ข้อมูลห้องพัก** ได้ไหม | ✅ ครบ | `pages/lodging-settings.html` — ที่พัก · ประเภทห้อง · ห้องรายห้อง · แผนราคา · ฤดูกาล · override รายวัน · นโยบายยกเลิก · บริการเสริม (`LodgingController` 20+ endpoint) |
| 2 | **แสดงข้อมูล**ที่พักบนเว็บ | ⚠️ ครึ่งเดียว | `LodgingPublicInfo` ส่ง 30 ฟิลด์ (คำอธิบาย · สิ่งอำนวยความสะดวก · แผนที่ · โทร · LINE · เวลาเช็คอิน) แต่ `renderLodgingSearch` **แสดงแค่ ชื่อ + ดาว + ที่อยู่** — ที่เหลือ **ไม่มีใครวาด** |
| 3 | **แสดงรูป** | ⚠️ ครึ่งเดียว | เก็บได้หลายรูป (`ImagesJson` ทั้งที่พักและประเภทห้อง) · หน้าเว็บวาด **รูปแรกของประเภทห้องรูปเดียว** (`storefront.html:1483`) · **แกลเลอรีที่พักไม่เคยถูกวาดเลย** · ไม่มี lightbox · ไม่มีตัวอัปโหลด (ให้กรอก URL เอง) |
| 4 | แขก **เช็ควัน + จองเอง** ครบ | ✅ ครบ | `/booking` · `/rooms` → ค้นห้องว่าง → เลือกแผนราคา/บริการเสริม → ใบเสนอราคาสด (`/quote`) → ฟอร์มจอง → `/reservations` · ด่านฝั่งเซิร์ฟเวอร์ครบ (เปิดรับจองออนไลน์ · ล่วงหน้าขั้นต่ำ/สูงสุด · จำนวนคืน · บัตรประชาชน · ยอมรับกติกาที่พัก) |
| 5 | **แนบสลิป** | ✅ มี / ⚠️ ไม่มีทางปฏิเสธ | แขกอัปโหลดผ่าน token ได้ (`/reservations/{token}/slip`) · เจ้าหน้าที่เปิดดูแล้วกด "ยืนยัน + บันทึกรับมัดจำ" · **ไม่มีปุ่ม "ปฏิเสธสลิป"** — สลิปไม่ตรง/ปลอม มีทางเดียวคือ**ยกเลิกทั้งใบ** และ**แขกไม่เคยรู้ว่าถูกปฏิเสธ** (หน้าเขาค้าง "รอที่พักตรวจสอบ" ตลอดไป) |
| 6 | **Payment gateway** ให้แขกจ่ายออนไลน์ | ❌ **ต่อไม่ถึง** | `LodgingReservationPaymentHandler` เขียนครบและถูกต้อง (ตรวจห้องว่างซ้ำ · ออกใบเสร็จมัดจำ · กัน webhook ซ้ำ) แต่ **`PaymentGatewayController` เป็น `[Authorize]`** ⇒ แขกที่ไม่ล็อกอิน **สร้าง PaymentIntent ไม่ได้เลย** · `storefront.html` ไม่มีการเรียก `/pay/intents` แม้แต่ที่เดียว ⇒ handler นี้ไม่มีวันถูกเรียกจากฝั่งแขก |
| 7 | **หลักฐานการจองให้โหลดเก็บ** | ❌ **ไม่มี** | หน้า `/reservation/{token}` มีแค่ปุ่ม "กลับหน้าหลัก" กับ "ยกเลิกการจอง" — ไม่มี PDF/พิมพ์/ส่งอีเมลยืนยัน · ไม่มี `LodgingVoucher` ที่ไหนในเรพ |
| 8 | ลูกค้าจัดการ **add-on** ตรงไหน | ✅ มีที่ | เมนู **"ส่วนเสริมของฉัน"** → `/pages/addons.html` (โควตาเอกสาร · ซื้อเพิ่ม · ภารกิจแลกโควตา · เปิด/ปิดฟีเจอร์) · ฝั่งแพลตฟอร์ม `/pages/admin-addons.html` (ตั้งราคา · เปิด/ปิดการขาย) |
| 9 | "เปิด gateway → จ่ายแล้วเปิดได้เลย · ยังไม่เปิด → แนบสลิป + admin ตรวจ · ปลอม → ปิดฟังก์ชัน" **สำหรับ add-on** | ❌ **ยังไม่มีทั้งเส้น** | วันนี้เปิด add-on = **ติ๊กยอมรับค่าใช้จ่ายแล้วกดเปิด ได้ทันทีฟรี** (`MeteringController.ToggleFeature`) แล้วค่อยเก็บเงินทีหลังผ่าน `AddOnMonthlyBillingJob` — **ไม่มีทั้งช่องจ่ายผ่าน gateway และช่องแนบสลิป** · เส้นสลิปที่มีอยู่จริงคือของ **แพ็กเกจรายเดือน** (`SubscriptionPayment` + `SlipOcrAmountMatches` + อนุมัติ→ต่ออายุ) ซึ่ง **ไม่ผูกกับ add-on เลย** และการ Reject สลิปก็ **ไม่ปิดฟีเจอร์ใด ๆ** |

---

## 2. สิ่งที่ทำถูกแล้ว (อย่าไปแก้)

- **ด่านกติกาการจองอยู่ฝั่งเซิร์ฟเวอร์ทั้งหมด** — `LodgingService.Reservations.cs:325-328`
  ตรวจ `OnlineBookingEnabled` · `RequireGuestIdNumber` · `AcceptHouseRules` และ
  `:94-103` ตรวจจำนวนคืน/ล่วงหน้า ⇒ แก้ HTML ฝั่งแขกแล้วข้ามไม่ได้
- **ช่องฝั่งพนักงานถูกตัดทิ้งเสมอ** ที่ทางเข้าสาธารณะ
  (`LodgingPublicController:64` — `req with { Source = null, …, ConfirmImmediately = false }`)
  ⇒ แขกยืนยันการจองให้ตัวเองไม่ได้
- **เส้นเงินเดินผ่าน `IDocumentService` ตัวเดียว** ตามกฎ CLAUDE.md
  (มัดจำ `Receipt(IsDeposit)` → เช็คเอาต์ `TaxInvoice` + `DepositApplied*` →
  ยกเลิก `RefundDeposit`/`RealizeDeposit`) — ไม่มีการออกเลขเอง
- **`ConfirmAsync` ตรวจห้องว่างซ้ำตอนเงินเข้า** และ **ปล่อยให้ throw** เมื่อ oversold
  แทนที่จะกลืน — ถูกตามกฎ "ห้าม catch {} ในเส้นเงิน"
- **หน้าเว็บ escape ครบ** — `Store.esc` / `Store.jsArg` ทุกจุดที่ต่อค่าของแขก
- **สลิปตั้งชื่อไฟล์เป็น GUID** และอยู่ในลิสต์ `publicUploadPrefixes` อย่างตั้งใจ
  (มีคอมเมนต์อธิบายไว้) — ไม่ใช่การหลุดโดยบังเอิญ

---

## 3. งานที่ต้องทำต่อ (สำหรับ Opus) — เรียงตามผลกระทบ

> กติกาเดิมทุกข้อ: reproduce ก่อนแก้ · logic เงิน/ภาษีแตกเป็น pure class + เทสต์
> ในคอมมิตเดียว · checker ใหม่ต้องผ่าน negative test · อัปเดต
> `DOCUMENT_FLOW.md` / `ACCOUNT_STRUCTURE.md` / `TEST_PLAN.md` ในคอมมิตเดียวกัน

### LDG-P0-01 · ทางจ่ายเงินออนไลน์ของ "แขก" ไม่มีอยู่จริง
**อาการ:** `LodgingReservationPaymentHandler` + `SiteOrderPaymentHandler` เขียนครบ
แต่ทางเข้าเดียวที่สร้าง `PaymentIntent` ได้คือ `PaymentGatewayController`
ซึ่งเป็น `[Authorize]` ⇒ **ไม่มีลูกค้าปลายทางรายไหนจ่ายออนไลน์ได้เลย**
ทั้งร้านค้าและที่พัก — handler สองตัวเป็นโค้ดที่ไม่มีวันถูกเรียก

**ทำ:**
1. `PublicPaymentController` (`[AllowAnonymous]`) — สร้าง intent ได้เฉพาะเมื่อ
   **พิสูจน์สิทธิ์ด้วยของที่ผู้เรียกถืออยู่แล้ว**: `reservationToken` (ที่พัก) /
   `sessionToken` (ตะกร้า) — **ห้ามรับ `SourceId` ดิบ ๆ** (ไม่งั้นใครก็สร้าง
   intent ให้การจองของคนอื่นได้ = IDOR)
2. **ยอดต้องมาจากเซิร์ฟเวอร์** — คิดจาก `DepositRequired`/`BalanceDue` ของแถวนั้น
   ห้ามเชื่อ `Amount` ที่ส่งมา (คลาสเดียวกับ "ค่าที่แต่งขึ้น")
3. Rate limit ต่อ token + ต่อ IP · ปิดทางสร้าง intent ซ้ำไม่จำกัด
4. `storefront.html`: ปุ่ม "ชำระออนไลน์" บนหน้า `/reservation/{token}` →
   PromptPay QR / บัตร → poll `/status` → สำเร็จแล้ว re-render หน้าเดิม
5. **fallback ต้องอยู่เสมอ**: ที่พักที่ยังไม่เปิด gateway เห็นเฉพาะแนบสลิป
   (resolver ตัวเดียวตอบว่า "ที่พักนี้จ่ายออนไลน์ได้ไหม" — ห้ามให้หน้าเว็บเดาเอง)

### LDG-P0-02 · สลิปปฏิเสธไม่ได้ (silent no-op ของจริง)
**อาการ:** แขกอัปสลิปปลอม → เจ้าหน้าที่เปิดดู → **ไม่มีปุ่มปฏิเสธ** ทางออกเดียวคือ
ยกเลิกทั้งใบ · หน้าแขกค้าง "รอที่พักตรวจสอบ" ตลอดไป · `PaymentSlipUrl` ค้างบนแถว

**ทำ:**
1. `POST /lodging/reservations/{id}/reject-slip` (ฝั่งพนักงาน) รับ `reason` **บังคับ**
   → ล้าง `PaymentSlipUrl`/`SlipUploadedAt`/`PaymentReference` ·
   เก็บประวัติลง `LodgingReservationSlipReview` (ใครปฏิเสธ · เมื่อไร · เหตุผล ·
   URL เดิม) — **ห้ามลบไฟล์จริง** (อาจต้องใช้เป็นหลักฐาน)
2. **ต่ออายุ hold** ให้แขกมีเวลาส่งใหม่ (ไม่งั้นปฏิเสธ = ปล่อยห้องทันที)
3. หน้าแขกขึ้นสถานะ "สลิปไม่ผ่าน: {เหตุผล} — กรุณาส่งใหม่" + เปิดฟอร์มอัปใหม่
4. แจ้งแขก (อีเมล/LINE ถ้ามี) — การกระทำที่ทำให้แขกต้องทำอะไรต่อ ห้ามเงียบ
5. นับจำนวนครั้งที่ถูกปฏิเสธ ≥ N → เจ้าหน้าที่กด **"ปิดรับสลิปของการจองนี้"**
   (ตรงกับที่เจ้าของโปรเจกต์ขอ: สลิปปลอม → ปิดฟังก์ชันได้)

### LDG-P0-03 · เปิด add-on ได้ฟรีทันที ไม่มีทั้งจ่ายและสลิป
**อาการ:** `MeteringController.ToggleFeature` เปิดฟีเจอร์ให้ทันทีแค่ติ๊กยอมรับ
ค่าใช้จ่าย → เก็บเงินทีหลังผ่าน job รายเดือน · ไม่เก็บก็ไม่มีอะไรปิดให้

**ทำ (ตรงตามที่เจ้าของสั่ง — สองเส้นทาง เส้นเดียวกันต้นทาง):**
1. `AddOnPurchase` (แถวการซื้อ) + สถานะ
   `AwaitingPayment → PendingReview → Active | Rejected`
2. **มี gateway** → `PaymentIntent(SourceKind = AddOnPurchase)` → webhook สำเร็จ →
   `SetFeatureEnabledAsync` ทันที (handler ตัวใหม่ ใช้โครง `IPaymentCompletionHandler` เดิม)
3. **ยังไม่มี gateway** → แนบสลิป (ใช้เส้น `SubscriptionPayment` ที่มี
   `SlipOcrAmount`/`SlipOcrAmountMatches` อยู่แล้ว **อย่าเขียนตัวตรวจสลิปตัวที่สอง**)
   → สถานะ `PendingReview` · **ฟีเจอร์เปิดให้ใช้ชั่วคราวได้ แต่ต้องติดป้าย**
   "รอตรวจสอบการชำระเงิน"
4. **admin ปฏิเสธ → ปิดฟีเจอร์จริง**: `RejectPaymentAsync` ต้อง cascade ไป
   `SetFeatureEnabledAsync(false)` + audit + อีเมลแจ้ง — วันนี้ Reject
   **ไม่ทำอะไรกับสิทธิ์เลย**
5. `IEntitlementService` ต้องอ่านสถานะนี้ (ไม่ใช่แค่ "เปิด/ปิด") ⇒ ตัวตัดสิน
   อยู่ที่เดียวตามเดิม

### LDG-P1-04 · หลักฐานการจองให้แขกโหลดเก็บ
**ทำ:** `GET /lodging/reservations/{token}/voucher.pdf` (`[AllowAnonymous]`, token เท่านั้น)
- เนื้อหา: เลขที่จอง · ชื่อที่พัก/ที่อยู่/โทร/แผนที่ · วันเช็คอิน-เอาต์ + เวลา ·
  ประเภทห้อง/จำนวน/ผู้เข้าพัก · รายการราคาแยกบรรทัด · มัดจำ/คงเหลือ ·
  **นโยบายยกเลิกแบบ snapshot** · กติกาที่พัก · QR กลับมาหน้าการจอง
- **ต้องไม่ใช่เอกสารภาษี** — ห้ามมีคำว่า "ใบกำกับภาษี/ใบเสร็จรับเงิน" บนหัว
  (ใบเสร็จมัดจำเป็นคนละใบและออกผ่าน `IDocumentService` อยู่แล้ว)
- เดินผ่าน **renderer ตัวเดียวกับที่ระบบใช้** และผ่าน `DocumentLabels` —
  ห้ามฝัง string literal (กฎ "สอง renderer ห้าม drift")
- ปุ่ม "⬇ บันทึกหลักฐานการจอง" บนหน้า `/reservation/{token}` +
  แนบไปกับอีเมลยืนยัน (ถ้ามีอีเมล)

### LDG-P1-05 · หน้าเว็บไม่ได้แสดงสิ่งที่เก็บไว้แล้ว
**ทำ (แก้ที่ `storefront.html` อย่างเดียว ไม่ต้องแตะ backend):**
- แกลเลอรีรูปที่พัก (`info.images`) + สิ่งอำนวยความสะดวก (`info.amenities`) +
  คำอธิบาย (`info.description`) + แผนที่ (`info.mapUrl`) + ติดต่อ — **ทั้งหมดมีใน
  payload อยู่แล้ว ไม่มีใครวาด**
- ประเภทห้อง: รูปหลายใบ + lightbox (วันนี้ `images[0]` ใบเดียว)
- `info.onlineBookingEnabled === false` → **ซ่อนฟอร์มจอง** แล้วโชว์ช่องทางติดต่อ
  (วันนี้โชว์ฟอร์มปกติ แล้วไปตายที่เซิร์ฟเวอร์ตอนกดจอง = ผู้ใช้กรอกฟรี)
- **ตัวอัปโหลดรูป**ในหน้าตั้งค่า (วันนี้ให้พิมพ์ URL เอง — ลูกค้า SME ทำไม่ได้)
  → ใช้ `ProcessAndSaveAsync` + prefix ใหม่ `/uploads/lodging` และ
  **ต้องเพิ่มใน `publicUploadPrefixes`** (`tools/upload_route_check.py` บังคับอยู่แล้ว)

### LDG-P2-06 · เก็บงานค้างที่รู้ตัวอยู่แล้ว
- `EarlyCheckInFee` / `LateCheckOutFee` มีคอลัมน์ + หน้าตั้งค่า แต่ **ไม่มีใครอ่าน**
  (จดไว้ใน `LODGING_TAKETIME_ANALYSIS.md` §2 แล้ว) → ต่อสายเข้า `AddCharge` ตอนเช็คอิน/เอาต์
- สลิปที่พักอยู่ใน `publicUploadPrefixes` — ไฟล์มีชื่อผู้โอน/เลขบัญชี = PII
  พิจารณาย้ายไปเส้นเดียวกับ `/uploads/attachments` (มี endpoint ตรวจสิทธิ์ของตัวเอง)
  แล้วให้แขกดูของตัวเองผ่าน token — **ตัดสินใจอย่างตั้งใจ ไม่ใช่ปล่อยตามเดิม**
- ไม่มีอีเมล/LINE ยืนยันการจองอัตโนมัติ (`NotifyOwnerOnBooking`/`NotifyEmails` มี
  คอลัมน์แล้ว — ตรวจว่ามี call site จริงหรือยังก่อนเขียนใหม่)

---

## 4. ผลตรวจคุณภาพของงานรอบก่อน (49e9014 → 920f377)

### พบและ**แก้แล้วในรอบนี้**
| ID | เรื่อง | ความรุนแรง |
| --- | --- | --- |
| REV-01 | `DocumentService.cs:12047,12049` — `"` ASCII ในสตริงธรรมดา **ปิดสตริงกลางคำ** ⇒ CS1002 ล้มทั้ง solution (ผมเขียนเองใน `802f149`) | **ล้ม build** |
| REV-02 | `SequenceNumber` เตือน "ออกเลขนอก transaction" ด้วย `Debug.WriteLine` ⇒ **หายไปทั้งหมดใน Release** ทั้งที่คอมเมนต์เขียนว่า "ต้องดัง" | สูง |
| REV-03 | `DOCUMENT_FLOW.md` **ไม่ถูกอัปเดต** ใน 4 คอมมิตที่เปลี่ยน flow (C-T04 ด่านงวดปิด · C-T11 retention) — ผิดกฎ hard requirement ของ CLAUDE.md ตรง ๆ | สูง (doc drift) |
| REV-04 | ไม่มี checker ตัวไหนดู `"` ในสตริง **ธรรมดา** — `verbatim_string_check` ตรวจเฉพาะ verbatim/raw | ช่องว่างของด่าน |

**กลไกที่เพิ่มเพื่อกันตัวที่สาม:** `tools/string_quote_close_check.py` (ตัวที่ 29)
— tokenizer ที่รู้จัก comment · char literal · verbatim · raw · **interpolation hole**
แล้วฟ้องเมื่อตัวปิดสตริงตามด้วยตัวอักษร/ตัวเลข/`{`
_negative test: บั๊กจริง 3 รูปแบบ → จับครบ 3 · รูปที่ถูก 8 แบบ (`{x ?? ""}` ·
`{id.ToString("N")}` · `$"…"[..14]` · `@"…""…"""` · raw string) → ไม่ฟ้องสักจุด ·
ทั้งเรพ 864 ไฟล์ → 0 จุด_

_บทเรียนซ้อนตอนเขียน checker (ผ่าน 4 รุ่น):_
1. _slice `text[i:]` ในลูป = O(n²) กับไฟล์ 1 MB ⇒ **รันไม่จบใน 120 วิ** — ต้อง `match(text, i)`_
2. _ไม่ track interpolation hole ⇒ ฟ้องผิด **382 จุด** (ซ้ำรอยบทเรียนของ `verbatim_string_check`)_
3. _ตอนแก้ลิสต์ตัวปิด **ทำ `,` หล่นหาย** ⇒ ฟ้องผิดพุ่งเป็น **16,791 จุด** —_
   _ตัวเลขที่พุ่งผิดปกติหลังแก้จุดเล็ก ๆ คือสัญญาณว่า state machine desync ไม่ใช่ "เจอบั๊กเพิ่ม"_
4. _`"…"[..14]` (range indexer) ถูกต้องตามภาษา ต้องอยู่ในลิสต์ตัวปิด_

### ยังต้องทำ (ไม่ได้ทำในรอบนี้ — งานของ Opus)
| ID | เรื่อง |
| --- | --- |
| REV-05 | **ยังไม่เคยคอมไพล์เลยทั้งชุด** (env ไม่มี .NET SDK) · จุดเสี่ยงสูงสุดที่ checker มองไม่เห็น: `Program.cs` บล็อก DataProtection (`AddKeyManagementOptions` + `Npgsql`) · positional record ที่เพิ่มพารามิเตอร์ (`CreateApiKeyRequest` · `ApiKeyResponse` · `OcrCorrectionRequest`) → CS1739 ที่ทุกจุดสร้าง · `IPayrollService.IsEmployeeOfUserAsync` (ตรวจแล้ว: implementer เดียว ✅) |
| REV-06 | **`throw` ใหม่ 3 ตัวเปลี่ยนพฤติกรรมของเส้นที่เคยผ่าน** — งวดปิด (9 จุด) · แก้เงินได้โดยไม่ส่ง WHT · ที่ดิน + ค่าเสื่อม ⇒ ต้อง audit ข้อมูลจริงก่อน deploy ว่ามีกี่บริษัทที่งวดเก่ายัง `Closed` อยู่แล้วจะเจอ 400 ทันที + เขียน release note |
| REV-07 | `JobLock` **จอง connection จาก pool ตลอดอายุงาน** (session-level lock ผูกกับ connection) — งานที่รันนานจะกิน pool · วัดจริงแล้วค่อยตัดสินว่าต้องแยก connection เฉพาะล็อกไหม |
| REV-08 | migration retention `UPDATE "Documents" … GREATEST(…)` **รันทุกครั้งที่ boot** และสแกนทั้งตาราง → ใส่ `WHERE "RetentionUntil" IS NULL OR …` ให้แคบลง หรือทำเป็น one-shot ที่จำว่าเคยรันแล้ว |
| REV-09 | **เทสต์ที่เขียนใหม่ยังไม่เคยรัน** (`DisplayTextTests` · `SequenceNumberTests` · `AiPromptSanitizerTests`) — "control ที่ไม่มีเทสต์ยืนยัน = ไม่มี control" ใช้กับเทสต์ที่ไม่เคยรันด้วย |

---

## 5. Backlog หลักที่ยังเหลือ (`SYSTEM_REVIEW_2026-09.md`)

**P0 = 0 · P1 = 31 · P2 = 43 · P3 = 20** (ติ๊กแล้ว 84 แถว) — แบ่งเป็น 3 กลุ่ม:
1. **ฟีเจอร์ใหม่** — ภ.ง.ด.50 export · TFRS บทที่ 9 ค่าเผื่อหนี้สงสัยจะสูญ ·
   ไฟล์โอนเงินเดือนเข้าธนาคาร · หน้าข้อมูล ปกส./PVD รายคน · ปฏิทินวันหยุด
2. **ออกแบบใหม่ (ต้อง scope แยก)** — ยุบสองแดชบอร์ด · ชั้นเขียนสต็อกเดียว ·
   component layer · migration lock + `CONCURRENTLY`
3. **ต้องตัดสิน "ต่อสายหรือลบ"** — E-commerce 460 บรรทัดที่ไม่มี UI ·
   Open Banking ที่คอมเมนต์เขียนว่า "simulate" · `api.js` 101 เมธอดที่ไม่มีหน้าไหนเรียก
   → **ห้ามตัดสินเองโดยไม่ถามเจ้าของโปรเจกต์**

---

_Last verified against codebase: 2026-09-04_

---

## 6. สถานะหลังรอบ 126b — ทำอะไรไปแล้วบ้าง

| ID | สถานะ | สิ่งที่ทำ |
| --- | --- | --- |
| **LDG-P0-01** | ✅ | `PublicPaymentController` (`[AllowAnonymous]`) + `IPublicPaymentResolver` · `PayWidget` เพิ่มโหมด `publicBase` (**ไม่ส่ง sourceId/amount**) · ปุ่มชำระออนไลน์บนหน้า `/reservation/{token}` และหน้า order-success · `GET public-pay/methods` บอกว่าเปิด gateway ไหม (หน้าเว็บห้ามเดา) · เพดาน 20 intent/ชม./source · ตรวจ `SourceKind+SourceId` ซ้ำทุกครั้งที่อ่านสถานะ |
| **LDG-P0-02** | ✅ | `RejectSlipAsync` + `POST reservations/{id}/reject-slip` · เหตุผลบังคับ · ต่อ hold 24 ชม. · อีเมลถึงแขก · `SlipUploadBlocked` · UI ทั้งฝั่งพนักงาน (ปุ่ม + รายการเหตุผลสำเร็จรูป) และฝั่งแขก (ป้ายเหตุผล + ซ่อนฟอร์มเมื่อถูกปิด) |
| **LDG-P0-03** | ✅ | `AddOnPaymentStatus` + ฟิลด์บน `CompanyFeature` + migration · `AddOnPaymentPolicy` (pure + 9 เทสต์) · `AddOnPurchaseService` · `AddOnPurchasePaymentHandler` (`PaymentSourceKind.AddOnPurchase`) · endpoint ลูกค้า (intent/slip) + แอดมิน (คิว + อนุมัติ/ปฏิเสธ) · **ปฏิเสธ = ปิดฟีเจอร์จริง** ผ่าน `SetFeatureEnabledAsync` + audit + อีเมล |
| **LDG-P1-04** | ✅ | `Helpers/LodgingVoucherBuilder` (pure) + `GET reservations/{token}/voucher.pdf` · ปุ่ม "⬇ บันทึกหลักฐานการจอง (PDF)" · 8 เทสต์ รวมด่านคำต้องห้าม + culture th-TH + escape |
| **LDG-P1-05** | ✅ | storefront วาดแกลเลอรี/สิ่งอำนวยความสะดวก/คำอธิบาย/แผนที่/ติดต่อ/กติกาที่พัก · lightbox · รูปหลายใบต่อประเภทห้อง · ซ่อนฟอร์มเมื่อ `onlineBookingEnabled=false` · **ตัวอัปโหลดรูป** (`POST lodging/images` + prefix `/uploads/lodging`) |
| **LDG-P2-06** | ✅ | `EarlyCheckInFee`/`LateCheckOutFee` ต่อสายแล้ว · **สลิปออกจาก `publicUploadPrefixes`** → เสิร์ฟผ่าน endpoint ที่มีด่าน (`Helpers/LodgingSlipPath` + 20 เทสต์) · **LINE แจ้งเจ้าของ**เมื่อมีจอง/สลิปใหม่ |
| **REV-08** | ✅ | migration retention เพิ่มเงื่อนไข `RetentionUntil <` ค่าใหม่ ⇒ boot รอบสองเป็นต้นไปแตะ 0 แถว |

### ⚠️ แก้บันทึกของตัวเอง — ที่เขียนไว้รอบ 126 ผิด 1 ข้อ

ในตาราง §3 (LDG-P2-06) ผมเขียนว่า *"ไม่มีอีเมล/LINE ยืนยันการจองอัตโนมัติ"* —
**ครึ่งแรกผิด**. `LodgingService.TryNotifyAsync` มีอยู่แล้วและถูกเรียกจริงทุกจุด:

| เหตุการณ์ | ถึงใคร | มีมาตั้งแต่ |
| --- | --- | --- |
| `created` — รับคำขอจอง (พร้อมลิงก์ token + กำหนดชำระมัดจำ) | แขก | รอบ 124 |
| `confirmed` — ยืนยันการจอง (พร้อม `ConfirmationMessage` + กติกาที่พัก) | แขก | รอบ 124 |
| `created` / `slip` — จองใหม่ · แขกส่งสลิป | เจ้าของ (`NotifyEmails`) | รอบ 124 |
| `slip-rejected` — สลิปไม่ผ่าน + เหตุผล | แขก | รอบ 126 |
| `created` / `slip` — เข้ากลุ่ม LINE ของบริษัท | เจ้าของ | **รอบ 126b** |

ที่ขาดจริงคือ **LINE เท่านั้น** — และเฉพาะฝั่งเจ้าของ. ฝั่งแขกส่ง LINE **ไม่ได้**
เพราะแขกจองแบบไม่ล็อกอิน เราไม่มี LINE user id ของเขา (`Property.LineId` เป็น id
ไว้ให้แขกติดต่อที่พัก ไม่ใช่ปลายทาง push) — ไม่แต่งให้ดูเหมือนมี

_(กติกา CLAUDE.md: "ผลตรวจจากทีม/agent ต้อง verify ก่อนเชื่อ — มันผิดได้ทั้งสองทาง"
และ "เมื่อพบว่าที่รายงานมาผิด ให้บันทึกไว้ด้วยว่าผิด ไม่ใช่แค่ข้ามไปเงียบ ๆ")_

### 🔎 พบเพิ่มระหว่างแก้ — นอกขอบเขต LDG แต่คลาสเดียวกัน

`/uploads/slips` (สลิปค่าบริการที่ผู้เช่าโอนให้แพลตฟอร์ม) **ยังอยู่ใน
`publicUploadPrefixes`** — ความเสี่ยงเดียวกับสลิปที่พักทุกประการ (ชื่อผู้โอน +
เลขบัญชี + ยอด · ชื่อไฟล์ GUID · URL ถูกส่งกลับใน API ของหน้าแอดมิน) ·
ไม่แก้ในรอบนี้เพราะกระทบ `SubscriptionController` + หน้าแอดมิน + เส้นซื้อ add-on
ที่เพิ่งต่อ ⇒ ต้องทำเป็นงานของตัวเองพร้อมเทสต์ · **ตัวด่านพร้อมใช้แล้ว**
(`LodgingSlipPath` รับ root/prefix เป็นพารามิเตอร์ ⇒ ทำตัวคู่ขนานได้ทันที)

### ยังเหลือ (ของรอบถัดไป)
- **REV-05** ยังไม่เคยคอมไพล์ — checker 29 ตัวผ่านหมดแต่ไม่ทดแทน `dotnet build`
- **REV-06** `throw` ใหม่เปลี่ยนพฤติกรรม (งวดปิด · WHT · ที่ดิน) ต้อง audit ข้อมูลจริง + release note
- **REV-07** `JobLock` จอง connection ตลอดอายุงาน — วัดจริงก่อนตัดสิน
- **REV-09** เทสต์ใหม่ 6 ไฟล์ยังไม่เคยรัน
- **PII-SLIP-02** ย้าย `/uploads/slips` (สลิปค่าบริการของแพลตฟอร์ม) ออกจาก public prefix ด้วยวิธีเดียวกัน
