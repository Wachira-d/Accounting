fastapi==0.115.0
uvicorn[standard]==0.30.0
paddleocr==2.9.1
paddlepaddle==2.6.2
python-multipart==0.0.9
httpx==0.27.0
Pillow==10.4.0
numpy==1.26.4
pydantic==2.9.0
python-json-logger==2.0.7
pdf2image==1.17.0
# EasyOCR — pure-Python OCR ensemble partner for PaddleOCR.
# Different model architecture (CRNN vs PP-OCRv4) gives real ensemble value
# vs running the same engine twice. Pulls torch as a heavy dep but the
# tradeoff is genuinely better Thai-text accuracy on low-confidence regions.
easyocr==1.7.2
