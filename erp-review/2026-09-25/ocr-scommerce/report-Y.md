# ทีม Y (รอบ 195) — มุมนักบัญชี ใบ Scommerce TXE05202609T004679

> subagent เขียนไฟล์ไม่ได้ — main agent บันทึกจากรายงานที่ส่งกลับ · อ่านอย่างเดียว

## ต้นเหตุ (ตรงกับทีม X — ข้อความคำเตือนจำลองได้ตรงหน้าจอทุกคำ)
- ไม่มีสัญลักษณ์ V/N ท้ายบรรทัด ⇒ `OcrLineVatMarks.Assign` ไม่ทำอะไร ⇒ อัตรามาจาก `OcrService.cs:6759-6771` → `ThaiVatTypeRule.Suggest`
- `ThaiVatTypeRule.cs:35` "นม" ⇒ "นมผงเอนฟาโกร…" = ยกเว้น (−1) · ตัดทิ้งมี "นมข้นหวาน" แต่ไม่มี "นมผง" · ค่าส่ง 0.00 = 7% ⇒ `SpreadHeaderVat` ฐานที่เสีย VAT = 0
- กระดาษพิสูจน์ทั้งใบเสีย VAT สองทาง: 7% × 4,695.33 = 328.67 พอดี + พิมพ์ "ยกเว้นภาษีมูลค่าเพิ่ม 0.00" — ตัวเดาจากคำชนะกระดาษ (ขัด DOCTRINE G1)
- **หัวใบกับบรรทัดขัดกัน**: หัวคง VAT 328.67 / รวม 5,024 (`OcrService.cs:6291-6294`) แต่บรรทัด VAT 0 — ยังไม่ยืนยันว่า JE ตอนอนุมัติ/บันทึกซ้ำใช้ฝั่งไหน

## 1. ชนิดเอกสาร
- ใบสำคัญจ่าย ถูก (`OcrDocumentRoleInferrer.cs:462`) · `HasTaxInvoiceReference` ตั้ง (`OcrService.cs:6214,6328`) · ภ.พ.30 รับ (`TaxService.cs:818-844`) · §82/5 ไม่มีข้อไหนบล็อก ยกเว้น (3) วัตถุประสงค์ธุรกิจ = ไม่ทราบ
- ความเสี่ยงแฝง: หมายเหตุ "ยกเลิกใบกำกับภาษีอย่างย่อ…" เข้าตัวจับใบอย่างย่อ · ตัวตรวจคำปฏิเสธ (:682-710) ไม่นับ "ยกเลิก" · รอดเพราะเลขผู้ซื้อผ่าน mod-11 (:364-371)
- `DOCUMENT_FLOW.md:1918` อ้าง "PV master switch" ที่ไม่มีในโค้ด

## 3. ผัง 52120 = "วัสดุสิ้นเปลือง (บริการ)" ใต้ 52 ต้นทุนบริการ (`ChartOfAccountTemplates.cs:217`)
- ไม่มีข้อมูลว่าบริษัทขายอะไร นอกจาก `IndustryType` · `BusinessPurpose` (กฎ #2 B) ปรากฏในโค้ด 0 ครั้ง
- ที่มาของ 52120 ระบุไม่ได้โดยไม่มีแถวสแกน (`[Reasoning]`) — ผู้สมัคร: learner · NaiveBayes · VendorIntel · student/AI · baseline ข้ามบริษัท
- เสนอ: **ถาม ไม่เดา** เฉพาะหมวดเสี่ยงที่บริษัทไม่มีประวัติ (ไม่ถามทุกใบ — G5)

## 4. Cr 21230 ไม่ได้มาจาก fallback ผังเลขต่ำสุด (เส้นนั้นเลือกแค่ 111x) — มาจาก `DefaultPaymentAccountId` / engine / ผู้ใช้แก้ (`OcrService.cs:2214/4554/4748`) · กระดาษไม่บอกวิธีจ่าย · หน้าเอกสารไม่บอกที่มา

## 5. ส่วนลด — ฐานภาษี/ต้นทุนเท่ากันทั้งสองแบบ (§79(1)) ต่างแค่การแสดง · 4.41% ไม่ใช่ตัวเลขบนกระดาษ และใส่ให้บรรทัดค่าส่ง 0.00 ด้วย

## 6. ใบนี้แทนใบอย่างย่อ D20260918000097 — ไม่มีตัวอ่านหมายเหตุนี้ · ตรวจซ้ำเทียบแค่ `SupplierInvoiceNumber`/`DocumentNumber` กับ TXE… (:8835-8890) · ใบอย่างย่อเดิมจะมี `SupplierInvoiceNumber = null` เลขอยู่ `Reference` · ตัวจับใบก่อนหน้าไม่ดูใบสำคัญจ่าย

## 7. คำเตือน — สามข้อรากเดียว · ท่อน "ตอนนี้…" ต่อท้ายทุกข้อ (`OcrApprovalGapWarning.cs:44`) · ข้อ 1 บอกเหตุผิด ("รายการที่อ่านไม่ได้") · บรรทัดค่าส่ง 0.00 ติด 7% กดข้อความที่เจาะจงกว่า (`OcrAmountIntegrity.cs:112`) · "ยังไม่มีคำแนะนำ" เพราะรอ AI ทั้งที่คำตอบเป็นเลขคณิต · เส้น OCR ไม่อ่าน `VatTypeInference` (`AiSuggestionController.cs:509`)

## ข้อเสนอ
- **P0** helper pure พิสูจน์ "ทั้งใบเสีย VAT" จากเลขคณิตกระดาษ — ชนะเฉพาะอัตราที่เดา ไม่ทับ engine/ผู้ใช้ · เทสต์สองครึ่ง · ยืนยัน JE หัวใบ vs บรรทัด
- **P1** เส้น OCR อ่าน/บันทึก `VatTypeInference` · ตัวอ่าน "ยกเลิก…แทน" หาใบเดิมจาก `Reference` เสนอสลับหลักฐาน · "ยกเลิก" เป็นคำปฏิเสธ · รวมคำเตือนรากเดียว + ปุ่ม "ตั้งอัตราตามกระดาษ" ผ่านเส้น repopulate เดิม
- **P2** แสดงที่มาของผังเดบิต/แหล่งเงินบนหน้าเอกสาร · ไม่ใส่ % ส่วนลดบนบรรทัด 0.00

## คำถามให้เจ้าของตัดสิน
- Q1 ถามวัตถุประสงค์ธุรกิจไหม (ขายต่อ/ใช้ในกิจการ/สวัสดิการ/ส่วนตัว) · ถ้าส่วนตัวและกรรมการจ่าย ลงบัญชีไหม
- Q2 หจก.แอม แฮปปี้เนส ขายอะไร · `IndustryType` ตั้งถูกไหม
- Q3 ใบอย่างย่อที่ลงไปแล้ว: ลงรายการสลับหลักฐาน หรือ void
- Q4 ส่วนลดท้ายบิล: เก็บเป็นส่วนลดท้ายบิล หรือกระจายรายบรรทัด
- Q5 กระดาษไม่บอกวิธีจ่าย: ใช้ค่าเริ่มต้นบริษัท หรือเว้นว่าง
- Q6 ให้ระบบทับอัตรา VAT ที่ตัวเองเดาได้ไหม เมื่อเลขคณิตกระดาษพิสูจน์
