# ทีม X (รอบ 195) — ไล่ไปป์ไลน์สกัดข้อมูลกับใบ Scommerce TXE05202609T004679

> subagent เขียนไฟล์ไม่ได้ — main agent บันทึกจากรายงานที่ส่งกลับ · main agent เปิดยืนยันแล้ว: `ThaiVatTypeRule.cs:35` มี "นม"/"milk" ·
> `OcrService.cs:6763-6770` เรียก `ThaiVatTypeRule.Suggest` กับทุกบรรทัดที่ `VatRate == null` · สคริปต์จำลอง (พอร์ตตรรกะจริงเป็น python):
> scratchpad `x195/sim_chain.py` · `sim_exempt.py` · `sim_marks.py`

## จุดแรกที่เพี้ยน
- `Helpers/ThaiVatTypeRule.cs:35` คำยกเว้น "นม"/"milk" · `LooksExempt` (:67-73) ตัดสินยกเว้นเมื่อคำอยู่ต้นคำ (ตำแหน่ง 0)
- เรียกที่ `OcrService.cs:6763-6770` (`BuildScanLinesAsync`) ⇒ บรรทัด "นมผงเอนฟาโกร…" = −1 (ยกเว้น) · บรรทัดค่าส่ง 0.00 = 7%
- `SpreadHeaderVat` (:165) เห็นฐานที่มี VAT = 0 ⇒ VAT 0 ทุกบรรทัด ⇒ `OcrAmountIntegrity` ฟ้อง 3 ข้อ (ผลจำลองตรงหน้าจอทุกตัวเลข)
- **แถว "ยกเว้นภาษี 0.00" บนกระดาษไม่ใช่สาเหตุ** — `OcrLineVatMarks.Assign` ออกที่ :198 (ไม่มีสัญลักษณ์ท้ายบรรทัด)
- หลักฐานเสริม: ยอด 4,695.33 ไม่ใช่ 4,695.34 เพราะ `DocumentRounding` ข้ามบรรทัดอัตรา ≤ 0 (`OcrService.cs:6807-6810`)
- ผลจำลองเมื่อบรรทัดนมผงเป็น 7%: Amount 4,695.34 · VAT 328.67 · ผลต่างปัด −0.01 · รวม 5,024.00 · ไม่มี `[Σ-GAP]` (ตรง RND-01)

## ทำไม Total-first/ตัวกระทบยอดไม่ช่วย
- `OcrTotalDecomposer.Decompose` พิสูจน์ส่วนลดหักก่อน VAT (328.67 ปิดฐาน 4,695.33 พอดี) แต่ใช้แค่กระจายส่วนลด ไม่ส่งต่อถึงตัวตั้งอัตราบรรทัด
- `OcrLineReconciler` ดูแค่ยอดเงิน · `OcrLineVatMarks.Assign` "ทุกบรรทัดอัตราเดียวกันไม่แตะ" (:229) สมมติว่าเส้นเดิมให้ 7% ทั้งใบ — ผิดตั้งแต่ `5e3a323b`
- ไม่มีชั้นหลักฐาน "ตัวเลขหัวใบพิสูจน์ทั้งใบ" เหนือตัวเดาจากชื่อ (ขัด DECISION_DOCTRINE §1)

## ถดถอย
- เทียบก่อน 2026-09-04: **ใช่** — เดิม `headerVat > 0 ? 7 : 0` ทุกบรรทัด · `5e3a323b` ต่อ `ThaiVatTypeRule` เข้าเส้น OCR · `099b54ab` แก้ "ขนมปัง"/"milk tea" ไม่แตะ "นมผง"
- รอบ 192 ทำนายไว้ (`erp-review/2026-09-24/total-B.md:136-137` · RC-9 :173) แต่ไม่ได้แก้
- เทสต์รอบ 193 (`DocumentRoundingTests` ชุด Lazada) สมมติบรรทัด 7% อยู่แล้ว · `OcrReplayHarness.cs:119` ไม่มี `LineAmounts` ใบนี้ ⇒ ไม่มีเทสต์ตรวจการตั้งอัตราบรรทัด
- คลาสกว้าง: "นมสด" "นมเปรี้ยว" "นมถั่วเหลือง" "milk powder" "formula" = ยกเว้นทั้งหมด

## ส่วนลด 216.82 → 4.41% รายบรรทัด
- เคส C ของ `OcrLineReconciler` แล้ว `OcrService.cs:6886` เขียน 4.41% ลงทุกบรรทัด (รวมค่าส่ง 0.00) · `DiscountAmount` บรรทัด 1 = 216.82 ถูก · `ComputeLineAmounts` ใช้บาทก่อน % ⇒ ถ้าอัตราถูก ฐาน §87 ไม่เพี้ยน
- `BillDiscountAmount` ไม่ถูกใช้ · เสี่ยงกับใบผสม (Scommerce พิมพ์ส่วนลดแยกกลุ่มภาษี แต่ตัวกระจาย `OcrService.cs:6720-6733` เฉลี่ยทุกบรรทัด)

## ความเสียหายถ้ายอมรับ
- ภาษีซื้อ 328.67 หายจาก ภ.พ.30 · รายงานภาษีซื้อ §87 ลงคอลัมน์ "ยกเว้น" ผิด · ยอดจ่ายขาด 328.67
- เส้น OCR ไม่อ่าน/ไม่เขียน `VatTypeInference` (มีแค่ `AiSuggestionController.cs:511-544` หน้ากรอกมือ) ⇒ แก้กี่ครั้งใบถัดไปก็ยังผิด

## ข้อเสนอ
- **P1** helper pure `Helpers/OcrLineVatPlanner` รวมลำดับตั้งอัตรา (`OcrService.cs:6655-6771`): engine/ผู้ใช้ > สัญลักษณ์บนกระดาษ > **ทั้งใบพิสูจน์ด้วยตัวเลข** > `ThaiVatTypeRule` · เงื่อนไข: VAT หัวใบ > 0 · Σ บรรทัด = ฐานหัวใบ · |round(ฐาน×0.07) − VAT| ≤ ค่าเผื่อแคบ (หรือ 7/107 กรณีรวม VAT) · ยอดยกเว้นบนกระดาษ null/0 · เทสต์: ใบนี้ [7,7] · ครึ่งหลัง WholesaleMixedVat (53.48≠28) · Makro 951/49/1,000 (66.57≠49) · MakroPage3of3 · ใบส่งออก VAT 0 · ใบผสม "นมสด + น้ำปลา"
- **P2** `ThaiVatTypeRule` แคบลง ("นมผง"/"milk powder"/"formula" ไม่ยกเว้น) — ขอบเขตนมยกเว้น §81 ให้นักบัญชี/เจ้าของตัดสิน · ต่อสาย `VatTypeInference` เข้าเส้น OCR
- **P3** ส่วนลดท้ายบิลลง `BillDiscountAmount` ผ่านสูตร `AllocateBillDeductions` เฉพาะกลุ่มภาษีที่กระดาษระบุ (หลัง P1 แยกคอมมิต)
- **P4** `OcrApprovalGapWarning` เสนอ "ตั้งเป็น 7%" เมื่อผ่านกติกา P1 แทน "ยังไม่มีคำแนะนำ"
