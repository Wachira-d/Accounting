# รอบ 195 — ฝ่ายค้าน (ทีม I · `99824c52`) — อ่านอย่างเดียว

ขอบเขต: `OcrLineVatPlanner` · `BuildScanLinesAsync` · `ThaiVatTypeRule` P2 · `OcrAmountIntegrity`/`OcrApprovalGapWarning` · `ContainsAnyNotNegated`

## CONFIRMED

**C1 (P1 · ภาษีซื้อ) planner "พิสูจน์" ด้วย VAT ที่ระบบคำนวณขึ้นเอง ⇒ ถอดตาข่าย [Σ-GAP] ของใบยกเว้น**
- `SmartFieldExtractor.cs:527-535` ถ้าอ่านได้แต่ยอดรวม และข้อความมี "VAT"/"ภาษีมูลค่าเพิ่ม" ที่ใดก็ได้ (`:549` — รวมคำว่า "ยกเว้นภาษีมูลค่าเพิ่ม"/"NON VAT") ⇒ ระบบถอดเอง Sub = T/1.07, VAT = T − Sub **โดยไม่ผ่านด่านสินค้ายกเว้น/เลขผู้ขาย** ด่าน `VatBackCalcGuard.Decide` อยู่ใน `ParseThaiDocument` (`OcrService.cs:5560`) ซึ่งรัน**ก่อน** `Enrich` (`:528`→`:535`) ⇒ ตอนด่านปฏิเสธ มันเว้น VAT ไว้ว่าง แล้ว Enrich มาเติมทับทีหลัง
- `OcrLineVatPlanner.cs:103-111` (ราคารวม VAT) เช็กเงื่อนไข `round(T×7/107) == VAT` ซึ่งเป็นสูตรเดียวกับที่ใช้สร้าง VAT ตัวนั้น ⇒ ผ่านทุกครั้ง (F2 ข้อ 6)
- ตัวเลข: ใบร้านผักที่มีเลขผู้เสียภาษี 13 หลัก รายการ "ผักกาดขาว 500 · ผลไม้รวม 570" รวม 1,070 และไม่พิมพ์ VAT ⇒ VAT ที่ระบบคำนวณ = 70.00 · `Classify` ⇒ เคส A · planner ⇒ `AllStandard7` [7,7] พร้อมหมายเหตุ "[Σ] …**พิสูจน์จากตัวเลขหัวใบ**: VAT หัวใบ 70.00 = 7/107 × 1,070.00"
  - **ก่อนคอมมิตนี้:** บรรทัดเป็น −1,−1 ⇒ `OcrAmountIntegrity` (2) ขึ้น `[Σ-GAP] กระดาษมี VAT 70.00 แต่ทุกบรรทัด…` ⇒ `OcrPostingReadiness` บล็อกการอนุมัติอัตโนมัติ
  - **หลังคอมมิตนี้:** ไม่มี gap ⇒ `OcrController.cs:420` "สร้าง+อนุมัติ" และปุ่ม LINE "อนุมัติเลย" (`LineBotService.cs:666`) ผ่าน ⇒ **ภาษีซื้อ 70 บาทที่ไม่มีอยู่จริงเข้า ภ.พ.30** (§81 / §82/5) · readiness ไม่ดู `FieldConfidence` (0.5/0.7)
- ทางแก้: planner ต้องรับ "ที่มาของ VAT" มาด้วย (`FieldConfidenceJson[VatAmount] ≥ 0.85` หรือไม่มี `[VAT back-calc]`) ถ้าเป็นค่าที่คำนวณ ⇒ `Unknown` · เทสต์ทิศตรงข้าม `ใบผักรวม1070_VATคำนวณ_ไม่ตัดสิน`

**C2 (P2 · §82/5(2)) คำว่า "ยกเลิก" ในช่วง 14 ตัวอักษรกลืนหัวใบอย่างย่อจริง** — `OcrDocumentRoleInferrer.cs:700`
- ข้อความ `"สินค้าซื้อแล้วไม่รับคืน/ยกเลิกไม่ได้\nใบกำกับภาษีอย่างย่อ"` มีส่วนนำหน้า 13 ตัวอักษร (`ยกเลิกไม่ได้\n`) ⇒ **ก่อนแก้ = จับได้ · หลังแก้ = ถูกนับเป็นคำปฏิเสธ** (จำลองด้วยตรรกะเดียวกัน)
- ถ้าเจอคำนี้จุดเดียวบนกระดาษ และสลิปไม่พิมพ์ "ราคารวม VAT" (ทางสำรองเชิงโครงสร้าง `:381-399` ไม่ทำงาน) ⇒ ใบอย่างย่อถูกเคลมภาษีซื้อ
- ใบ Scommerce ไม่ต้องใช้กฎนี้อยู่แล้ว เพราะเลขผู้ซื้อผ่าน mod-11 (`:365`)
- ทางแก้: ถือว่าปฏิเสธเฉพาะเมื่อ prefix **ลงท้าย**ด้วย `ยกเลิก`/`การยกเลิก` (ติดคำอย่างย่อ) หรือมี "แทน"/"ฉบับใหม่" ตามหลัง · เพิ่มเทสต์ `ยกเลิกไม่ได้_แล้วขึ้นบรรทัดหัวใบอย่างย่อ_ยังจับได้`

## PLAUSIBLE

- **P1 (ข้อ 4) รวมคำเตือนโดยไม่ตรวจว่ามาจากรากเดียวกันจริง** — `OcrApprovalGapWarning.cs:58-66` รวม TotalMismatch เข้าคำแนะนำอัตราทุกครั้งที่ `RateAdvice` มีค่า แต่ไม่ได้เช็ก `diff ≈ vatGap` แบบที่ `OcrAmountIntegrity` ทำ
  - ตัวอย่าง: ฐาน 1,000 · VAT 70 · ยอดรวมบนกระดาษ 1,120 (มีค่าธรรมเนียมหลัง VAT 50 ที่อ่านไม่ได้) ⇒ ทั้งสามข้อถูกรวมเป็น "ตั้ง 7%" ข้อเดียว และข้อความบอกว่าเป็น "เรื่องเดียวกัน" ⇒ ส่วนต่าง 50 บาทไม่ถูกเรียกชื่อ (เหลือแค่ตัวเลขในท่อน "ตอนนี้…")
  - ยังต้องกดรับทราบอยู่ ⇒ ไม่ถอดด่าน
- **P2 (P2 กว้างเกิน/ขัดคำตัวเอง)** — `ThaiVatTypeRule.cs:60`
  - `"นม uht"` ⇒ "นม UHT รสจืด" (นมโคล้วนที่ผ่าน UHT ซึ่งตามแนวสรรพากรมักถือว่ายกเว้น §81(1)(ก)) กลายเป็น 7% ทั้งที่คอมเมนต์เขียนว่า "นมสดรอเจ้าของ"
  - `"formula"` ใช้ `Contains` ทั้งคำอธิบาย ⇒ "ปุ๋ย formula 15-15-15" ไม่ยกเว้นอีก และ `VatBackCalcGuard` ก็ไม่กันการถอด VAT ให้ใบนั้นด้วย
  - ผลกระทบ: คอลัมน์ §87 ผิด (Σ VAT ยังเท่ากระดาษ) · ควรส่งให้เจ้าของ/นักบัญชีตัดสิน
- **P3 ดึงรายการซ้ำ ไม่ล้าง `[Σ-GAP]` เก่า** — `OcrService.cs:7507-7590` ต่อหมายเหตุท้ายเดิมทุกครั้ง ⇒ หลัง planner แก้ถูกแล้ว คำเตือน 3 ข้อเดิมยังขึ้น (เพราะ `RateAdvice` เป็น null เมื่อทุกบรรทัด 7% ⇒ ไม่รวมข้อ) · ข้ออ้าง F3 ข้อ 10 จึงถูกแค่ครึ่งเดียว · ทิศปลอดภัย (บล็อก)
- **P4 ป้ายบอกที่มา** — เหตุผลของ planner อยู่แค่ใน `<pre>` ProcessingNotes · ช่องอัตรา VAT รายบรรทัดไม่มีป้าย "พิสูจน์จากตัวเลข" แยกจาก "อ่านจาก engine" · เมื่อรวมกับ C1 ป้ายนี้จะโกหก

## NOT-A-BUG

- **ค่าเผื่อ ±max(0.02, 0.005n)** ส่วนยกเว้นที่ถูกกลืนได้มากสุดคือ E ≤ slack/0.07:
  - n=10 ⇒ 0.71 บาท · n=100 ⇒ 7.14 บาท
  - Σ VAT = VAT บนกระดาษ ⇒ **ไม่ได้เคลมภาษีซื้อเกิน** มีแค่ฐานในคอลัมน์ขยับไม่ถึง 1 บาท
- **ทางเข้า (ข้อ 1)**
  - เส้นสร้าง/line-preview/ดึงรายการซ้ำ ⇒ `BuildScanLinesAsync` (`:6382/:7154/:7569`) · LINE ⇒ `CreateDocumentFromScanAsync` (`:8369`) · `SpreadHeaderVat` มีผู้เรียก 1 จุด
  - `DocumentService.cs:13239` แค่ sync ค่ากลับไปที่สแกน
- **ทิศตรงข้ามอื่น** planner คืน `Unknown` ≡ พฤติกรรมเดิม ในเคสเหล่านี้:
  - engine ให้ 0/−1 บางบรรทัด
  - ส่วนลดหลัง VAT
  - ส่งออก VAT 0
  - VAT ที่อ่านผิดแต่ไม่เท่ากับ 7% ของฐานพอดี
- `RateAdvice` ใช้กับใบราคารวม VAT ไม่ได้ (Amount = ยอดรวม ⇒ 7%×ยอดรวม ≠ VAT) ⇒ แค่ไม่แนะนำ ไม่ผิดทิศ

## คอมไพล์

- ไม่เห็นความเสี่ยงชัด:
  - `ContainsAnyNotNegated` เป็น internal และ csproj มี `InternalsVisibleTo`
  - `DocumentLine.VatRate` เป็น `decimal` ⇒ tuple เข้าชนิดกัน
  - `OutputVatRate.VatOn` · `OcrVatGroupTable.Found` มีอยู่จริง
- checker ที่รันผ่าน: using · record_arg · namespace_shadow · undeclared_local · tuple_name_merge · ocr_helper_test · dead_helper
- `required_call_site_check` เกินเวลา 120 วินาที ⇒ **ยังไม่ได้ตรวจ**
