# ทีม T1 — "สมองนักบัญชี" (Thai CPA + RD auditor + AP/AR clerk) — ตรวจไปป์ไลน์ OCR → เอกสาร → JE

branch: (ดู git log) · วันที่ตรวจ 2026-09-05 · วิธี: อ่านโค้ด + grep call site เท่านั้น (ไม่มี .NET SDK)
กติกา: ทุกข้อมี file:line ที่เปิดดูจริง · ไม่รายงานซ้ำสิ่งที่ปิดแล้ว (OCR-BRIEF ข้อ 6) · ไม่แก้โค้ด

> รายงานนี้เขียนแบบ append ทีละส่วน — ถ้าส่วนท้ายขาด แปลว่า session ถูกตัด

## §0 สรุป 5 บรรทัด (เติมตอนจบ)

(รอ)

## §1 Decision matrix — "นักบัญชีที่เก่งที่สุดในโลกตัดสินอะไรตอนลงเอกสาร 1 ใบ"

(กำลังเติม)

## §2 Findings (เรียง P0→P3)


### T1-01 🔴 [P1][S] JE ตรงจากสแกน (`CreateJournalEntryFromScanAsync`) ลงภาษีซื้อ/หัก ณ ที่จ่าย ด้วยรหัสผังที่**ไม่มีในผังมาตรฐาน** → ตกไป fallback "ชื่อมีคำว่า…" แล้วหยิบบัญชีผิด
- ไฟล์: `Accounting/Services/Implementations/OcrService.cs:5453-5461` · fallback `:5522-5535` · ผังมาตรฐาน `Accounting/Services/ChartOfAccountTemplates.cs:46-50,142-148`
- โค้ด: `ResolveTaxAccountAsync(companyId, new[] { "11511", "1151", "115" }, AccountType.Asset, "ภาษีซื้อ")` · `new[] { "21701", "2161", "2162" }, …, "หัก ณ ที่จ่าย"` · fallback `.Where(a.AccountType == type && a.AccountName!.Contains(nameKeyword)).OrderBy(a => a.AccountCode).First()`
- ทำไมพัง: (1) grep ผังมาตรฐาน: `11511`/`1151`/`115`/`21701`/`2161`/`2162` = **0 hit ทุกตัว** (ผังจริงคือ 11610 ภาษีซื้อ ภ.พ.30 · 21916/21917 ภ.ง.ด.3/53) → (2) ตกไปค้นชื่อ → ภาษีซื้อ: บัญชีที่ชื่อมี "ภาษีซื้อ" เรียงตามรหัสตัวแรกคือ **`116` "ภาษีซื้อรอเครดิต" (บัญชีคุมระดับ 3, string "116" < "11610")** ไม่ใช่ 11610 → (3) หัก ณ ที่จ่าย: บัญชีหนี้สินชื่อมี "หัก ณ ที่จ่าย" ตัวแรก = **`21914` ภ.ง.ด.1 (เงินเดือน)** ทั้งที่ใบผู้รับเหมาต้องเข้า 21916/21917 → (4) ถ้าบริษัทปิดบัญชีคุมไว้ (IsActive=false) หรือตั้งชื่อต่างออกไป → คืน null → **โค้ดพับยอด VAT เข้าค่าใช้จ่ายเงียบ ๆ** (`expenseAmt = total - vatLine` เมื่อ vatAcc null ⇒ vatLine=0) ⇒ เคลมภาษีซื้อหายไปทั้งก้อนโดยไม่มีใครบอก
- ผลกระทบ: JE จากปุ่ม "บันทึก JE" ของหน้าสแกน ลง VAT เข้าบัญชีคุม/บัญชีรอเครดิต ⇒ ภ.พ.30 กระทบยอด GL ไม่ตรง (TaxGlReconciliation) · WHT เข้าแบบผิด (ภ.ง.ด.1 แทน 3/53) ⇒ ยอดนำส่งรายแบบผิดทั้งสองแบบ
- defect class: `gl_code_check` (เลขผังบัญชี hardcode ชนความหมายผังมาตรฐาน) + "ค่า default ที่แต่งขึ้นเพื่อให้โค้ดเดินต่อได้" (พับ VAT เข้าค่าใช้จ่ายแทนที่จะล้มดัง)
- ทางแก้: ใช้ตัว resolve บัญชีภาษีตัวเดียวกับ `AutoPostToJournalAsync` (เส้นเอกสาร) — รหัส 11610/11640 · 21916/21917 ตาม `PayerFormType` ของคู่ค้า (บุคคล→ภ.ง.ด.3 / นิติบุคคล→53) · ไม่พบ = **throw** พร้อมบอกให้ตั้งผัง ไม่พับยอด
- ฝ่ายค้าน: "เส้น JE ตรงมีไว้สำหรับกรณีที่ไม่อยากสร้างเอกสาร ผู้ใช้เลือกบัญชีเดบิต/เครดิตเองอยู่แล้ว" → คำตอบ: ผู้ใช้เลือกได้แค่ Dr/Cr หลัก บัญชี VAT/WHT ระบบเลือกให้เองและไม่โชว์ในฟอร์ม (request มีแค่ `PostVat`/`PostWht` bool) — ผู้ใช้ไม่มีทางรู้ว่าลงผิดบัญชี
- ความมั่นใจ: สูง (เปิดผังแล้ว 0 hit) — ต้องเช็คต่อ: บริษัทที่ใช้ผังเก่า 4 หลัก (ถ้ามี) จะไม่โดน

### T1-02 🔴 [P0][M] เส้น "บันทึก JE ตรง" ข้ามด่านภาษีทุกด่านที่เส้นเอกสารมี — §82/5 (ใบย่อ/ค่ารับรอง/น้ำมัน), §82/3, tax point, หนังสือรับรอง 50 ทวิ, รายงานภาษีซื้อ
- ไฟล์: `OcrService.cs:5417-5511` (ทั้งเมธอด) เทียบกับ `:4809` (`vatNotClaimable` ใช้เฉพาะเส้นเอกสาร) · `:5200-5213` (ปิดเคลมรายบรรทัด เฉพาะเส้นเอกสาร)
- โค้ด: `var vat = (request.PostVat ? result.ExtractedVatAmount : 0m) ?? 0m;` — ไม่มีการอ่าน `ProcessingNotes.Contains("[VAT-CLAIM]")` เลยในเมธอดนี้ (grep `[VAT-CLAIM]` ในไฟล์: :914-941 ตอนสแกน, :4806-4809 และ :5200 เฉพาะ `CreateDocumentFromScanAsync`)
- ทำไมพัง: สแกนใบกำกับอย่างย่อ (7-Eleven/ปั๊มน้ำมันรถเก๋ง/ค่ารับรอง) → screener ตั้ง `[VAT-CLAIM]` ถูกต้อง → ผู้ใช้กดปุ่ม "บันทึก JE" แทน "สร้างเอกสาร" โดยติ๊ก `PostVat` (default UI ต้องเช็ค) → **Dr ภาษีซื้อเต็มจำนวน** ทั้งที่กฎหมายห้าม §82/5(2)/(4)/(6) → และเพราะเป็น JE ล้วน ไม่มี `Document` ⇒ ไม่ผ่าน `TaxPointResolver` (:DocumentService 5116) · ไม่ผ่านด่าน §82/3 ใน `TaxService` (อ่านจาก Documents) · ไม่ออก 50 ทวิ · ไม่มี `SupplierInvoiceNumber` ให้รายงานภาษีซื้อ §87 → ยอดอยู่ใน GL 11610 แต่ไม่อยู่ใน ภ.พ.30 (ตรงกับที่ `TaxGlReconciliationService:213` เตือนว่า "อยู่ใน GL แต่ไม่นับในแบบ")
- ผลกระทบ: P0 — เคลมภาษีซื้อต้องห้าม / GL กับแบบยื่นไม่ตรงถาวร / WHT ที่หักไว้ไม่มีหนังสือรับรองและไม่เข้าทะเบียนนำส่ง ภ.ง.ด.3/53
- defect class: "ด่านที่ครอบแค่ทางเดียว คือด่านที่ไม่มี" (C-T12 ปิดฝั่งเอกสารแล้ว แต่เส้น JE ตรงในไฟล์เดียวกันยังเปิด) + R3
- ทางแก้: (ก) อย่างน้อย: ถ้า `ProcessingNotes` มี `[VAT-CLAIM]` → บังคับ `PostVat=false` + บอกเหตุผล (ข) ทางที่ถูก: เส้น JE ตรงควรรับเฉพาะเอกสารที่ **ไม่มี VAT และไม่มี WHT** (ใบเสร็จค่าธรรมเนียม/บิลไม่จด VAT) — ใบที่มี VAT/WHT ต้องเดินเส้นเอกสารเสมอเพราะ ภ.พ.30/ภ.ง.ด. อ่านจาก Document (ค) ใน UI ซ่อนปุ่ม JE เมื่อ VAT>0 หรือ HasWht พร้อมบอกเหตุผล
- ฝ่ายค้าน: "นักบัญชีมืออาชีพอยากลง JE เร็ว ๆ เอง" → คำตอบ: ลงได้ แต่ระบบต้องบอกว่า "ใบนี้จะไม่ขึ้น ภ.พ.30/ไม่ออก 50 ทวิ" — ตอนนี้เงียบสนิท และการเคลมภาษีซื้อต้องห้ามไม่ใช่เรื่องความเร็ว
- ความมั่นใจ: สูง (เมธอดทั้งก้อนไม่มีการอ้าง `[VAT-CLAIM]`/`IsVatClaimable`) — ต้องเช็คต่อ: UI `document-scan.html` ตั้ง `postVat` default อย่างไร

### T1-03 🔴 [P1][S] ฐานคำนวณ WHT ในเส้น JE ตรงใช้ `ExtractedSubTotal` (ยอด**ก่อนส่วนลด**) ขณะที่เส้นเอกสารใช้ `total − VAT` — สองสูตรในไฟล์เดียว
- ไฟล์: `OcrService.cs:5434-5443` vs `:4736-4738`
- โค้ด: JE: `var subtotal = result.ExtractedSubTotal ?? 0m; … wht = Math.Round(subtotal * result.WhtRate / 100m …)` · เอกสาร: `var whtBase = Math.Max(0, (ExtractedTotalAmount ?? 0) - (ExtractedVatAmount ?? 0));` พร้อมคอมเมนต์อธิบายว่า "ExtractedSubTotal on discounted papers is the PRE-discount figure and would overstate the withholding by discount × rate"
- ทำไมพัง: ผู้เขียนเส้นเอกสารรู้แล้วว่า ExtractedSubTotal ผิดบนใบมีส่วนลด แต่เส้น JE ตรงยังใช้ค่านั้น ⇒ ใบเดียวกันได้ WHT ต่างกันตามปุ่มที่กด
- ผลกระทบ: หักเกิน (ผู้รับเงินโดนหักเกิน ต้องขอคืน) · ภ.ง.ด.3/53 ยอดไม่ตรงกับที่จ่ายจริง
- defect class: "Resolver กลาง ห้ามคำนวณเอง" · "แก้ตัวเดียว เหลือที่เหลือ"
- ทางแก้: ยุบเป็น helper `OcrWhtBase.Compute(total, vat, subtotal, discount)` ตัวเดียว ใช้ทั้งสองเส้น
- ฝ่ายค้าน: "ส่วนใหญ่ subtotal = total − VAT อยู่แล้ว" → คำตอบ: ใช่ในใบไม่มีส่วนลด แต่ใบผู้รับเหมา/ค่าบริการที่มีส่วนลดท้ายบิลไม่ใช่เคสพิสดาร และคอมเมนต์ในโค้ดเองยืนยันว่าเจอเคสจริง
- ความมั่นใจ: สูง

### T1-04 🟠 [P1][S] วันที่เอกสารที่ OCR อ่านไม่ได้ถูกแทนด้วย "วันนี้" เงียบ ๆ — 4 จุด — และวันนี้กลายเป็น tax point/งวด ภ.พ.30
- ไฟล์: `OcrService.cs:4696` (`ThaiDate.CalendarDateUtc(result.ExtractedDate ?? DateTime.UtcNow)`), `:5486` (JE `entryDate = … ?? DateTime.UtcNow.Date`), `:2896` (50 ทวิ credit), `:6188` (สินทรัพย์ `purchaseDate`)
- ทำไมพัง: (1) OCR อ่านวันไม่ได้ (Tesseract/ใบซีดจาง) → `ExtractedDate=null` → (2) `DocumentDate = วันนี้` และ `SupplierTaxInvoiceDate = result.ExtractedDate = null` (:4931) → (3) ตอนอนุมัติ `TaxPointResolver.Resolve` (`Tax/TaxPointResolver.cs:50` `issueDate = SupplierTaxInvoiceDate ?? DocumentDate`) → tax point = วันนี้ → (4) ใบกำกับเดือน ก.ค. ที่สแกนเดือน ก.ย. ถูกเคลมงวด ก.ย. โดยที่ตัวกันกรอบ §82/3 (`InputVatClaimPeriodRules:129` ใช้ฐาน `SupplierTaxInvoiceDate ?? DocumentDate`) ก็มองว่า "ใบนี้ออกวันนี้" ⇒ **ใบเก่าเกิน 6 เดือนก็หลุดด่าน §82/3 ได้** เพราะวันจริงหายไปแล้ว
- ผลกระทบ: งวดภาษีผิด + ด่าน 6 เดือนถูกปิดโดยไม่ตั้งใจ · เลขเอกสารของเราออกตามวันที่ผิด (gap-free แต่ผิดเดือน)
- defect class: "ค่า default ที่แต่งขึ้นเพื่อให้โค้ดเดินต่อได้ อันตรายกว่าการไม่ตอบ" (ญาติของ `?? "00000"` E-OCR-03 ที่ปิดไปแล้วเฉพาะสาขา)
- ทางแก้: ไม่รู้วัน = สร้าง Draft ได้แต่ต้อง (ก) stamp `FieldConfidence[DocumentDate]=0.30` (เช่นเดียวกับสาขา) (ข) บล็อก **approve** (ไม่ใช่ create) จนกว่าผู้ใช้ยืนยันวัน — ด่านเดียวกับ "ต้องระบุเหตุผลใบลดหนี้" (ค) `SupplierTaxInvoiceDate` ห้ามเป็น null เมื่อ `HasTaxInvoiceReference=true`
- ฝ่ายค้าน: "กฎเหล็ก #3 บอกห้ามปล่อยช่องว่าง ต้องเติมค่าให้" → คำตอบ: กฎเดียวกันข้อ 3 บอกว่าค่าที่ confidence ต่ำต้อง**มีป้าย** — ตอนนี้วันนี้ถูกเติมโดยไม่มี key ใน FieldConfidence เลย (grep `OcrFieldKeys.DocumentDate` ตอน fallback = ไม่พบ) ผู้ใช้จึงไม่รู้ว่าเป็นค่าเดา
- ความมั่นใจ: สูงสำหรับ default · กลางสำหรับผลต่อ §82/3 (ต้องยืนยันว่า `InputVatClaimPeriodRules` ไม่มีแหล่งวันอื่น)

### T1-05 🔴 [P1][M] OCR สร้าง `Document` ด้วยมือ (`_db.Documents.Add`) ไม่ผ่าน `IDocumentService.CreateDocumentAsync` ⇒ ด่านที่เส้นกรอกมือมี OCR ไม่มี: ธง `ChartOfAccount.InputVatClaimable=false` · อัตราแลกเปลี่ยน · ผังผิดฝั่ง
- ไฟล์: `OcrService.cs:4857-5215` (สร้าง entity เอง, `_db.Documents.Add(document)` :5215) vs `DocumentService.cs:1251-1334` (โหลด `InputVatClaimable` ของทุกบัญชีที่บรรทัดใช้ → `enforcedClaimable`) · `:417-433 ResolveExchangeRateAsync` (throw เมื่อสกุลไม่ใช่ THB และไม่มีอัตรา) · `:392-408 EnsureLineAccountMatchesDocSide`
- โค้ด: grep `InputVatClaimable` ใน OcrService = มีแต่ของ role inferrer (:773/:808/:916) **ไม่มีการอ่านธงระดับผังบัญชี** · grep `ExchangeRate`/`ResolveExchangeRateAsync` ใน OcrService = **0** ขณะที่ `:4867 Currency = InferCurrency(...) ?? "THB"` ตั้งสกุลได้ และ `Document.ExchangeRate` default `= 1m` (`Models/Entities/Document.cs:442`)
- ทำไมพัง: (1) บริษัทตั้งผัง 54470 "ค่าใช้จ่ายรถยนต์นั่ง (ภาษีซื้อต้องห้าม)" `InputVatClaimable=false` ไว้ (ผังมาตรฐาน `ChartOfAccountTemplates.cs:242`) → กรอกมือระบบบังคับไม่เคลม ✓ → แต่ใบเดียวกันผ่าน OCR (ถ้า screener keyword ไม่โดน) → บรรทัดลง 54470 พร้อม `IsVatClaimable=true` (default) → อนุมัติ → Dr 11610 ⇒ เคลมภาษีซื้อต้องห้าม (2) invoice USD → `Currency="USD"`, `ExchangeRate=1` → JE ลงตัวเลข USD เป็นบาทตรง ๆ (ToGlAmount ×1) = ยอดผิด 30+ เท่า **เงียบ** — คอมเมนต์ที่ :4864 เขียนเองว่ากันเคสนี้ แต่แก้ครึ่งเดียว (รู้สกุลแล้วแต่ไม่รู้อัตรา)
- ผลกระทบ: เคลม VAT ต้องห้าม / งบผิดสกุล / กติกาสองมาตรฐานระหว่างเส้นกรอกมือกับ OCR (ขัดกฎ CLAUDE.md "โมดูลที่มีเงินห้ามออกเอกสารเอง — เดินผ่าน IDocumentService เสมอ")
- defect class: "ด่านที่ครอบแค่ทางเดียว" + "Resolver กลาง ห้ามคำนวณเอง"
- ทางแก้: (ก) ระยะสั้น: หลัง build lines เรียกตัวบังคับธงผังบัญชีตัวเดียวกับ :1330 (แยกเป็น pure helper `InputVatAccountPolicy.Enforce(lines, accountFlags)`) + ถ้า `Currency != THB` → เรียก `ResolveExchangeRateAsync` หรือ **ไม่ตั้ง Currency เลยแล้วเตือน** (ตั้งสกุลโดยไม่มีอัตรา = ค่าที่แต่งขึ้น) (ข) ระยะกลาง: map `OcrScanResult` → `CreateDocumentRequest` แล้วเรียก `CreateDocumentAsync` ให้ OCR เป็นแค่ "ผู้กรอกฟอร์ม" ตามกฎเหล็ก #3
- ฝ่ายค้าน: "CreateDocumentAsync ตรวจเข้มจนใบ OCR ที่ข้อมูลไม่ครบจะสร้างไม่ผ่าน ผิดกฎเหล็ก #3 (ต้องได้ Draft เสมอ)" → คำตอบ: ธงผังบัญชีกับอัตราแลกเปลี่ยนไม่ใช่ "ข้อมูลไม่ครบ" แต่เป็นกติกาบัญชีที่ต้องเหมือนกันทุกทางเข้า; ส่วนที่เข้มเกิน (required fields) ทำเป็น flag `allowIncomplete` ใน request ได้
- ความมั่นใจ: สูง (grep 0 hit ทั้งสองเรื่อง) — ต้องเช็คต่อ: `ApproveDocumentAsync` มีการ re-enforce ธงผังตอนอนุมัติไหม (grep `InputVatClaimable` ใน DocumentService พบแค่ create/update :1251-1334, :2305-2384 ⇒ ไม่มี)

### T1-06 🔴 [P1][S] `POST ocr/{id}/create-document?approve=true` และ `create-journal-entry` ไม่มีด่านสิทธิ์เลย — สมาชิกที่ไม่มีสิทธิ์อนุมัติ กด "ยืนยัน + ลงบัญชี" จากหน้าสแกน = อนุมัติ + ลง JE ได้
- ไฟล์: `Controllers/OcrController.cs:15` (`[Authorize]` ระดับคลาสอย่างเดียว) · `:269-306` (`approve=true` → `_documentService.ApproveDocumentAsync(...)` ตรง) · `:342-348` (JE ตรง) · grep `DocumentPermissionHelper|CanApproveAsync|HasPermission` ใน OcrController = **0**
- เทียบ: `DocumentController.cs:570,591,630,…` ทุกเส้นอนุมัติผ่าน `CanApproveAsync` · `SignatureApprovalService.cs:104` ก็ผ่านแล้ว (คอมมิต 4cbb52a "ช่องอนุมัติทางอ้อม 3 ทาง") — **เส้น OCR เป็นทางที่ 4 ที่ถูกลืม**
- ผลกระทบ: ด่านสิทธิ์ที่ปิดไป 3 ทางในรอบก่อนยังเปิดอยู่ 1 ทาง = ยังไม่มีด่าน (สร้าง JE จริง · ออกเลขเอกสารจริง · ภ.พ.30 นับ)
- defect class: R3 "ด่านสิทธิ์ไล่ไม่ครบทุกทางเข้า" · `write_permission_gate_check` เป็น allow-list ที่ไม่มี OcrController (บทเรียน MeteringController ซ้ำ)
- ทางแก้: ก่อน `CreateDocumentFromScanAsync` เช็ค `CanCreateAsync(docType)`; ก่อน approve เช็ค `CanApproveAsync(docType)` — ถ้าไม่ผ่านให้สร้าง Draft แล้วตอบใน `ProcessingNotes` ว่า "สร้างแล้ว รออนุมัติ (ไม่มีสิทธิ์อนุมัติ)" (ห้าม silent) · JE ตรง: ต้องมีสิทธิ์ JournalEntry.Create · เพิ่ม `OcrController` เข้า allow-list ของ checker
- ฝ่ายค้าน: "ApproveDocumentAsync ข้างในเช็คสิทธิ์อยู่แล้ว" → คำตอบ: grep `CanApproveAsync` ใน DocumentService พบจุดเดียว :10759 (เฉพาะ settlement Receipt) ไม่ใช่ใน `ApproveDocumentAsync` ทั่วไป
- ความมั่นใจ: สูง

### T1-07 🟠 [P1][M] "ต้องหัก ณ ที่จ่ายไหม/อัตราเท่าไร" ตัดสินจาก **กระดาษพูดถึง WHT** เท่านั้น — แต่ใบแจ้งหนี้ค่าบริการของผู้ขายไทยเกือบทั้งหมด**ไม่พิมพ์ WHT** (หน้าที่หักเป็นของผู้จ่าย)
- ไฟล์: `Ocr/ExpenseCategoryResolver.cs:421-433` (`if (... result.StatutoryWhtRate ... && docMentionsWht)` → ตั้ง; ไม่งั้น trace "ข้าม WHT inference — เอกสารไม่มียอดหัก ณ ที่จ่าย") · ตารางอัตรา `:244-394` (3/5/2/1/15 ตรง ท.ป.4/2528) · regex กระดาษ `OcrService.cs:4359-4371` อยู่ใน `ParseThaiDocument` (**เส้น Tesseract เท่านั้น** :4078) — เส้น Azure (:2620-3110) และ `EnrichFromRawText` (:6962-7160) grep `หัก|WHT|HasWht` = 0 · แหล่งที่เหลือ = ประวัติผู้ขาย `:1219-1226` (ต้องเคยลงมาก่อน)
- ทำไมเป็นช่องว่าง: นักบัญชีเห็นใบ "ค่าบริการทำความสะอาด 10,700" จากบริษัทจำกัด → รู้ทันทีว่าต้องหัก 3% = 300 และจ่ายจริง 10,400 — โดยไม่ต้องให้กระดาษบอก. ระบบ: ผู้ขายรายใหม่ + Azure path + กระดาษไม่พิมพ์ WHT ⇒ `HasWht=false` ⇒ PV จ่ายเต็ม 10,700 ⇒ ไม่มี 50 ทวิ ⇒ **§54 ผู้จ่ายรับผิดภาษีที่ไม่ได้หัก** · ด่านตอนอนุมัติ (`DocumentService.cs:15645-15674` C-T05) เตือนเฉพาะยอดสะสม ≥ 1,000 และ**ไม่บอกอัตรา** ("ถ้าเป็นค่าซื้อสินค้าไม่ต้องหัก — ยืนยันเพื่ออนุมัติต่อ") ขณะที่ resolver รู้อัตราอยู่แล้ว
- defect class: "ของที่สร้างไว้แล้วไม่ได้ถูกเรียกใช้" (ครึ่งหนึ่ง — StatutoryWhtRate ถูกอ่านแต่ถูกล็อกด้วยเงื่อนไขที่แทบไม่เป็นจริงบนใบจริง) + `BranchCodeExtractor`-class (ตัวอ่านอยู่แค่เส้น Tesseract)
- ทางแก้: (ก) แยก "กระดาษหัก" (extracted) กับ "กฎหมายบอกให้หัก" (statutory) เป็นสองช่อง: `WhtRate` + `SuggestedStatutoryWhtRate` — ช่องหลังเติมจาก resolver เมื่อ `role=Buyer` และหมวดเป็นบริการ/เช่า/โฆษณา/ขนส่ง โดย**ไม่ต้องให้กระดาษพูดถึง** แต่ติดป้าย "⚙️ ระบบแนะนำ 3% (ท.ป.4/2528) — ผู้ขายเป็นนิติบุคคล" confidence 0.6 (ข) ย้าย regex WHT ออกจาก `ParseThaiDocument` ไป `EnrichFromRawText` ให้ทุก tier ได้ใช้ (ค) คำเตือน C-T05 ให้ระบุอัตราจาก resolver
- ฝ่ายค้าน: "เดา WHT ผิดแล้วหักลูกค้าเกิน แย่กว่าไม่หัก" → คำตอบ: ทั้งสองทางผิดไม่เท่ากันแบบเดียวกับ §82/5 — ไม่หัก = ผู้จ่ายรับผิด+เงินเพิ่ม 1.5%/เดือน · หักโดยมีป้ายให้ผู้ใช้ยืนยัน = แค่ยืนยัน/ปลด; และไม่ได้เสนอให้ hard-set แต่เสนอเป็นค่าแนะนำติดป้ายตามกฎเหล็ก #3 ข้อ 3
- ความมั่นใจ: สูง

### T1-08 🟠 [P2][S] `InferIncomeTypeCode` บน 50 ทวิ คืน null แทบทุกใบ — แบบฟอร์มจริงพิมพ์ชื่อประเภทเงินได้**ครบทุกข้อ**เป็นหัวรายการ
- ไฟล์: `OcrService.cs` (เมธอด `InferIncomeTypeCode`, ~:3050-3075): `hits = families.Where(f => f.Markers.Any(m => t.Contains(m)))…; return hits.Count == 1 ? hits[0] : null;`
- ทำไมพัง: แบบ 50 ทวิ ของกรมสรรพากรพิมพ์ "1. เงินเดือน ค่าจ้าง … 40(1) · 2. ค่าธรรมเนียม ค่านายหน้า … 40(2) · 3. ค่าแห่งลิขสิทธิ์ … 40(3) · 4. (ก) ดอกเบี้ย (ข) เงินปันผล … · 5. …" เป็นหัวข้อบนทุกใบ ⇒ markers "เงินเดือน", "ค่านายหน้า", "ค่าธรรมเนียม", "ดอกเบี้ย", "เงินปันผล" โดนพร้อมกัน ≥ 5 family ⇒ `hits.Count > 1` ⇒ null เสมอ ⇒ `WhtCreditReceived.IncomeTypeCode=null` ⇒ `CheckRate` ตรวจอัตรากับ ท.ป.4 ไม่ได้ (คอมเมนต์ :2960 บอกว่าใส่ไว้เพื่อสิ่งนี้)
- ทางแก้: ตัดสินจาก **บรรทัดที่มีตัวเลขยอดเงินตามหลัง** (แถวที่ถูกกรอก) ไม่ใช่จากคำที่ปรากฏ — เช่น regex `ตามมาตรา 40\((\d)\)[^\n]*?\d[\d,]*\.\d{2}` เลือกแถวที่มีจำนวนเงิน · ถ้ายังหลายแถว → null
- ฝ่ายค้าน: "OCR อ่านตำแหน่งตัวเลขบนฟอร์มพิมพ์ไม่นิ่ง" → คำตอบ: ก็ยังดีกว่าเกณฑ์ปัจจุบันที่พิสูจน์ได้ว่าเป็นจริงไม่ได้บนฟอร์มมาตรฐาน — และผลลัพธ์ null คือค่าที่ระบบเดิมได้อยู่แล้ว ไม่แย่ลง
- ความมั่นใจ: สูง (ตรรกะ) · ยังไม่ได้รันกับ raw text จริง (ไม่มี sample ในเรพ — ต้องเช็ค `Accounting.Tests/*Wht*` ว่ามี fixture ไหม)

### T1-09 🟠 [P2][S] บรรทัด "ค่าขนส่ง/บริการอื่น (ตรวจสอบใบจริง)" ถูก**แต่งขึ้น**เพื่อให้ยอดบรรทัดตรงกับหัวใบ — เป็นรายการบัญชีที่ไม่มีบนกระดาษ
- ไฟล์: `OcrService.cs:5003-5017` (Case A: `missing = hdrTotal − grossSum; if (missing > TOL) items.Add(new … Description = "ค่าขนส่ง/บริการอื่น (ตรวจสอบใบจริง)", Amount = missing)`)
- ทำไมเป็นปัญหา: เงื่อนไข Case A (`grossSum > hdrSub + 1 && grossSum <= hdrTotal + 1`) เข้าได้ทั้งเคส "ราคารวม VAT" จริง และเคส **OCR อ่านบรรทัดขาด/อ่านหัวใบผิด** — ส่วนต่างถูกสร้างเป็นบรรทัดค่าใช้จ่ายที่ได้ GL เดียวกับใบ (fallback `scanDebitAccountId`) + VAT เฉลี่ย + WHT เฉลี่ย ⇒ ลงบัญชี "ค่าขนส่ง" ที่ไม่มีใครขนส่ง; ผู้ใช้กด "ยืนยัน" ตามกฎเหล็ก #3 ก็หลุด
- defect class: "ค่า default ที่แต่งขึ้นเพื่อให้โค้ดเดินต่อได้"
- ทางแก้: เก็บส่วนต่างเป็น `UnreconciledDifference` บนหัว + ป้ายเหลือง "ยอดบรรทัดขาด X" แล้ว**บล็อกอนุมัติ** (ไม่บล็อกสร้าง) จนกว่าผู้ใช้จะเติมบรรทัดจริงหรือยอมรับเป็นค่าขนส่ง — ใน UI ให้ปุ่ม "เป็นค่าขนส่งจริง" ที่ตั้งบรรทัดพร้อม GL ค่าขนส่ง (ไม่ใช่ GL เดียวกับสินค้า)
- ฝ่ายค้าน: "มีคำว่า (ตรวจสอบใบจริง) อยู่แล้ว" → คำตอบ: คำเตือนใน Description ไม่ใช่ป้าย confidence และไม่หยุดการอนุมัติ — และหลังอนุมัติคำนั้นไปโผล่ในรายละเอียด JE/รายงานภาษีซื้อ
- ความมั่นใจ: สูง

### T1-10 🟠 [P2][S] `SupplierBranchCode ?? "00000"` ยังถูกเขียนลง **Document ที่สร้าง** — E-OCR-03 แก้เฉพาะป้าย % ในหน้า review
- ไฟล์: `OcrService.cs:4933-4935` `SupplierBranchCode = bookSupplierRef ? (string.IsNullOrWhiteSpace(vendorBranchForBook) ? "00000" : vendorBranchForBook) : null`
- ทำไมพัง: ป้าย 30% บนหน้า review ถูกแล้ว แต่พอผู้ใช้กดยืนยัน ค่า "00000" ถูกเขียนลง `SupplierBranchCode` ของเอกสาร → รายงานภาษีซื้อ §87 พิมพ์ "สำนักงานใหญ่" สำหรับใบสาขาที่อ่านไม่ได้ ⇒ ผิดประกาศฯ 199 เหมือนเดิม แค่ผู้ใช้ "ได้เห็นป้าย" ก่อน
- ทางแก้: เก็บ null + ให้ด่าน §86/4 ตอนอนุมัติ (`DocumentService.cs:4655-4660`) บังคับกรอกเมื่อ `HasTaxInvoiceReference && VatAmount>0` — ตอนนี้ null ถูกกลบก่อนถึงด่าน
- ฝ่ายค้าน: "ผู้ขายรายย่อยเกือบทั้งหมดเป็นสำนักงานใหญ่จริง" → คำตอบ: จริง 90% แต่ 10% ที่ผิดคือรายงานภาษีที่ยื่นแล้ว — ให้เป็นค่าแนะนำที่ผู้ใช้ยืนยัน ไม่ใช่ค่าที่เขียนเงียบ
- ความมั่นใจ: สูง

### T1-11 🔴 [P1][M] สแกน "หนังสือรับรอง 50 ทวิ ที่เราถูกหัก" → ระบบสร้าง **ใบสำคัญรับ (ReceiptVoucher) standalone** ⇒ ตอนอนุมัติลง JE เป็น **ขายสด: Dr เงินสด / Cr รายได้ + ภาษีขาย** — รายได้เบิ้ล + เงินสดปลอม ทั้งที่เงินเข้าธนาคารและใบขายมีอยู่แล้ว
- ไฟล์: `Ocr/OcrDocumentRoleInferrer.cs:541-545` (`if (weAreWithheld == true) { target = DocumentType.ReceiptVoucher; reasons.Add("… หลักฐานรับเงินสุทธิ (ใบสำคัญรับ) …") }`) → `OcrService.cs:4446-5215` สร้าง `Document{ DocumentType=ReceiptVoucher, RelatedDocumentId = linkedPo?.Id (=null), PaidAmount = headerTotal, TotalAmount = ExtractedTotalAmount − WHT }` โดย**ไม่มีด่านใดตรวจ `IsWhtCertificate`** ในเส้นสร้าง (grep `WeAreWithheld|IsWhtCertificate` ใน :4446-5276 = เฉพาะ :5231 ที่ **เพิ่ม** ทะเบียนเครดิต — ไม่ได้กันการสร้างใบ) → `DocumentService.cs:13766-13790` `Receipt/ReceiptVoucher` ที่ `RelatedDocumentId == null` ⇒ `receiptSettlesAr=false` ⇒ เข้า branch **ขายสด** (doc-comment :12095 "cash sale, ไม่ link: Dr เงินสด, Cr รายได้ + Cr ภาษีขาย")
- ทำไมพัง: (1) ลูกค้าจ่ายเงินสุทธิเข้าธนาคาร → ระบบมี Invoice/TIV + (มักจะ) Receipt จากการกระทบยอดธนาคารแล้ว (2) 2 เดือนต่อมาลูกค้าส่ง 50 ทวิ มา → บัญชีสแกนเก็บเข้าระบบเพื่อ "เอาเครดิตภาษี" (3) ระบบตั้ง target = ReceiptVoucher · ผู้ใช้กด "⚡ ยืนยัน + ลงบัญชี" ตามที่ปุ่มแนะนำ (4) เกิดใบสำคัญรับใบใหม่ยอด = ยอดเงินได้บนใบ 50 ทวิ → JE Dr เงินสด / Cr รายได้ (+ ภาษีขายถ้า OCR อ่าน VAT ได้) ⇒ **รายได้ปีนั้นเบิ้ล · เงินสดในบัญชีเกินธนาคาร · ภ.พ.30 ภาษีขายเบิ้ล** (5) ทะเบียนเครดิต WHT (:5231 `EnsureWhtCreditFromCertAsync`) ถูกสร้าง**ควบ**ไปด้วย = ส่วนที่ถูกต้องเพียงส่วนเดียวถูกพ่วงกับส่วนที่ผิด
- ผลกระทบ: P1 (P0 ถ้าบริษัทรับงานบริการที่ถูกหักทุกใบ — 50 ทวิ เป็นเอกสารรายเดือนของ SME บริการ)
- defect class: "ค่า default ที่แปลว่า 'ยังไม่ระบุ' ต้องแยกจากค่าที่มีความหมายจริง" (`RelatedDocumentId=null` = ขายสด — บันทึกไว้แล้วใน CLAUDE.md แต่เส้น OCR ไม่ได้คิดถึง) + "เงื่อนไขที่ 'ปกติเป็นจริงเสมอ' ต้องมี else"
- ทางแก้: 50 ทวิ ที่เราถูกหัก **ไม่ควรสร้าง Document เลย** — เส้นที่ถูกคือ (ก) `EnsureWhtCreditFromCertAsync` อย่างเดียว (มีอยู่แล้ว) + (ข) จับคู่กับใบขาย/ใบเสร็จเดิมด้วย (PayerContactId, ยอด, ปีภาษี) แล้ว**ผูก AttachmentId เข้าใบเดิม** (ค) ถ้าใบขายเดิมยังไม่บันทึก WHT ให้เสนอ "ปรับปรุง WHT บนใบเสร็จเดิม" ไม่ใช่ออกใบใหม่ · ใน `CreateDocumentFromScanAsync`: `if (weAreWithheld == true) throw BusinessRuleException("50 ทวิ ไม่ใช่เอกสารรายได้ — ลงทะเบียนเครดิตแล้ว ผูกกับใบขายเลขที่ …")`
- ฝ่ายค้าน: "target เป็น RV เพื่อให้มีที่แนบไฟล์และ audit trail" → คำตอบ: `WhtCreditReceived.AttachmentId` (:2957) เก็บไฟล์อยู่แล้ว · audit trail ไม่ควรแลกด้วย JE ที่ผิด · และคอมเมนต์ในตัว inferrer เองเขียนว่า "**ไม่ใช่**ใบขายใบใหม่" แต่ผลลัพธ์ของ AutoPost คือใบขายใบใหม่พอดี — เจตนากับโค้ดไม่ตรงกัน
- ความมั่นใจ: สูง (ไล่ครบ 3 ไฟล์) — ต้องเช็คต่อ: `whtBasis`/`IssuedAsCashReceipt` branch ที่ :13790+ ไม่มีทางแยก RV ที่ไม่มีต้นทางออกจากขายสด (อ่านแล้วไม่มี)

### T1-12 🟠 [P1][M] Invoice ต่างประเทศ: รู้สกุลเงินแต่ไม่รู้อัตรา · ตั้ง VAT รายบรรทัด = 0% · ไม่ตัดสิน ภ.พ.36 (§83/6) และ ภ.ง.ด.54 (§70) เลย
- ไฟล์: `OcrService.cs:2995-3015 InferCurrency` + `:4867 Currency = … ?? "THB"` (ไม่มี ExchangeRate) · `Helpers/ThaiVatTypeRule.cs:53-68` (`LooksForeignVendor` = เลขภาษีไม่ใช่ 13 หลัก → `Suggest` คืน `"0"`) ใช้ที่ `OcrService.cs:5040-5046` · grep `ภ.พ.36|Pp36|ภ.ง.ด.54|Pnd54|ReverseCharge|ต่างประเทศ` ใน OcrService = 0 (มีแค่คอมเมนต์ :2988)
- ทำไมเป็นช่องว่าง: นักบัญชีเห็น invoice จาก AWS/Google/Adobe (ผู้ขาย ตปท. ไม่มีเลข 13 หลัก) → ตัดสิน 4 อย่าง: (1) แปลงเป็นบาทด้วยอัตรา ธปท. วันจ่าย (2) **ยื่น ภ.พ.36 นำส่ง VAT 7% แทนผู้ขาย** ภายในวันที่ 7 เดือนถัดไป แล้วเอาใบเสร็จ ภ.พ.36 มาเคลมภาษีซื้อเดือนถัดไป (ผัง 11620 มีอยู่ใน template :48) (3) ถ้าเป็นค่าสิทธิ/บริการ → **หัก ณ ที่จ่าย §70 15%** (หรือ DTA) ยื่น ภ.ง.ด.54 (ผัง 21918 มี :148) (4) ลงค่าใช้จ่ายด้วยยอดบาท. ระบบ: ทำข้อ 4 ด้วย**ยอดสกุลนอกตรง ๆ** (rate=1) และป้าย 0% บนบรรทัดทำให้ผู้ใช้เข้าใจว่า "ไม่มีภาระ VAT" ซึ่งตรงข้ามกับความจริง (0% เป็นแนวคิดฝั่งขายส่งออก §80/1 ไม่ใช่ฝั่งซื้อจาก ตปท.)
- ผลกระทบ: ภาระ ภ.พ.36/ภ.ง.ด.54 หายทั้งก้อน (เบี้ยปรับ 2 เท่า + เงินเพิ่ม) · งบผิดสกุล
- ทางแก้: เมื่อ `LooksForeignVendor` และ role=Buyer → (ก) `Currency` จากกระดาษ + `ExchangeRate` จาก `IBotExchangeRateService` ณ วันเอกสาร (มี DI อยู่แล้ว `DocumentService.cs:32`) (ข) ตั้งธง `RequiresPp36=true` + บรรทัด VAT 7% แบบ "self-assessed" ไม่ใช่ 0% (ค) เสนอ WHT §70 ตามหมวด (บริการ/ค่าสิทธิ 15%) ติดป้ายแนะนำ (ง) ถ้าไม่มีอัตรา → **ไม่ตั้ง Currency** และเตือน แทนที่จะตั้งสกุลพร้อม rate 1
- ฝ่ายค้าน: "ภ.พ.36 มี flow แยกอยู่แล้วในระบบ (:2504 พูดถึง)" → คำตอบ: มี flow แต่ OCR ไม่พาไปถึง — ผู้ใช้ต้องรู้เองว่าต้องไปทำ ขัดกับโจทย์ "อัพแล้วจบ"
- ความมั่นใจ: สูง (grep) · กลาง เรื่องขนาดงาน

### T1-13 🟠 [P2][S] Screener §82/5 ใช้ substring ทั้ง raw text — คำว่า "pure"/"เพียว", "b7", "ก๊าซ/แก๊ส", "ค่าจอดรถ" ทำให้ใบที่ไม่เกี่ยวกับรถถูกตั้ง "ไม่เคลม" เงียบ ๆ
- ไฟล์: `Ocr/ProhibitedInputVatScreener.cs:44-62` (VehicleCostKeywords รวม `"b7", "b20", "b95", "ก๊าซ", "แก๊ส"`), `:66-79` (FuelVendorKeywords รวม `"pure", "เพียว"`), `:107-110` (`hay = rawText + vendorName + lines` แล้ว `Contains` ทั้งก้อน) · ผลถูกใช้เป็น `Claimable=false` → `[VAT-CLAIM]` → `IsVatClaimable=false` ทุกบรรทัด (`OcrService.cs:5208-5213`)
- ทำไมพัง: (1) ร้านอาหารซื้อ **ก๊าซหุงต้ม** (LPG ถัง 15 กก.) จาก ปตท./ร้านแก๊ส — คำว่า "แก๊ส"+"ปตท" โดนทั้งสองลิสต์ → ตั้งไม่เคลมทั้งใบ ทั้งที่เป็นภาษีซื้อปกติ 100% (2) invoice ที่มี "Pure" ในชื่อสินค้า/ที่อยู่ ("Pure Cotton", "Pure Water") หรือ "B7" ในที่อยู่ ("อาคาร B7") → เข้าข่าย vehicle-cost แล้วไม่มี ClaimableVehicleKeyword → ไม่เคลม (3) ผลเป็น**ทุกบรรทัด**ของใบ ไม่ใช่บรรทัดที่โดน keyword — ใบ Makro ที่มีน้ำมันรถ 1 บรรทัด + วัตถุดิบ 30 บรรทัด ถูกปิดเคลมทั้ง 31 บรรทัด
- ผลกระทบ: เสียสิทธิ์ภาษีซื้อ (ทิศ "ปลอดภัย" แต่เป็นเงินจริง) + ผู้ใช้ต้องไล่ติ๊กคืนทีละบรรทัดใน 6 เดือน
- ทางแก้: ตัดสิน**รายบรรทัด** (line description ก่อน; rawText ใช้เฉพาะเมื่อไม่มีบรรทัด) · keyword สั้น ≤ 3 ตัวอักษรใช้ `\b` · "ก๊าซ/แก๊ส" ต้องมี "รถ/NGV/LPG รถ" ประกอบ · ลบ "pure/เพียว" หรือบังคับให้อยู่ใน vendorName เท่านั้น
- ฝ่ายค้าน: "default ไม่เคลมปลอดภัยกว่า" (คอมเมนต์ในไฟล์เองพูดถึง) → คำตอบ: เห็นด้วยกับทิศ แต่ต้องเป็นเมื่อ "สงสัยจริง" — ร้านอาหารซื้อแก๊สทุกสัปดาห์ = ระบบผิดทุกสัปดาห์ ผู้ใช้จะเลิกเชื่อป้ายเตือนทั้งหมด (alarm fatigue) ซึ่งทำให้ด่านที่ถูกก็พังไปด้วย
- ความมั่นใจ: สูง (อ่าน keyword list + `ContainsAny` ทั้งก้อน)

### T1-14 🟠 [P2][S] ด่านกันซ้ำตอนสร้างเอกสารต้องการ **เลขที่ + ยอดตรงถึงสตางค์** — OCR อ่านยอดผิด 1 หลัก = ใบซ้ำหลุด · เส้น JE ตรงไม่มีด่านซ้ำเลย
- ไฟล์: `OcrService.cs` `FindDuplicateDocumentWarningAsync` (~:5300-5350): `if (docNo.Length < 3 || total is null or 0) return null; … hit = candidates.FirstOrDefault(c => Math.Abs(c.TotalAmount - total.Value) <= 0.01m); if (hit == null) return null;` · `CreateJournalEntryFromScanAsync :5417-5511` ไม่เรียก `FindDuplicateDocumentWarningAsync`
- ทำไมเป็นช่องว่าง: นักบัญชีถือว่า "ผู้ขายเดียวกัน + เลขใบกำกับเดียวกัน" = ใบเดียวกันเสมอ (เลขใบกำกับ unique ต่อผู้ขายตาม §86/4) — ยอดต่างแปลว่า OCR อ่านผิด ไม่ใช่คนละใบ. ระบบกลับใช้ยอดเป็นเงื่อนไข AND ⇒ อ่าน 1,070 เป็น 1,010 → หลุด ⇒ เคลมภาษีซื้อสองครั้ง
- ทางแก้: เลขที่+ผู้ขายตรง → เตือนเสมอ (ยอดต่างให้บอกว่า "ยอดต่าง X — น่าจะอ่านผิด") · เส้น JE ตรงเรียกด่านเดียวกัน (เก็บ `Reference = ExtractedDocumentNumber` อยู่แล้ว :5495 ใช้ค้นได้)
- ฝ่ายค้าน: "เลขที่ซ้ำข้ามปีของผู้ขาย (เลขวิ่งใหม่ทุกปี) จะเตือนผิด" → คำตอบ: จำกัด candidates ภายใน ±12 เดือน หรือปีภาษีเดียวกัน
- ความมั่นใจ: สูง

### T1-15 🟠 [P2][S] "ใบมัดจำ" ฝั่ง**ซื้อ** (เราจ่ายมัดจำ) ไม่มีเส้นทาง — ถูกลงเป็นค่าใช้จ่ายทันที ทั้งที่ต้องเป็นสินทรัพย์ (เงินมัดจำจ่าย/ค่าใช้จ่ายจ่ายล่วงหน้า)
- ไฟล์: `OcrService.cs:1479-1490` (`DepositKeywordRegex` ใช้เฉพาะ `OurRole == "Seller"`) · `Ocr/ExpenseCategoryResolver.cs` grep `มัดจำ|เงินล่วงหน้า|จ่ายล่วงหน้า` = 0
- ทำไมเป็นช่องว่าง: ใบเสร็จรับเงินมัดจำ 30% จากผู้รับเหมา/ผู้ขายเครื่องจักร → นักบัญชีลง Dr เงินมัดจำจ่าย (สินทรัพย์) ไม่ใช่ค่าใช้จ่าย/ต้นทุน; VAT เคลมได้ตาม tax point §78/1 (บริการ: จ่ายเงิน = tax point) — ระบบ: PV + GL จาก resolver (ค่าจ้าง/ซื้อสินค้า) ⇒ ค่าใช้จ่ายรับรู้เร็วเกิน ผิดงวด และเมื่อใบจริงมาทีหลังจะลงซ้ำทั้งก้อน
- ทางแก้: ขยาย `DepositKeywordRegex` ให้ฝั่ง Buyer → target PV + `IsDeposit=true` + GL 11xxx เงินมัดจำจ่าย (มี `DepositApplied*` flow ฝั่งขายอยู่แล้ว — ต้องมีคู่ฝั่งซื้อ)
- ฝ่ายค้าน: "งานเพิ่มทั้ง flow" → คำตอบ: ขั้นแรกแค่**เตือน+เลือก GL สินทรัพย์** ก็ตัดความผิดเรื่องงวดได้แล้ว (S) ส่วน flow หักมัดจำตอนใบจริงเป็น M แยกต่างหาก
- ความมั่นใจ: สูง (regex/grep)

### T1-16 🟠 [P2][S] ผู้ขายแบรนด์ค้าปลีก (Makro/HomePro/ไทวัสดุ/Tops…) → บังคับ 51110 "ต้นทุนสินค้า" ไม่ดูว่าบริษัทเราขายอะไร
- ไฟล์: `Ocr/ExpenseCategoryResolver.cs:289-300` (`VendorBrands` → `"51110", "ต้นทุนสินค้า"`)
- ทำไมเป็นช่องว่าง: บริษัทซอฟต์แวร์ซื้อกาแฟ/กระดาษ A4/สายไฟที่ Makro/HomePro = ค่าใช้จ่ายสำนักงาน/ซ่อมบำรุง ไม่ใช่ COGS; รายงานกำไรขั้นต้นเพี้ยน และ §65 ตรี(3) เครื่องดื่มพนักงานอาจเป็นสวัสดิการ. resolver รับ `industry/businessType` เข้ามาแล้ว (`OcrService.cs:1008-1009`) แต่ rule นี้ไม่ใช้
- ทางแก้: ให้ rule นี้ชนะเฉพาะ `BusinessType ∈ {Trading, Manufacturing, Restaurant}`; อื่น ๆ ตกไป keyword รายบรรทัด (Makro พิมพ์รายการละเอียดอยู่แล้ว)
- ฝ่ายค้าน: "คอมเมนต์บอกว่า rule นี้กันเคส Makro ที่อ่านรายการไม่ได้" → คำตอบ: ใช้เป็น fallback เมื่อ `Items.Count == 0` ได้ แต่ไม่ควรชนะรายการที่อ่านได้
- ความมั่นใจ: สูง

### T1-17 🔵 [ออกแบบ][P2] ใบกำกับภาษีที่ไม่มีเครดิตเทอมบนกระดาษ → default "จ่ายแล้วด้วยเงินสด" (PaymentVoucher · Cr บัญชีรหัสต่ำสุดที่ขึ้นต้น 111) — สถานะจ่ายและแหล่งเงินเป็นค่าที่แต่งขึ้น
- ไฟล์: `Ocr/OcrDocumentRoleInferrer.cs:452-462` (`target = buyerInvoiceDefaultTarget ?? DocumentType.PaymentVoucher`) · `OcrService.cs:1544-1552` (`creditPrefixes = {"1111","1110","111"}` → lowest-code) · `:4880-4889` (`PaymentType=Cash, PaidAmount=headerTotal, BalanceDue=0`) · `DocumentService.cs:5106-5108` (Cash + BalanceDue 0 ⇒ Status **Paid** ทันทีที่อนุมัติ)
- ทำไมนักบัญชีทำต่าง: ใบกำกับที่ไม่มีหลักฐานการจ่าย (สลิป/ตราชำระแล้ว) = **ตั้งหนี้ก่อน** เสมอ; "จ่ายแล้ว" ต้องมีหลักฐานว่าเงินออกจากบัญชีไหน. default ปัจจุบันทำให้ (1) เงินสดในมือติดลบทางบัญชีเมื่อความจริงจ่ายผ่านธนาคาร (2) กระทบยอดธนาคารไม่เจอคู่ เพราะรายการถูกลง "เงินสด" (3) 50 ทวิ ถูกออกทันทีตอนอนุมัติ (`DocumentService.cs:5225-5236` `whtPaidOnApprove`) ทั้งที่ยังไม่จ่ายจริง → วันที่บนหนังสือรับรองผิด (ต้องเป็นวันจ่าย ท.ป.4/2528)
- ทางเลือกที่ปฏิเสธ: "เปลี่ยน default เป็น PurchaseInvoice ทั้งระบบ" — ปฏิเสธเพราะ SME เงินสดจำนวนมากไม่มี AP process จริง และตั้งค่าได้แล้ว (`OcrBuyerInvoiceDefaultTarget`)
- ข้อเสนอ: default ยังเป็น PV ได้ แต่ (ก) **ไม่ตั้ง PaymentAccount เอง** — ว่างไว้ + ป้ายเหลือง "เลือกแหล่งเงิน" (การอนุมัติ PV ที่ไม่มีแหล่งเงินควรถูกบล็อกอยู่แล้ว — ต้องเช็ค) (ข) `PaymentDate` ใช้วันที่บนสลิปเมื่อมี ไม่ใช่ `docDate` (ค) เมื่อบริษัทมี BankFeed ให้เดินเส้น "ตั้งหนี้แล้วจับคู่ธนาคาร" เป็น default แทน
- ฝ่ายค้าน: "SME ไทยส่วนใหญ่ cash-basis จริง" → คำตอบ: จริงเรื่อง*ชนิดเอกสาร* แต่ไม่จริงเรื่อง*แหล่งเงิน* — เงินสดในมือของ SME ที่จ่ายด้วยโอน/บัตรมีน้อยมาก การเลือก "รหัสต่ำสุด" ไม่ใช่การตัดสินใจทางบัญชี
- ความมั่นใจ: สูง (โค้ด) · ระดับ P เป็นดุลพินิจ

### T1-18 🟠 [P3][M] มิติโครงการ/ศูนย์ต้นทุน มาจาก partner metadata เท่านั้น — ไม่เรียนจากประวัติ (ผู้ขาย X → โครงการ Y)
- ไฟล์: `OcrService.cs:689-705` (`if (Items.Count > 0 && !IsNullOrWhiteSpace(externalMetadataJson)) … OcrMetadataProjectMatcher`) · `:5146 ProjectId = item.ProjectId` · `:5152-5157` header จากบรรทัด
- ช่องว่าง: ผู้รับเหมาช่วงที่ทำงานโครงการเดียวมา 6 เดือน — นักบัญชีลง project ทันที; ระบบให้ผู้ใช้เลือกทุกใบ (UI มี main-project picker แต่ต้องกด) — `_vendorIntel` เก็บประวัติ GL/WHT/terms อยู่แล้ว (:1207-1231) เพิ่ม `TypicalProjectId` ได้ในทรงเดียวกัน
- ฝ่ายค้าน: "โครงการเปลี่ยนบ่อย เดาผิดอันตราย" → คำตอบ: เสนอเป็นค่าแนะนำติดป้าย ไม่ auto-set เมื่อ vendor มี >1 โครงการในปีนั้น
- ความมั่นใจ: สูง (grep) 

### T1-19 🟠 [P2][S] "ซื้อของเข้าสต๊อก vs ค่าใช้จ่าย" ตัดสินจากประวัติ `ProductAliases` ของผู้ขายเท่านั้น — บริษัทค้าขายที่เพิ่งเริ่มใช้ระบบ ทุกใบซื้อสินค้าลงเป็นค่าใช้จ่าย
- ไฟล์: `OcrService.cs:2187-2194` (`SuggestedEntryMode = (hasLines && vendorHasProductHistory) ? "Stock" : "Expense"`) · `ApplyProductCrossReferenceAsync` (:1074) จับคู่สินค้าได้แต่ผลไม่ถูกใช้ตัดสิน entry mode · `companyContext.BusinessType` มีอยู่ (:757) ไม่ถูกใช้ตรงนี้
- ทำไมนักบัญชีทำต่าง: ดู (1) ประเภทกิจการ (Trading/Manufacturing → สินค้าคงเหลือ §87(3)) (2) บรรทัดจับคู่กับ Product master ได้ไหม (3) ผู้ขายเป็น supplier สินค้าหรือบริการ. ระบบใช้แค่ข้อ "เคยนำเข้ามาก่อน" ⇒ cold-start ผิดทิศเสมอ (ขัดกฎเหล็ก #1 ข้อ 3 cold-start) และ COGS/สต๊อกผิดจนกว่าจะมีคน import ครั้งแรก
- ทางแก้: `Stock` เมื่อ (BusinessType ∈ Trading/Manufacturing **และ** ≥50% บรรทัดจับคู่ Product ได้) หรือมีประวัติ; แสดงเหตุผล
- ฝ่ายค้าน: "นำเข้าสต๊อกผิด แก้ยากกว่าลงค่าใช้จ่ายผิด" → คำตอบ: จึงเสนอเป็น *suggestion* (ช่องนี้เป็น suggestion อยู่แล้ว) ไม่ใช่ auto-import
- ความมั่นใจ: สูง
