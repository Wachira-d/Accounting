# รอบ 193 · ฝ่ายค้านรอบสอง (เงิน/ภาษี) — ตรวจการแก้ของ L2 (198fb5c) และ O1 (f7bad8d)

> อ่านอย่างเดียว · ไม่มี .NET SDK = **ยังไม่ได้คอมไพล์/รันเทสต์** · เลขบรรทัดอ้างที่ HEAD `ae0cc3d`
> · ตัวเลขปัดเศษได้จากการจำลองสูตรจริง (`RefundableGross`/`PlanCancellation`/`AllocateRefund` + ด่านคืนเกินของ `RefundDepositAsync`) ด้วย Python decimal ROUND_HALF_UP
> · checker ที่รันเอง: `required_call_site_check` (67 กติกา) · `record_arg` · `nullable_arg` · `undeclared_local` · `service_interface` ·
>   `tuple_name_merge` · `arg_type` · `accessibility` · `dto_nullable_contract` · `namespace_shadow` ได้ 0 ทุกตัว ·
>   `bash tools/check_all.sh` ไม่จบใน 600 วินาที (สแกน 26,317 ไฟล์ รวม `.claude/worktrees`) จึงไม่มีผลรวม

## สรุป 8 บรรทัด
- L2 ปิดครบ 5 ข้อ (C3 C5 C8 C9 C10) · C2 และ C7 ปิดในระดับตรรกะ · **ปิดบางส่วน 3 ข้อ (C1 C4 C6)**
- O1 ปิด 9 ข้อ · **ปิดบางส่วน 2 ข้อ (C5 C10)** · C8 ปิดแล้วแต่ปลดล็อกกว้างเกินไป (ดู N5)
- **CONFIRMED ใหม่ 6 ข้อ** หนักสุด 3 ข้อ:
  - **N1** เส้นหักมัดจำแบบขับ JE ที่ยอมให้ลงเมื่องวดยังไม่ยื่น ทำให้ด่าน P0-3 เรื่อง "ผู้ซื้อได้ใบกำกับสองใบ" กลับมา และผู้ใช้เว็บเลี่ยงปุ่มได้
  - **N2** ใบของ integration ที่ถูกบล็อกมีทางกลับไปสู่ผลเดิมของ C1 ได้ 3 ทาง
  - **N3** ตัวซ่อม "ยอดคืนแล้วตามใบมัดจำ" ทำให้การจองค้าง "ค้างคืน" ถาวร ทั้งในเคสที่ตั้งใจจะซ่อมและเคสที่มีการคืนมัดจำจากหน้าอื่นมาก่อน
- ไม่พบความเสี่ยงคอมไพล์จากการอ่าน: overload ใหม่ของ `ApproveDocumentAsync` ไม่ชนกัน · `PreviewTotals` เรียกเมธอด static ที่รับ `IReadOnlyList` ได้ · ทุก catch ใหม่ใช้ตัวแปรที่อยู่ใน scope
- เทสต์และ checker ที่เพิ่มล็อกได้แค่ว่า "มีการเรียก" ยังไม่ล็อกพฤติกรรม (§5)

---

## 1. ตารางปิด/ไม่ปิด

### L2 (review193-L2.md)

| ID | ผล | หลักฐาน | ที่ขาด |
|---|---|---|---|
| C1 | **ปิดบางส่วน** | `DocumentService.cs:15617-15641` `GuardDrivesGrossApplyAsync` · `IntegrationService.cs:1053-1075` catch เฉพาะรหัสกฎแล้ว return `success=false` | ไม่ถอยไปตั้งหนี้เงียบแล้ว **เฉพาะตอนกำลังสร้างใบ**. ยังขาด: (ก) งวดที่ยังไม่ยื่นกลับไปใช้พฤติกรรมก่อน L2 ซึ่งผู้ซื้อได้ใบกำกับสองใบ (N1) (ข) ทางกู้ใบที่ถูกบล็อกพาไปสู่ผลเดิมของ C1 (N2) |
| C2 | ปิด (ตรรกะ) | `Lifecycle.cs:706-708` วางแผนก่อน `CreateDocumentAsync` (:724) · `:623-625` กดซ้ำ = `ResumeCheckOutAsync` · `:778` นับ `DepositRealizedForDocumentId` · `LodgingDepositSettlement.cs` `PlanCheckout` | เทสต์ `ทำเช็คเอาต์ต่อ_มัดจำที่ตัดชำระใบนี้แล้วไม่ตัดซ้ำ` ส่งใบที่ `RealizedBase` = ฐานเต็ม ⇒ ถูกข้ามด้วย `rem.Gross <= 0.005` ก่อนถึงสาขา `AppliedToDocumentId == finalDocumentId` **สาขานั้นจึงไม่ถูกทดสอบเลย** · กดเช็คเอาต์พร้อมกันสองครั้งยังได้ใบกำกับสองใบ (ปัญหาเดิม ไม่มีล็อก) |
| C3 | ปิด | `Lifecycle.cs:932-935` ด่าน `Terminal` อยู่ที่ `CancelCoreAsync` · `:197-199` เส้นแขก | — |
| C4 | **ปิดบางส่วน** | คอลัมน์ `DatabaseMigrationHelper.cs:963` (`ADD COLUMN IF NOT EXISTS` รันซ้ำได้) · `RealizeDepositAsync` อ่าน `FinalInvoiceId` (:3393-3400) · void `:7382` / purge `:8233` → `ReverseDepositRealizationsForAsync` (:7973) | GL ถูกแล้ว แต่: (ก) การจองยังเป็น CheckedOut และ `CheckOutAsync` ต้องการ CheckedIn (:621) ⇒ **โมดูลที่พักออกใบใหม่ไม่ได้** (ทีมเขียนไว้เอง Q11) (ข) `RestoreVoidedDocumentAsync` (:7778) คืนใบเป็นร่างแล้วอนุมัติใหม่ได้ โดยใบยังหักฐานมัดจำ 1,869.16 (`BillDiscountAmount`) แต่ JE รับรู้ไม่ถูกทำซ้ำ ⇒ รายได้ขาด 1,869.16 และหน้าเงินมัดจำแสดงมัดจำ 2,000 ว่า "คงเหลือ คืนได้" (ความเสี่ยงคืนเงินซ้ำ) · purge ไม่มีกติกา call-site |
| C5 | ปิด | `CheckOutAsync` ด่าน + `FindOrCreateContactAsync` ก่อน `BuildChargeAsync` · `BuildChargeAsync` (:582) ไม่ `Add` เข้า DbSet · ผูกหลังอนุมัติ (:729-733) | — |
| C6 | **ปิดบางส่วน** | `RefundableGross` ตัวเดียว · จำลอง 20,000 กรณีแบบคืนครั้งเดียว = ล้ม 0 ✓ | **คืนเป็นสองงวด (ช่อง Amount บางส่วนใน `RecordRefundPaid`) ค้าง 0.01 ใน 1,915/20,000 = 9.6%** (N4) |
| C7 | ปิด (เลือกทิศแล้ว + บันทึก) | `LodgingDepositSettlement.RoundingDelta` · `Lifecycle.cs:745-749` หมายเหตุ | ภ.พ.30 ยังตรง: VAT 3.92 + 61.51 = 65.43 = Cr 21911 ใน GL = ผลรวม VAT บนใบกำกับสองใบ · แขกจ่ายรวม 1,000.01 (รอเจ้าของตอบ Q9) |
| C8 | ปิด | `LodgingPricingEngine.ChargeVatRate` ทั้ง `BuildChargeAsync:592` และบรรทัด folio เดิมตอนเช็คเอาต์ (:688) | ข้อมูลเก่า = SQL ในรายงานทีม |
| C9 | ปิด | `storefront.html:1777,1785,1866-1870` ใช้ `statusLabel`/`onlinePayableAmount`/`onlinePaymentNote` จากเซิร์ฟเวอร์ | — |
| C10 | ปิด | migration สองคำสั่ง (ล้างแล้วติดป้าย · รันซ้ำได้) · `RefundStateOf` → Unknown | — |
| PLAUSIBLE P1 (ตัวซ่อม) | **สร้างบั๊กใหม่** | `Lifecycle.cs:1092-1101` `SyncRefundPaidFromDeposits` | ดู N3 |

### O1 (review193-O1.md)

| ID | ผล | หลักฐาน | ที่ขาด |
|---|---|---|---|
| C1 | ปิด | `PaymentSettlementAdjustment.PostsCashAtApproval(..., settlesSourceDocument)` · ผู้เรียก 4 จุด (`DocumentService.cs:1348, 2416, 15406-15410` · `OcrService.cs:7392`) | ตอบข้อ 2 ทิศตรงข้ามไว้ใน §4 |
| C2 | ปิด | `ConvertCoreAsync` `DocumentRounding.Inherit(..., ยกครบทุกบรรทัด)` (:9802) · Clone POST/preview | preview ของ Clone ส่ง `roundingAdjustment` ไปฟอร์มที่แก้บรรทัดได้ ⇒ แก้บรรทัดแล้วผลต่างเดิมยังติดไป (PLAUSIBLE) |
| C3 | ปิด | `OcrDtos` + `PreviewDocumentLinesAsync` · `document-scan.html` ส่งต่อ · `documents.html` เติมค่าก่อน `calcSum` | — |
| C4 | ปิด | `DocumentRounding.EtaxSummation` · `EtaxInvoiceService.cs:886-910, 1001-1008` | ยังไม่ได้ตรวจกับ XSD/Schematron ของ ETDA จริง (header `SpecifiedTradeAllowanceCharge` ไม่มี `TypeCode` แต่ระดับบรรทัดก็ไม่มีเหมือนกัน) |
| C5 | **ปิดบางส่วน** | `Helpers/ApprovalAcknowledgement` · `ApprovalService.cs:335-342` (422 ก่อนบันทึก) · `:441-447` · ลายเซ็น `SignatureApprovalService.cs:447, 521` | เว็บ workflow ปิดแล้ว · **เส้นลายเซ็นที่เจอ [Σ-GAP] ได้ HTTP 500 ข้อความกลาง ๆ หลังบันทึกลายเซ็นแล้ว** (N6) |
| C6 | ปิด | `DocumentsV1Controller.cs:252-255` พรีวิวไม่เรียก AI · `withAiHints:false` | คำเตือนที่ไม่ใช่ gap ยังตอบ 500 (เป็นมาก่อน O1 · `DocumentApprovalWarningsException` ไม่ใช่ `InvalidOperationException`) |
| C7 | ไม่แก้ (มอบทีม S2) | — | อยู่นอกขอบเขตรอบนี้ |
| C8 | ปิด แต่กว้างเกิน | `OcrService.cs:7397-7409` · `OcrPostingReadiness.cs` `paySettled ||= DeferredTag` | เขียนแท็กปลดล็อกแม้ข้อเสนอขัดกับยอดเอกสาร (N5) |
| C9 | ปิด | `CreatePaymentAsync:11137-11140` · JE `if (thbCash != 0m)` · ฝั่งขายถูกกัน (:11209) | JE สมดุล (§4) · PLAUSIBLE: WHT/50 ทวิ ในงวดที่จ่ายเงิน 0 |
| C10 | **ปิดบางส่วน** | `required_call_site_check` +23 กติกา | ล็อกได้แค่ว่ามีการเรียก ไม่ล็อกพฤติกรรม (§5) |
| P2 | ปิด | `DocumentRounding.CapShifts` ต่อสายที่ `BuildScanLinesAsync` | — |
| P3 | ปิด | `MatchTolerance` = 0.005 ใช้ทั้งข้อเสนอ ด่านสแกน และ AutoPost | ไม่มีใบที่เคยผ่านแล้วกลับมาค้าง (§4) |
| P4 | ปิด (อ่านผ่าน ๆ) | `OcrStoredAmountAudit` | — |
| P5 | ปิด | บรรทัดที่ VAT ≤ 0 ไม่ย้าย · ฐาน WHT + ผลต่าง (`TaxService`) | — |
| P6 | ปิด | `EtaxPdfXmlExtractor.AbbreviatedClaimBlock` → `[VAT-CLAIM]` ก่อน · `OcrService.cs:6199-6211` `!vatNotClaimable` จึงไม่ติด `[TAX-INV-PENDING]` (ไม่ไปพัก 11640) | — |

---

## 2. CONFIRMED ใหม่

### N1 (P1 · §86/4 · R5 ทางเข้าอื่น) เส้นหักมัดจำแบบขับ JE ที่ "ยอมเมื่องวดยังไม่ยื่น" ทำให้ผู้ซื้อได้ใบกำกับสองใบสำหรับ VAT ก้อนเดียว และผู้ใช้เว็บเลี่ยงปุ่มได้
- ด่านของปุ่ม "หักมัดจำ" (`DocumentService.cs:4069-4078`) บล็อก**ทุกงวด** และคอมเมนต์เขียนเหตุผลไว้เองว่า "งวดที่ยังไม่ยื่นก็ผิดเหมือนกัน … **ผู้ซื้อถือใบกำกับสองใบสำหรับภาษีก้อนเดียว**" (ข้อความ `GrossApplyBlockedMessage` ก็เขียนแบบเดียวกัน)
- `DrivesGrossApply` (L2 C1) ดูแค่ว่า**งวดของผู้ขาย**ยื่นแล้วหรือยัง ⇒ งวดยังไม่ยื่น = `AllowedVatMoved` · ธงไปอยู่ที่ `InternalNotes` อย่างเดียว ผู้อนุมัติไม่เห็นกล่อง 422
- ทางเข้าที่ได้ผลนี้ไม่ได้มีแค่ integration: `documents.html:8952-8971` ติ๊ก "ขายเงินสดใบเดียว" แล้วเลือกมัดจำ ⇒ ส่ง `depositAppliedDrivesJournal = true`
  - มัดจำใบเดียวกัน ผู้ใช้คนเดียวกัน: ไม่ติ๊ก = ถูกบล็อก · ติ๊ก = ผ่าน
  - ข้อความคอมมิตที่ว่า "ปุ่ม 'หักมัดจำ' ยังเข้มเท่าเดิม" จริงเฉพาะตัวปุ่ม ช่องทางข้าง ๆ ในฟอร์มเดียวกันหลวมลง
- ตัวเลข (การจอง 7,450 · มัดจำ VAT ทันที 2,000 = ฐาน 1,869.16 + VAT 130.84 ออกเดือน M1 · เช็คเอาต์เดือน M2):

| | ปุ่ม/โมดูลที่พัก (เข้ม) | ขับ JE เมื่อ M1 ยังไม่ยื่น (หลวม) | ขับ JE เมื่อ M1 ยื่นแล้ว (บล็อก) |
|---|---|---|---|
| ใบสุดท้าย | ฐาน 5,093.46 · VAT 356.54 · รวม 5,450.00 | รวม 7,450 · VAT 487.38 | ใบ Approved มีเลขแล้ว แต่ไม่มี JE |
| VAT บนใบกำกับที่ผู้ซื้อถือ | 130.84 + 356.54 = **487.38** | 130.84 + 487.38 = **618.22** (เกิน 130.84) | 618.22 |
| ภ.พ.30 ของผู้ขาย | M1 130.84 · M2 356.54 | M1 **0** (แถวมัดจำถูกข้ามเพราะ `DepositAppliedToDocumentId` · `TaxService.cs:615`) · M2 487.38 | M1 130.84 (ยื่นแล้ว) · M2 487.38 (`TaxService` นับเอกสาร Approved ตามสถานะ) = 618.22 แต่ Cr 21911 ใน GL มีแค่ 130.84 |
| ผลกฎหมาย | ถูก | ถ้า M1 ≠ M2: M1 ยื่นขาด 130.84 ⇒ เงินเพิ่ม 1.5%/เดือน (≈ 1.96 บาท/เดือน) + เบี้ยปรับ · ผู้ซื้อที่จด VAT เคลมภาษีซื้อเกิน 130.84 ไม่ว่า M1 จะตรงกับ M2 หรือไม่ | ภ.พ.30 ≠ GL อยู่ 487.38 จนกว่าจะมีคนแก้ |

- ทีม L2 ตั้งเป็นคำถามเจ้าของ Q8 ไว้แล้ว แต่ Q8 ถามเรื่อง "เดือน" เท่านั้น **ไม่ได้ถามเรื่องใบกำกับสองใบของผู้ซื้อ** · ข้อความธง `DrivesVatMovedNote` ก็พูดถึงแค่เรื่องเดือน
- ข้อเสนอ: ให้ Q8 ครอบเรื่องใบกำกับของผู้ซื้อด้วย · อย่างน้อยให้เส้นฟอร์มเว็บ (มีคนอยู่หน้าจอ) ใช้ด่านเข้มเท่าปุ่ม แล้วผ่อนเฉพาะ integration ถ้าเจ้าของอนุมัติ

### N2 (P1 · R1/R5) ใบ integration ที่ถูกบล็อก "ล้มดัง" แค่ครั้งแรก หลังจากนั้นมีทางกลับไปสู่ผลเดิมของ C1 ได้ 3 ทาง
- สถานะหลังบล็อก (`IntegrationService.cs:1053-1075`): ใบ TaxInvoice เป็น `Approved` มีเลขแล้ว · `IssuedAsCashReceipt = true` · `BalanceDue = 7,450` · ไม่มี JE · e-Tax hook ถูกข้ามเพราะ `return` ก่อน `_issuedHooks.RunAsync` (:1091)
- ทางที่ 1 — ยิงซ้ำ (ต้นทางมักยิงซ้ำเมื่อได้ `success=false`): ด่าน idempotency `:704-724` เจอใบเดิม ⇒ ตอบ **`"Already synced"` `success=true`**
- ทางที่ 2 — resync (คอมเมนต์ `:717-719` แนะนำเองว่า "JE หาย → ใช้ resyncUpdate=true เพื่อ rebuild")
  - `ResyncGuardAsync` (:3009) ผ่าน เพราะ `PaidAmount = 0` และงวดของใบยังไม่ยื่น
  - `originals.Count == 0` ⇒ `PostMappingJournalAsync` (:2901) = ตั้งหนี้: Dr ลูกหนี้ 7,450 / Cr รายได้ + Cr 21911 487.38 · 217xx ค้าง 1,869.16 · `IssuedAsCashReceipt` ยังเป็น true
  - **ได้ผลเดียวกับ C1 ของรอบแรกทุกประการ** และ log เป็น "Updated"
- ทางที่ 3 — รับชำระ: คำตอบบอก "ห้ามรับชำระยอดค้างของใบนี้" แต่ไม่มีโค้ดตัวไหนอ่านธงนี้ (`grep ImmediateVatGrossApplyRuleCode` มีแค่ 3 จุดที่เขียนธง) ⇒ `CreatePaymentAsync` และ `ProcessPaymentAsync` รับชำระได้ ⇒ Cr ลูกหนี้ทั้งที่ไม่เคยมี Dr ลูกหนี้
- ข้อเสนอ:
  - ให้ idempotency กับ resync รู้จักธง `RD-86/4-DEPOSIT-TIV-DOUBLE` (ตอบ false ซ้ำ / ห้าม mapping JE)
  - ทุกเส้นรับชำระต้องบล็อกใบที่มีธงนี้
  - ประทับ `[ETAX-AUTO-FAILED]` หรือธงเทียบเท่า ให้ใบกำกับที่ออกไปแล้ว

### N3 (P1 · R1 สถานะปลายทาง) `SyncRefundPaidFromDeposits` นับซ้ำการคืนที่ทำก่อนหน้า และบันทึกไม่ทันก่อน throw ⇒ การจองค้าง "ค้างคืน" ถาวร
- `Lifecycle.cs:1092-1101`:
  - `refundedOnDocs = Σ DepositRefundedAmount` นับ**ทุก**การคืนบนใบมัดจำ รวมการคืนที่ทำก่อนยกเลิก/ก่อนเช็คเอาต์
  - แต่ `PlanCancellation`/`PlanCheckout` ตัดการคืนพวกนั้นออกจาก `RefundAmount` ไปแล้ว (ผ่าน `Remaining`) ⇒ **นับสองครั้ง**
- `:1049-1051` ตัวซ่อมทำให้ pending เป็น 0 แล้ว `ValidateRefundPayment` throw ("การจองนี้ไม่มียอดค้างคืนเงิน") **ก่อน** `SaveChangesAsync` ⇒ สิ่งที่ซ่อมไม่ถูกบันทึก
- เคส ก — มีการคืนมัดจำบางส่วนจากหน้า "เงินมัดจำ" ก่อนยกเลิก:
  - มัดจำ 2,000 · คืนจากหน้าเงินมัดจำ 500 · ยกเลิกค่าปรับ 1,000 ⇒ แผน: ริบ 1,000 · ค้างคืน 500
  - กด "ยืนยันคืนเงิน 500": gap = min(500, 500) − 0 = 500 ⇒ pending 0 ⇒ throw
  - ⇒ **แขกไม่ได้เงิน 500** · 217xx ค้าง ≈ 467.29 ฐาน · หน้าจอบอก "ค้างคืน 500" ตลอดไป และกดบันทึกไม่ได้ทุกครั้ง
- เคส ข — เคสที่ตัวซ่อมตั้งใจแก้ (คำขอก่อนล้มหลัง `RefundDepositAsync` commit แต่ก่อนบันทึกการจอง) ที่ใบสุดท้าย/ใบเดียว:
  - gap = ยอดค้างทั้งหมด ⇒ pending 0 ⇒ throw ⇒ ค้างถาวร
  - **ตัวซ่อมใช้ได้แค่เมื่อ gap < pending เท่านั้น**
- ข้อเสนอ:
  - นับเฉพาะการคืนที่เกิดหลัง `CancelledAt`/`CheckedOutAt` (หรือจาก JE ที่อ้างเลขการจอง)
  - บันทึกผลซ่อมก่อนตรวจยอด และถ้า pending เป็น 0 หลังซ่อม ให้จบแบบสำเร็จ (สถานะ Paid) แทนการ throw

### N4 (P2) C6 ยังค้าง 0.01 เมื่อคืนเป็นหลายงวด
- `ValidateRefundPayment` ยอมให้คืนบางส่วน แต่ด่านของ `RefundDepositAsync` คิดฐานที่คืนไปแล้วแบบสะสม `round(ΣG × (1−p))` ขณะที่ `RefundableGross` ของงวดถัดไปคิดจากฐานคงเหลือ ⇒ ต่างกันได้ 0.01
- จำลอง 20,000 กรณี (มัดจำ 1–3 ใบ · ค่าปรับสุ่ม · คืนสองงวดสุ่ม): **ขาด 0.01 = 1,915 กรณี (9.6%)** · คืนงวดเดียว = 0 กรณี
- ตัวอย่างใบเดียว:
  - มัดจำ 4,727.07 (ฐาน 4,417.82 / VAT 309.25) · ค่าปรับ 1,500.51 ⇒ ค้างคืน 3,226.56
  - คืนงวดแรก 72.02 ⇒ ยอดคืนได้สูงสุดเหลือ 3,154.53 แต่ยอดค้าง 3,154.54 ⇒ ถูกปฏิเสธ
  - คืน 3,154.53 ผ่าน แต่ค้าง 0.01 ถาวร (`RefundState=Pending`)

### N5 (P2 · R2 "ไม่รู้" → ค่าแต่ง) `[PAY-AT-PAYMENT]` ปลด `[PAY≠TOTAL]` แม้ข้อเสนอจากกระดาษขัดกับยอดเอกสาร
- `OcrService.cs:7397-7409` เขียนแท็กให้ PI/Expense ทุกใบที่มี `[PAY≠TOTAL]` ไม่ว่า `planFits` เป็นอะไร
  - ครอบ 3 กรณี: ไม่มีข้อเสนอ · มีหัก ณ ที่จ่าย · และ**ข้อเสนอบอกยอดใบกำกับ X แต่เอกสารลงยอด Y ≠ X**
  - ข้อความของแท็กประกาศว่า "ลงยอดตามใบกำกับ {Y} (**ถูกต้อง อนุมัติได้ตามปกติ**)"
- `OcrPostingReadiness` ถือแท็กนี้เท่ากับ `[PAY-SETTLED]` ⇒ ปุ่ม "สร้าง+อนุมัติ" และ "อนุมัติเลย" ใน LINE ผ่านได้
- `[PAY≠TOTAL]` เดิมกันกรณีที่ตัวแยกยอดอาจเลือกยอดรวมผิดตัวด้วย เคสที่มีข้อเสนอที่ขัดกันคือหลักฐานว่ายอดอาจผิด
  - ⇒ ด่านเดียวที่เคยหยุดใบนี้ถูกถอดออก พร้อมข้อความว่า "ถูกต้อง" ซึ่งระบบไม่ได้รู้จริง
- ข้อเสนอ: ปลดล็อกเฉพาะเมื่อ `planFits` เป็นจริง · กรณีไม่มีข้อเสนอให้ใช้คำว่า "ยังไม่ได้ตรวจส่วนต่าง" · กรณีข้อเสนอขัดกับยอดเอกสาร ให้คงการหยุดไว้

### N6 (P2 · F2 ข้อ 7 ล้มดัง) เส้นลายเซ็นที่เจอ `[Σ-GAP]` ได้ HTTP 500 หลังบันทึกลายเซ็นแล้ว
- `DocumentApprovalWarningsException` สืบจาก `Exception` ตรง ๆ (`DocumentService.cs:17707`) และ controller ของเส้นลายเซ็นไม่จับ
  - ⇒ `ExceptionMiddleware` ตอบ 500 "เกิดข้อผิดพลาดภายในระบบ (รหัสอ้างอิง …)"
- ลายเซ็นภายใน (`SignatureApprovalService.cs:521`): ทุกขั้นเซ็นครบแล้วและถูกบันทึกไปแล้ว ⇒ ไม่มีการเซ็นครั้งถัดไปที่จะสั่งอนุมัติซ้ำ
  - เอกสารค้างอยู่ที่ร่าง/รออนุมัติ และไม่มีข้อความบนเอกสารบอกว่าต้องไปกดอนุมัติเองที่หน้าเอกสาร
- ลายเซ็นลูกค้าภายนอก (`:412-449`): แถว customer approval ถูกตั้งเป็น Approved และบันทึกลายเซ็นแล้วก่อนเรียกอนุมัติ
  - ลูกค้ากดซ้ำ ⇒ ไม่เจอแถว Pending ⇒ **สร้างขั้นลูกค้าใหม่และลายเซ็นซ้ำ** (:389-409)
- ทางไปต่อมีอยู่ (อนุมัติที่หน้าเอกสาร ระบบจะถามให้รับทราบ) แต่ไม่มีใครบอกผู้ใช้
- ใบสำคัญจ่ายเงินสดที่อนุมัติอัตโนมัติ (`:1549-1557`) ก็ค้างเป็นร่างโดยมีแค่ `LogInformation`
- ข้อเสนอ: จับ exception นี้ในเส้นลายเซ็นแล้วเขียนหมายเหตุบนเอกสาร พร้อมตอบข้อความที่บอกทางไปต่อ

---

## 3. PLAUSIBLE

- **P-a (G1 "ไม่มีข้อมูล ≠ ผ่าน")** `GuardDrivesGrossApplyAsync` ถือว่า "ไม่มีแถว TaxReport" = ยังไม่ยื่น
  - บริษัทที่ยื่นนอกระบบ หรือเลยกำหนดยื่น (วันที่ 15 ของเดือนถัดไป) ไปแล้วแต่ไม่ได้กดประกาศว่ายื่น ⇒ `AllowedVatMoved`
  - ⇒ ภ.พ.30 เดือนเช็คเอาต์ที่ออกจากระบบนับ 130.84 ซ้ำกับที่ยื่นไปแล้ว
  - ควรใช้ "เลยกำหนดยื่นแล้ว" เป็นหลักฐานอีกชั้นหนึ่ง
- **P-b** กดเช็คเอาต์สองครั้งพร้อมกัน ⇒ ใบกำกับสองใบ (ไม่มีล็อกระดับการจอง · เป็นมาก่อน L2 แต่ตอนนี้ C2 เรียก `CreateDocumentAsync` + `ApproveDocumentAsync` ในคำขอเดียวแล้ว)
- **P-c (O1 C9)** PI ที่มี WHT ถูกชำระ 438 ผ่านทางที่ไม่หัก WHT แล้วปิดยอดด้วย "เงิน 0 + คูปอง"
  - `isFinalPayment` จริง ⇒ `paymentWht = remainingCap` ทั้งก้อน (`:11278-11292`)
  - ⇒ 50 ทวิ ถูกออกเป็น Issued ณ วันที่ของงวดที่ไม่มีเงินออก (`:11493-11512`)
- **P-d** ผังบัญชีของบรรทัดปรับไม่ถูกจำกัด (`Check` ตรวจแค่ว่ามีผัง/ยอด/ทศนิยม)
  - ใส่ผังเงินสด/ธนาคาร (111xx) −98 กับเงิน 0 ⇒ เงินออก 98 ใน GL แต่ `Payment.Amount = 0` และ `BankAccount.CurrentBalance` ไม่ขยับ
  - ช่องนี้มีตั้งแต่ O1 รอบแรก แต่ C9 ทำให้ทำได้โดยไม่มีเงินจริงประกอบ
- **P-e** `ActualPaidNotApplicableReason` ให้ PI/Expense ใช้ได้เสมอ แต่ PI ที่ `PaymentType = Cash` ได้ `BalanceDue = 0` ตั้งแต่สร้าง (:1269, 1513-1517) ⇒ ไม่มีหน้าบันทึกการชำระให้ใช้ค่า ⇒ silent no-op ยังเหลือ 1 ทาง
- **P-f** P8 เดิม (กระทบยอดธนาคารแบบจับคู่เอกสารเห็นยอด 536 ไม่ใช่ 438) ตอนนี้ครอบใบสำคัญจ่ายที่แปลงจาก PI ด้วย (เพราะ C1 ทำให้ขาเงินสดเป็น 438)
- **P-g** preview ของ Clone ส่งผลต่างปัดเศษเข้าฟอร์มที่แก้บรรทัดได้ ⇒ แก้บรรทัดแล้วผลต่างของกระดาษเดิมยังติดไป (ไม่เกิน ±1 บาทตาม `Validate`)
- **P-h** e-Tax XML ระดับหัว `SpecifiedTradeAllowanceCharge` (ไม่มี `TypeCode`) ยังไม่ได้ผ่าน XSD/Schematron ของ ETDA จริง
- **P-i (เล็ก)** `DepositPolicyResolver.cs:74-88` แทรก enum `DrivesGrossApplyVerdict` คั่นระหว่าง XML doc comment ของคลาสกับตัวคลาส ⇒ คำอธิบายคลาสไปติดที่ enum (ไม่กระทบการคอมไพล์ เพราะไม่ได้เปิด `GenerateDocumentationFile`/`TreatWarningsAsErrors`)

---

## 4. ตรวจแล้วไม่มีปัญหา (ตอบคำถามที่สั่งมา)

- **O1 C1 ทิศตรงข้าม (PV ที่แปลงจาก PI และผู้ใช้ตั้งใจเป็นเครดิต)**:
  - JE ของใบสำคัญจ่ายที่มี `RelatedDocumentId` ลงเงินสด**เสมอ**ตั้งแต่ก่อน O1 (`DocumentService.cs:15171-15237` ไม่มีสาขา Credit)
  - PV ที่สร้างเดี่ยว ๆ ก็ห้ามเป็น Credit (:1227-1232) ⇒ "PV ที่ยังไม่จ่าย" ไม่มีอยู่ในระบบ
  - C1 ไม่ได้ทำให้มีเงินสดออกเพิ่ม แค่ทำให้ช่องยอดชำระจริงมีผลกับขาเงินสดเดิม
  - ช่องนี้บนเอกสารชนิดอื่นถูกปฏิเสธดังทั้งตอนบันทึกและตอนลง JE
- **O1 C8/C9 JE**:
  - ชำระ 0 + คูปอง −98: `Check(0, 98, [−98])` ⇒ ยอดที่ปิด 98 · JE Dr เจ้าหนี้ 98 / Cr 51150 98 · ไม่มีขาเงินสด ⇒ สมดุล
  - ฝั่งขายถูกกัน (:11209) · บรรทัดปรับที่ทำให้ยอดปิด ≤ 0 ถูกปฏิเสธ (บวก +37 กับเงิน 0 = ปิด −37 ⇒ fail)
  - void กลับด้วย `Amount + Fee + SettlementAdjustmentAmount` ⇒ ไม่เปิดช่องปิดหนี้ด้วยยอด 0 ที่ไม่มีบรรทัดปรับ
  - `[PAY-AT-PAYMENT]` ไม่ได้ปลดแท็กอื่นใน `BlockingTags` (ปลดเฉพาะ `[PAY≠TOTAL]`) — ปัญหาอยู่ที่เงื่อนไขการเขียนแท็ก (N5)
- **O1 P3 ใบที่เคยผ่านที่ 0.02**:
  - ใบสำคัญจ่ายที่ต่าง 0.01–0.02: เดิมข้อเสนอผ่านแล้วไปล้มที่ AutoPost 0.005 เป็น `[APPROVE-FAIL]` อยู่แล้ว ตอนนี้หยุดที่ `[PAY≠TOTAL]` พร้อมเหตุผล
  - PI: ตอนนี้ผ่านด้วย `[PAY-AT-PAYMENT]` ⇒ ไม่มีใบไหนที่เคยอนุมัติเองได้แล้วกลับมาค้าง
- **O1 C5 workflow เว็บ**: ด่านพรีวิวอยู่ก่อน `action.Status = …` (ไม่บันทึกผลครึ่งทาง) · approval.html ถามแล้วส่งซ้ำ · อนุมัติหลายรายการข้ามใบที่มีคำเตือน ⇒ มีทางไปต่อ
- **L2 C2 gap-free**:
  - เลขใบสุดท้ายออกครั้งเดียว (Approve) หลังวางแผนเสร็จ · กดซ้ำเข้า `ResumeCheckOutAsync` ไม่สร้างเอกสารใหม่
  - ไม่รับรู้ซ้ำ เพราะหักด้วย JE ที่ `DepositRealizedForDocumentId == finalId`
  - ไม่ตัดชำระซ้ำ เพราะข้าม `AppliedToDocumentId == finalId` และจำกัดยอดด้วย `BalanceDue` · ค่าเสียหายไม่ถูกเพิ่มซ้ำ
- **L2 C2 ตัวเลข**: จ่ายเต็ม 7,450 แล้วเลื่อนจนเหลือ 5,000
  - โหมด VAT ทันที: หักฐาน 4,672.90 (VAT 327.10) · ใบสุดท้าย 0.00 · ค้างคืน 2,450 ⇒ ตอนคืนออกใบลดหนี้ VAT 160.28 ⇒ VAT สุทธิ 487.38 − 160.28 = 327.10 = VAT ของ 5,000 ✓
  - โหมดพัก/เต็มยอด: ตัดชำระ 5,000 · ค้างคืน 2,450 ✓
- **L2 C4 migration**: `ADD COLUMN IF NOT EXISTS` · JE กลับรายการไม่ได้พาค่า `DepositRealizedForDocumentId` ไปด้วย และถูกกรองด้วย `OriginalEntryId == null` ทั้งฝั่ง void และฝั่ง resume
- **L2 C6/C7 ภ.พ.30**: ภ.พ.30 = ผลรวม VAT บนใบ = ยอดสุทธิ 21911 ในทุกกรณีที่เดิน (ส่วนต่าง 0.01 อยู่ที่ VAT ของใบจริง ไม่ใช่ความต่างระหว่างรายงานกับ GL)
- **คอมไพล์ (อ่าน)**:
  - overload `ApproveDocumentAsync(…, bool)` กับ `(…, ApprovalAckSource, bool)` ไม่ชนกัน (จำนวนอาร์กิวเมนต์ต่างกัน · ผู้เรียกเดิมใช้ชื่อ `acknowledgeWarnings:` ที่มีแค่ใน overload 4 ตัว) · ไม่มีคลาสอื่น implement `IDocumentService`
  - `ApprovalWarningsResponse(ex.Warnings, null)` ตรงลายเซ็น
  - `JobLock.RunExclusiveAsync(_db, scope, part, work, logger, companyId)` ตรงลายเซ็น
  - `ReverseJournalEntryAsync(... reversalDate:, description:, systemTriggered:)` ตรงลายเซ็น
  - `PreviewTotals` → `AllocateBillDiscount`/`ReconcileTaxRounding` รับ `IReadOnlyList<DocumentLineRequest>` · `ComputeLineAmounts` เป็น static · `InternalsVisibleTo` มีใน csproj
  - catch ใหม่ใน integration อยู่ก่อน `catch (Exception)` และใช้ตัวแปรที่อยู่ใน scope ทั้งหมด

---

## 5. เทสต์/checker: ถอดการแก้แล้วยังเขียวไหม

| การแก้ที่ถอดออก | ผล |
|---|---|
| L2 C1 catch ใน integration: เปลี่ยนเนื้อ catch กลับไปตั้งหนี้ แต่คงข้อความรหัสกฎไว้ | **เขียว** (กติกาตรวจแค่ว่ามีข้อความและลำดับก่อน `catch (Exception exCash)`) |
| L2 C2 สาขา `AppliedToDocumentId == finalDocumentId` ใน `PlanCheckout` | **เขียว** (เทสต์ส่งใบที่ยอดคงเหลือเป็น 0 จึงไม่ถึงสาขานี้) |
| L2 C4 purge ไม่เรียก `ReverseDepositRealizationsForAsync` | **เขียว** (มีกติกาเฉพาะ `VoidDocumentAsync`) |
| L2 C4 ลบบรรทัดคืนยอด `DepositRealizedAmount` ในตัวกลับรายการ | **เขียว** |
| L2 ตัวซ่อม `SyncRefundPaidFromDeposits` ผิดตรรกะ (N3) | ไม่มีเทสต์ (อยู่ใน service) |
| L2 C6 คืนหลายงวด | ไม่มีเทสต์ (N4) |
| O1 C9 `if (thbCash != 0m)` / `closesByAdjustmentOnly` | **เขียว** (ไม่มีกติกา call-site · เทสต์ helper ตรวจแค่ `Check`) |
| O1 C8 เงื่อนไข `planFits` ของการเขียนแท็ก | **เขียว** (กติกาตรวจแค่ว่ามีการเรียก `DeferredNote(`) |
| O1 C5 ด่านพรีวิวใน `SubmitActionAsync` | จับได้ (กติกาลำดับ) |
| L2 C2/C7 ตัวเลขผ่าน `DocumentService.PreviewTotals` | จับได้ถ้าสูตรจริงเปลี่ยน ✓ (เป็นเทสต์ชิ้นเดียวในชุดนี้ที่เรียกโค้ด service จริง) |

**สรุป**: `required_call_site_check` ล็อกว่า "เรียกหรือไม่ และเรียกตามลำดับหรือไม่" ได้จริง (มี negative test ในตัว) แต่ไม่ล็อก "เรียกแล้วได้ผลถูกไหม" ⇒ N2 N3 N5 ผ่าน checker ทุกตัวได้หมด

## 6. ลำดับที่แนะนำ
1. **N3** — แก้ในไฟล์เดียว · ทำให้แขกไม่ได้เงินคืนและกดบันทึกไม่ได้
2. **N1 + N2** — ขยาย Q8 ของเจ้าของให้ครอบเรื่องใบกำกับสองใบของผู้ซื้อ · ปิดทางยิงซ้ำ/resync/รับชำระของใบที่ถูกบล็อก
3. **N5** — ปลดล็อกเฉพาะเมื่อ `planFits`
4. **N4 / N6 / C4 (restore)** — ตามลำดับ
