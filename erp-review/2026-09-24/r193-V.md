# รอบ 193 · ทีม V — ค่าตั้งต้องถึงทุกส่วนงาน (S-01 · S-02 e-Tax · S-05 · S-10 CMS · ของแถม lead)

สเปก = `erp-review/2026-09-24/audit-settings.md` §2/§3/§5 · ทำเฉพาะข้อที่ไม่ต้องรอเจ้าของ (Q1/Q2 ยังเปิด — ไม่ได้ตัดสินแทน)

> ⚠️ **ยังไม่ได้คอมไพล์** (เครื่องนี้ไม่มี .NET SDK) — CI บน `claude/**` คือคอมไพเลอร์ตัวแรก · `bash tools/check_all.sh`:
> checker **45** ตัวผ่าน (รวม 2 ตัวใหม่) · simulation 5 ✅ · brace/U+FFFD 35 ไฟล์ ✅ · ❌ ตัวเดียว = TEST_PLAN §0 (ห้ามแตะตามโจทย์ — ดู "เอกสารที่ต้องขยับ")

---

## S-01 (P0) สถานะ VAT สองธง — stopgap ไม่เลือกต้นทาง

**verify แล้วจริง**: `Company.IsVatRegistered` default false (`Company.cs:48`) · `CompanySettings.VatRegistered` default true (`CompanySettings.cs:115`) ·
`new CompanySettings { CompanyId }` 6 จุด · ผู้อ่าน `?? true` 6 จุด (DocumentService 5 + IntegrationService 1) · สร้างบริษัท 3 ทาง (`CompanyService.CreateAsync` ·
`AuthService` สมัคร · `AuthService` SSO) ไม่สร้างแถวค่าตั้ง · `SettingsService.UpdateSettingsAsync` sync `VatRegistered` กลับทับ `Company` ⇒ สถานการณ์ในรายงานเกิดได้จริง

| ทำอะไร | ที่ไหน |
|---|---|
| **`Helpers/CompanySettingsFactory`** ตัวสร้างแถวค่าตั้งตัวเดียว — `NewFor(Company)` (pure) · `AddNewAsync(db, companyId)` (lazy: อ่านธง/อัตราบริษัท + `Add` ไม่ save) · seed `VatRegistered`/`DefaultVatRate` จาก `Company` · ไม่พบบริษัท = `KeyNotFoundException` | ไฟล์ใหม่ |
| แถวค่าตั้งเกิด**พร้อมบริษัท** | `CompanyService.cs:79` · `AuthService.cs:208` (สมัคร) · `AuthService.cs:679` (SSO) |
| lazy-create 6 จุดเรียก `AddNewAsync` | `SettingsService.cs:532` · `DocumentEmailService.cs:394` · `EmailConfigController.cs:94` · `OwnerConfigController.cs:96` · `LineConfigController.cs:95` · `EtaxController.cs:336` (จุดที่ 6 — รายงานไม่ได้ระบุ) |
| **`Helpers/CompanyVatStatus`** — `IsRegistered(companyFlag, settingsFlag?)` = ค่าตั้งถ้ามีแถว ไม่งั้นบริษัท (**ลำดับเดิมเมื่อมีทั้งคู่ · TODO(Q1)**) · `DefaultRate` · `Compare` → `VatFlagAgreement` · `IsRegisteredAsync`/`ProfileAsync` ห่อ query (กรอง CompanyId) | ไฟล์ใหม่ |
| ผู้อ่าน `?? true` → `CompanyVatStatus` (รูปแบบ `?? true` บนธง VAT เหลือ **0 จุด**) | `DocumentService.cs:1362` (สร้าง—เคลมภาษีซื้อ) · `:2396` (แก้ไข) · `:4778` (ด่าน §90/2) · `:9274` (`IsCompanyVatRegisteredAsync` แปลงเอกสาร) · `:16997` (เตือน) · `IntegrationService.cs:1918` (`GetCompanyVatProfileAsync` — อัตราถอยไป `Company.VatRate` แทน 7) |
| **รายงานอ่านอย่างเดียว** (คำตัดสินข้อ 14 · ลอกรูปจาก `ContactHygieneController` ของ O2) | `Controllers/VatFlagConsistencyController.cs` |

รายงาน:
- `GET /api/companies/{companyId}/vat-flag-consistency` — `[Authorize]` + `[RequirePermission(CompanySettings.Edit)]` (เจ้าของผ่านอัตโนมัติ) · ทุก query กรอง `CompanyId` ·
  คืนธงทั้งสอง · ค่าที่เส้นเอกสารใช้วันนี้ · `agreement` (ชื่อ enum) + คำอธิบายไทย · ถ้าขัดกัน: ใบกำกับ/ใบเสร็จ/CN/DN ที่มี VAT ซึ่งออกแล้ว (`DocumentStatusRules.NotIssued` ตัดร่าง)
  ตั้งแต่วันที่สร้างแถวค่าตั้ง — ยอดต่อชนิด + รายการ ≤ 200 ใบ (`documentsTruncated`)
- `GET /api/admin/vat-flag-consistency` — `[Authorize(Roles="SystemAdmin")]` (ข้ามบริษัทโดยเจตนา) · เฉพาะบริษัทที่ขัดกัน + ยอดต่อชนิด (ไม่มีรายการใบ — ดูรายบริษัท) · `companiesWithoutSettingsRow`
- ⚠️ ข้อจำกัดที่ต้องรู้: **ระบบไม่มีประวัติการเปลี่ยนธง** ⇒ "ช่วงที่ขัดกัน" นับจาก `CompanySettings.CreatedAt` (อาจยาวกว่าจริง) — ข้อความบอกไว้ในผลลัพธ์ · CN/DN อาจเป็นฝั่งซื้อ ต้องดูรายใบ · ไม่มีหน้าเว็บ (endpoint อย่างเดียวตามโจทย์)

ผลที่ต้องได้ (ตามโจทย์): บริษัทไม่จด VAT → แถวค่าตั้งเกิดพร้อมบริษัทด้วย `VatRegistered=false` → `settings.html:2220` ไม่ติ๊ก → กดบันทึกส่ง `false` → `Company.IsVatRegistered` ไม่พลิก ✓
(ไม่ได้แก้ `settings.html` — ค่าที่หน้าเห็นมาจากแถวที่ถูก seed แล้ว)

## S-02 (ส่วน e-Tax เท่านั้น) — `IssuedDocumentHooks`

**verify**: จริง — POS `IssueTaxInvoiceAsync` · Integration 6 เมธอด ประทับ `Approved` เองโดยไม่มี e-Tax · **ไม่จริงบางส่วน**:
- **Import (`ImportExportService.cs:1076`) ไม่ใช่ช่องโหว่ e-Tax** — เป็น `ImportOpeningSubledgerAsync` (ลูกหนี้/เจ้าหนี้ยกมา `Invoice`/`PurchaseInvoice` `IsOpeningBalance`) ซึ่ง**ห้าม**ออก e-Tax (ใบกำกับออกในระบบเดิมแล้ว) ⇒ ใส่ baseline พร้อมเหตุผล ไม่ต่อสาย
- **CMS `SignDigitally: true` ตายตัว — ผลจริงเท่ากับอ่านค่าตั้ง** เพราะ `EtaxInvoiceService.GenerateAsync` ลงนามเมื่อ `etaxConfig.AutoSign && request.SignDigitally` (ตรวจ `EtaxAutoSign` อีกชั้น) ·
  ปัญหาจริงของ CMS คือเรียก `GenerateAsync` **ซ้ำ**หลัง `ApproveDocumentAsync` ออกให้แล้ว (ได้ "มี e-Tax แล้ว" เป็น `LogWarning` ทุกใบ) และล้มแล้วเงียบ — แก้แล้ว
- เพิ่มเติมที่รายงานไม่ได้ระบุ: `TryAutoGenerateEtaxAsync` เดิมกรองแค่ชนิด ⇒ ใบรับมัดจำ VAT พักรอ · ใบเสร็จรับชำระใบกำกับ · CN/DN ฝั่งซื้อ ถูก `GenerateAsync` โยน แล้วประทับ
  `[ETAX-AUTO-FAILED]` ทั้งที่ไม่มีอะไรล้ม (คำเตือนที่ฟ้องใบถูก — F2 ข้อ 8) ⇒ แยกเป็น "ข้ามโดยเจตนา" แล้ว

| ทำอะไร | ที่ไหน |
|---|---|
| `IIssuedDocumentHooks.RunAsync(companyId, Document)` + `(companyId, documentId)` — ย้าย `TryAutoGenerateEtaxAsync` มาทั้งก้อน · อ่าน `EtaxEnabled` + `EtaxAutoSign` · idempotent (มี e-Tax ไม่ใช่ Error = ข้าม) · ล้ม = **ไม่ throw** แต่ประทับ `[ETAX-AUTO-FAILED]` ลง `InternalNotes` **แล้ว SaveChanges** (เดิมต่อท้ายแต่ไม่ save เอง — พึ่ง save ที่ตามมา) + LogWarning · save ป้ายไม่ได้ = LogError · ถอดแถว EtaxInvoice ที่ Add ค้างก่อน save | `Services/Interfaces/IIssuedDocumentHooks.cs` · `Services/Implementations/IssuedDocumentHooks.cs` · `Program.cs` (AddScoped) |
| **`Helpers/EtaxAutoIssueScope`** — ชุดชนิด e-Tax (T01–T04) ชุดเดียว · `Judge` → `EtaxAutoSkip` (NotEtaxType · NotIssued · Voided · DeferredDepositReceipt · ReceiptSettlesTaxInvoice · PurchaseSideAdjustment) · "ไม่รู้ใบต้นทาง" = ไม่ข้าม (ให้ตัวออกดัง) | ไฟล์ใหม่ |
| `EtaxInvoiceService.GenerateAsync` ใช้ predicate ชุดเดียวกัน (ชนิด · มัดจำพักรอ · `AdjustmentNoteAccount.SourceIsPurchaseSide` แทนลิสต์มือ) — ข้อความ/พฤติกรรมเดิม | `EtaxInvoiceService.cs` ~100 · ~133 · ~193 |
| `ApproveDocumentAsync` เรียก hook (ลบ `TryAutoGenerateEtaxAsync` เดิม) · ctor รับ `IIssuedDocumentHooks?` ไม่มี DI = สร้างตัวจริงเอง (ห้าม null แล้วข้ามเงียบ) | `DocumentService.cs:5481` · `:81` |
| POS ใบกำกับเต็มรูป — เรียก**หลัง commit** | `PosService.Orders.cs:758` · ctor `PosService.cs` (required) |
| Integration: TIV `:1052` · CN `:1330` · DN `:1433` · Expense `:3370` · PV `:3497` · CIL `:3744` (สามตัวหลังไม่มี e-Tax — hook ข้ามในตัว แต่ทุกจุดที่ประทับ Approved เดินขั้นเดียวกัน) | `IntegrationService.cs` · ctor (required) |
| CMS: ถอดขั้น 6 เดิม → เรียก hook **เฉพาะเมื่อไม่ได้อนุมัติในรอบนี้** (ยืนยันซ้ำ/อนุมัติไว้ก่อน) · ถอดฟิลด์ `_etaxService` ที่ไม่มีใครอ่านแล้ว | `CmsCommerceService.cs:1150` · ctor `:38` |
| **checker ratchet `tools/approved_status_writer_check.py`** + `tools/approved_status_writer_baseline.txt` — ฟ้อง `Status = … DocumentStatus.Approved …` (รวม initializer/ternary) นอก `DocumentService*.cs` ที่เมธอดเดียวกันไม่เรียก `…IssuedHooks.RunAsync(` · baseline 3 เมธอด/4 จุดพร้อมเหตุผล (SampleData · PO ข้ามบริษัท · ยอดยกมา) · `--self-test` 4 ทิศ (จับจุดลืม · hook ในเมธอด**อื่น**ไม่นับ · ไม่ฟ้อง `==`/คอมเมนต์ · ไม่ฟ้องเจ้าของ) · **ยืนยันกับ HEAD ก่อนแก้**: จับได้ 10 เมธอด/11 จุด (POS 1 + Integration 6 + 3 ใน baseline) | ไฟล์ใหม่ |

**ไม่ได้แตะ (Q2)**: วงเงินอนุมัติ · SoD · Budget ของ POS/Integration · `EnforceFullTaxInvoiceFields` (S-07/Q4).

## S-05 `IsVehicleDealer` → §82/5(6) ตัวเดียว

**verify**: จริง — `ProhibitedInputVatScreener.Screen` ไม่มีพารามิเตอร์ · ด่านรถสองชุดในเมธอดเดียวกัน (`CollectApprovalWarningsAsync`) คำคนละลิสต์ ·
**ไม่จริง**: "`TaxService.cs:273` เคารพธง" — TaxService **อ่านธงแล้วไม่มีใครใช้** (ตัวตัด keyword ถูกถอดไปตามนโยบาย "ดุลพินิจผู้กรอก" ก่อนหน้านี้ — หมายเหตุ (c) ในลูป ภ.พ.30) ⇒ ถอดการอ่านที่ตายทิ้ง **ไม่ได้เพิ่มตัวตัดใน ภ.พ.30** (ตัวเลขรายงานไม่เปลี่ยน)

| ทำอะไร | ที่ไหน |
|---|---|
| **`Helpers/InputVatVehicleRule`** — ลิสต์คำ 4 ชุด (ย้ายจาก screener **ไม่แก้เนื้อ**) + `Judge(hay, vendor, isVehicleDealer)` → `VehicleVatVerdict` (NotVehicleCost · ClaimableVehicleType · DefaultNotClaimable · **VehicleDealerExempt**) · `Warning` · `DisablesClaim` · ตัวจับคำขอบคำ | ไฟล์ใหม่ |
| Screener: `Screen(..., bool isVehicleDealer)` (required — ห้าม default) · §82/5(4) ค่ารับรองไม่สนธง · dealer ⇒ `Claimable=null` (OCR ใส่ `[VAT-NOTE]` ไม่ใช่ `[VAT-CLAIM]`) + ข้อความ "รถยนต์นั่งที่ใช้เองยังต้องห้าม" | `Ocr/ProhibitedInputVatScreener.cs` |
| OCR ส่งธง | `OcrService.cs:1293` (บล็อก §82/5 — ไม่แตะส่วน payment allocation) |
| ด่านเตือนตอนอนุมัติ: ด่านบนส่งธง · **ถอดด่านรถชุดที่สอง** (ลิสต์ "น้ำมัน"/"ค่าซ่อม"/"อะไหล่"/"fuel" เดี่ยว ⇒ เคยเตือน ค่าซ่อมแอร์ · น้ำมันพืช · fuel surcharge) — ลิสต์คำรถเหลือ**ชุดเดียว** | `DocumentService.cs:16722` · ที่เดิม ~17123 เหลือคอมเมนต์ |
| TaxService: ถอดการอ่านธงที่ไม่มีผู้ใช้ | `TaxService.cs` ~266 |

ผลข้างเคียงที่ตั้งใจ (บอกไว้ให้ตัดสินได้): คำเตือนตอนอนุมัติของบริษัท**ไม่ใช่** dealer หายไปสำหรับใบที่แมตช์แค่ลิสต์ชุดที่สอง
(ส่วนใหญ่ false positive · แต่ "อะไหล่ Toyota" (ไม่มีคำว่า "รถ") ก็หายด้วย — ถ้าต้องการให้เพิ่มคำใน `InputVatVehicleRule` ชุดเดียวพร้อมเทสต์กระดาษจริง)

## S-10 (CMS เท่านั้น — ไม่แตะ Lodging)

- `CmsBookingService.cs:496` — `IsVatRegistered ? 7 : 0` → `OutputVatRate.ForCompany(IsVatRegistered, Company.VatRate)`
- `CmsLeadService.cs:181` — `VatRate: 7m` → `OutputVatRate.ForCompany(...)` ⇒ lead ของบริษัทไม่จด VAT ได้ใบเสนอราคา VAT 0 · อ่าน `Company` เหมือน CMS เดิม (ไม่เปลี่ยนว่าอ่านธงไหน — Q1)

## ของแถม — บันทึกภายใน lead หลุดถึงลูกค้า (verify: จริง)

`CmsLeadService.cs:196/210` — `Notes: lead.InternalNotes` (พิมพ์บนใบเสนอราคา) → `Notes: null` + หลังสร้างเติม `InternalNotes` ของเอกสาร (`[จาก lead LD-…] …` ต่อท้ายค่าเดิม)
(`CreateDocumentRequest` ไม่มีช่อง InternalNotes · ไม่ได้เพิ่ม เพื่อไม่แตะ DocumentService เกินจำเป็น)

## checker ใหม่ตัวที่สอง — `tools/company_settings_factory_check.py`

ฟ้อง `new CompanySettings {/(` · `new Models.Entities.CompanySettings(` · `CompanySettings x = new(` นอก `Helpers/CompanySettingsFactory.cs` (0 จุดวันนี้ · HEAD ก่อนแก้ = 6 จุด ยืนยันแล้ว) ·
`--self-test` จับ 3 รูป + ไม่ฟ้องคอมเมนต์/สตริง/`CompanySettingsResponse`/เจ้าของ

## เทสต์ (ยังไม่ได้รัน C# — ไม่มี SDK) — สองครึ่งทุกไฟล์

| ไฟล์ | พังกลับมาถูก | ถูกอยู่แล้วต้องไม่ถูกแตะ |
|---|---|---|
| `CompanyVatStatusTests` (ใหม่ 10 Fact + 2 Theory) | บริษัทไม่จด/ค่าเริ่มต้นเส้นสมัคร → แถวใหม่ไม่ติ๊กจด · ไม่มีแถว = ธงบริษัท · อัตราจากบริษัท · OutputVatRate ไม่จด = 0 | จด → ติ๊กจด 7% · มีแถว = ลำดับเดิม (4 กรณี) · ค่าอื่นของแถว = default entity · แถวใหม่ไม่ขัดกับบริษัทเสมอ · จำแนกทิศขัดกัน |
| `InputVatVehicleRuleTests` (ใหม่ 7 Fact + 2 Theory) | dealer: น้ำมัน/Diesel/อะไหล่รถ/ค่าซ่อมรถ ไม่ปิดเคลม + เตือนรถใช้เอง | ไม่ใช่ dealer: ปิดเคลมเหมือนเดิม · รถบรรทุกเปิดเคลม · PURE LIFE · ค่าซ่อมแอร์/น้ำมันพืช/fuel surcharge ไม่ใช่ค่ารถ · dealer ค่ารับรองยังต้องห้าม · แก๊สหุงต้ม |
| `EtaxAutoIssueScopeTests` (ใหม่ 7 Fact + 4 Theory) | ใบกำกับ POS/API · CN ฝั่งขาย · มัดจำที่รับรู้แล้ว ไม่ถูกข้าม · CN ไม่รู้ต้นทาง = ไม่ข้าม (ให้ดัง) | ชนิดไม่ใช่ใบกำกับ · ร่าง/รออนุมัติ/ปฏิเสธ · Voided · มัดจำพักรอ · ใบเสร็จรับชำระใบกำกับ · CN/DN ฝั่งซื้อ = ข้ามเงียบ |
| `ProhibitedInputVatScreenerTests` · `ProhibitedInputVatScopeTests` (แก้) | — | wrapper ส่ง `isVehicleDealer: false` — **ทุกเคสเดิมต้องผ่านเหมือนเดิม** (ลิสต์คำย้ายโดยไม่แก้เนื้อ) |

`python3 tools/test_inventory.py --row` → **298 ไฟล์ · 2,501 `[Fact]` + 385 `[Theory]` (1,725 `InlineData`)** (วางทับ TEST_PLAN §0 ตอน merge — รวมของทีมอื่นที่ merge ก่อนแล้วนับใหม่)

## F3 ข้อ 7–12 (สรุป — ฉบับเต็มในข้อความคอมมิต)

7. `?? true` บนธง VAT 6 → **0** · `new CompanySettings` 6 → **0** (+ checker) · `TryAutoGenerateEtaxAsync` → **0** (ย้ายเป็น hook) · ลิสต์คำรถ 2 → **1** · VAT 7 ตายตัวใน CMS booking/lead 2 → **0** (เหลือ `CmsCommerceService.cs:~1171` `Product.VatRate ?? 7m` ค่าเริ่มต้นรายสินค้า — นอกขอบเขต S-10) · จุดประทับ Approved นอกเจ้าของที่ไม่เรียก hook 10 เมธอด → **3** (baseline มีเหตุผล)
8. ทางเข้าสร้างบริษัท 3/3 · lazy 6/6 · ผู้อ่านธงฝั่งค่าตั้ง 6/6 · e-Tax: เว็บ/POS/Integration×6/CMS · §82/5(6): OCR + ด่านอนุมัติ (TaxService ไม่มีตัวตัดโดยนโยบาย) · **ยังไม่เดิน**: CN คืนมัดจำใน `DocumentService.cs:~3864` (ประทับ Approved เอง — ส่วนมัดจำที่ห้ามแตะ) · LINE/มือถือ อนุมัติผ่าน ApproveDocumentAsync อยู่แล้ว
9. เข้มขึ้น: บริษัท**ไม่มีแถวค่าตั้ง**และธงบริษัท = ไม่จด → ด่าน §90/2 บล็อกใบกำกับ/API ปฏิเสธบรรทัด VAT (เดิมผ่าน) — ทางไปต่อ: ติ๊ก "จด VAT" ที่หน้าบริษัท/ตั้งค่า (ข้อความ §90/2 เดิมบอกอยู่แล้ว) · เทสต์ทิศตรงข้าม `บริษัทจด_VAT_แถวค่าตั้งใหม่ยังติ๊กจดเหมือนเดิม` · `มีแถวค่าตั้ง_ลำดับเดิมไม่เปลี่ยน` · ในทางปฏิบัติแทบทุกบริษัทมีแถวแล้ว (layout โหลด `/settings` ทุกหน้า) · e-Tax จาก POS/API ล้ม = ป้ายบนเอกสาร ไม่ทำให้ขายล้ม
10. ค่าที่ persist ผิดแล้ว (แถวค่าตั้ง `true` ของบริษัทที่ไม่จด) — **ไม่ migrate** ตามคำตัดสินข้อ 14 → รายงาน `vat-flag-consistency` ให้คนตัดสิน · ใบ POS/API ที่ออกก่อนรอบนี้โดยไม่มี e-Tax — ไม่ย้อนออกให้อัตโนมัติ (ดู "สิ่งที่เหลือ")
11. ไม่มีเครื่องมือเรียก subagent ในบทบาทนี้ ⇒ ทำรอบฝ่ายค้านเองด้วย 3 คำถาม (ทางเข้าอื่น · ทิศตรงข้าม · สถานะประทับเอง — hook ไม่ประทับสถานะ ป้ายเป็นหมายเหตุ ไม่ใช่สถานะปลายทาง) · **main agent ควรส่ง diff ให้ฝ่ายค้านอีกรอบ** (แตะภาษี)
12. ต้องขยับ — ดูข้างล่าง

## เอกสารที่ main agent ต้องขยับ (ทีมนี้ไม่แตะ)

- **DOCUMENT_FLOW.md**: (1) ขั้น e-Tax อัตโนมัติของ `ApproveDocumentAsync` → `IssuedDocumentHooks.RunAsync` + ขอบเขต `EtaxAutoIssueScope` (ใบที่ข้ามโดยเจตนา 3 แบบไม่ติดป้ายแล้ว · ป้ายถูก save ทันที)
  (2) ทางเข้า POS ใบกำกับเต็มรูป · Integration TIV/CN/DN(+Expense/PV/CIL) · CMS → ได้ e-Tax อัตโนมัติแล้ว (หลัง commit · ล้มไม่ย้อนใบ) · Import ยอดยกมา/SampleData/PO ข้ามบริษัท = ไม่ออกโดยเจตนา
  (3) §90/2 + ภาษีซื้อ + แปลงเอกสาร + Integration: ไม่มีแถวค่าตั้ง = ธงบริษัท (เดิม "ถือว่าจด") · ธงสองตัวยังอยู่ ลำดับ "ค่าตั้งก่อน" รอ Q1
  (4) §82/5(6): ลิสต์คำชุดเดียว `InputVatVehicleRule` + ธง `IsVehicleDealer` ถึง OCR และด่านอนุมัติ · ด่านชุดที่สองถูกถอด (5) CMS lead: VAT ตาม `OutputVatRate` · บันทึกภายในไป `InternalNotes`
- **ACCOUNT_STRUCTURE.md**: Company ↔ CompanySettings — แถวค่าตั้งเกิดพร้อมบริษัททุกทาง (สร้าง · สมัคร · SSO) ผ่าน `CompanySettingsFactory` · endpoint ใหม่ `GET /api/companies/{id}/vat-flag-consistency` (✅) และ `GET /api/admin/vat-flag-consistency` (✅ แอดมิน)
- **TEST_PLAN.md**: §0 ตามแถวข้างบน · เคส S-01 (สร้างบริษัทไม่จด → ตั้งค่าไม่ติ๊ก/บันทึกไม่พลิก · ไม่มีแถว → §90/2 บล็อก) · S-02 (ใบกำกับ POS/API ได้ e-Tax เมื่อเปิด · ปิด → ไม่มีทุกทาง · มัดจำพักรอ/ใบเสร็จรับชำระ ไม่ติดป้าย) · S-05 (dealer เคลมได้ · ไม่ใช่ dealer เดิม) · S-10 (lead บริษัทไม่จด VAT 0)
- **CLAUDE.md** หมวด F: เพิ่มบรรทัด `python3 tools/approved_status_writer_check.py` และ `python3 tools/company_settings_factory_check.py` (check_all กวาดเองอยู่แล้ว)
- **CHANGELOG.md**: รอบ 193 ทีม V
- `audit-settings.md`: ติ๊ก S-01 (stopgap) · S-02 (e-Tax) · S-05 · S-10 (CMS) · ของแถม — และจด "ตรวจแล้วไม่จริง" 3 ข้อ (Import · CMS SignDigitally · TaxService เคารพธง)

## สิ่งที่ยังเหลือ / ต้องถามเจ้าของ

1. **Q1** ธงไหนเป็นต้นทาง — แก้ที่ `CompanyVatStatus.IsRegistered` ที่เดียว (TODO ในโค้ด) · และ POS/CMS/ที่พัก/TimeBilling/ปฏิทินยังอ่าน `Company` ตรง (ไม่ได้ย้ายมา helper เพราะจะเปลี่ยนพฤติกรรมเมื่อธงขัดกัน = เลือกต้นทางแทนเจ้าของ)
2. **Q2** วงเงิน/SoD/Budget ของ POS/Integration — ไม่ได้แตะ
3. **ถามเจ้าของเพิ่ม**: ระบบต้นทาง (TakeTime ฯลฯ) ออก e-Tax เองอยู่แล้วหรือไม่ — ถ้าใช่ ใบจาก Integration จะถูกออกซ้ำเมื่อบริษัทเปิด e-Tax อัตโนมัติ (Q2 ข้อเสนอ (ก) บอก "e-Tax ต้องออกทุกทาง" จึงทำตามนั้น)
4. ใบกำกับจาก POS/Integration ที่ออก**ก่อน**รอบนี้ในบริษัทที่เปิด e-Tax — ยังไม่มี e-Tax และไม่มีรายงานไล่ (ควรมีรายงานอ่านอย่างเดียวแบบเดียวกัน: ใบกำกับ Approved+ ที่ไม่มี `EtaxInvoice` ในบริษัทที่ `EtaxEnabled`)
5. CN คืนมัดจำ (`DocumentService.cs:~3864`) ประทับ Approved เองโดยไม่เรียก hook — อยู่ในส่วนมัดจำที่ทีมอื่นถือ (checker ไม่ตรวจไฟล์เจ้าของ) ควรต่อสายในรอบที่ส่วนนั้นว่าง
6. หน้าเว็บของรายงาน `vat-flag-consistency` (ตอนนี้ endpoint อย่างเดียว) · และคำเตือนบนหน้าตั้งค่าเมื่อธงขัดกัน
7. S-10 ส่วนที่พัก (`LodgingService.cs:71,74`) — ทีม L2 ถืออยู่

---

## หลังฝ่ายค้าน (review193-V-C3.md) — รอบแก้ต่อ

merge `claude/erp-system-review-team-660mev` ก่อน · verify ทุกข้อจากโค้ดตรงจุด — **ทุกข้อจริง** (รายละเอียดเดียวที่ปรับ: C-2 กับ CN/DN ใช้ธง
`IsTaxInvoiceByLaw` ไม่ได้ เพราะธงตรึงจากหัว "ใบลดหนี้" = false ทั้งที่ §86/9-10 ถือเป็นใบกำกับ ⇒ จำกัดกติกาไว้ที่ใบกำกับ/ใบเสร็จ)

| ข้อ | ทำอะไร | ที่ไหน |
|---|---|---|
| C-1 | `EtaxAutoSkip.NotFullTaxInvoice` (walk-in · ไม่ประสงค์รับ · ผู้ซื้อ §86/4 ไม่ครบ) ข้ามเงียบ · เกณฑ์ `TaxService.NotFullTaxInvoice` ตัวเดียว (แยกแกน `(vat, declined, contact)` ให้ hook ใช้ได้เมื่อไม่มี navigation) · เฉพาะใบกำกับ/ใบเสร็จ | `Helpers/EtaxAutoIssueScope.cs` · `IssuedDocumentHooks.cs` · `TaxService.cs` |
| C-2 | `EtaxAutoSkip.NotTaxInvoiceByLaw` (null = ใบเก่า ไม่ตีความ) · `GenerateAsync` ปฏิเสธด้วย + ด่าน "ไม่ใช่ใบกำกับเต็มรูป" ขยายจากใบเสร็จไปใบกำกับ | `EtaxInvoiceService.cs` ~160-190 |
| C-3 | ใบเสร็จ settlement (ใบกำกับ ณ วันรับเงิน §78/1) เรียก hook หลัง commit ในผู้เรียกทั้งสองของ `CreateSettlementReceiptAsync` (`IssueReceiptForPaymentAsync` · `CreatePaymentAsync`) | `DocumentService.cs` ~10999 · ~11036/11343/11407 |
| C-3 checker | เขียน `approved_status_writer_check` ใหม่: `new Document { Status = … }` ที่เกิดมาออกแล้ว (รวม Paid · ternary ข้ามบรรทัด · ตัวแปร = ถือว่าออก) + `x.Status = DocumentStatus.Approved;` · **ตรวจ DocumentService ด้วย** · ไม่ฟ้อง ternary คำนวณสถานะคืนบนเอกสารเดิม · ผ่านเมื่อเมธอดหรือผู้เรียกทุกตัวของตัวช่วยเรียก hook · self-test 5 จับ/5 ไม่ฟ้อง · กับโค้ดก่อนแก้จับ `CreateSettlementReceiptAsync` + `RefundDepositAsync` ได้ | `tools/approved_status_writer_check.py` |
| baseline | +1 แถว `RefundDepositAsync` (CN คืนมัดจำ — ช่องที่มีก่อนรอบนี้ checker เพิ่งมองเห็น · ส่วนมัดจำทีม L2 ถือ) พร้อมเหตุผล | `tools/approved_status_writer_baseline.txt` |
| C-4 | `GET /api/companies/{id}/input-vat/screen?lines=…&vendorName=…` → `ProhibitedInputVatScreener` ตัวเดียว (รู้จัก dealer) คืน `{claimable, ruleCode, warning, vehicle}` · `suggestVatClaim` ถามเซิร์ฟเวอร์ (debounce · seq กันผลตอบช้า · userOverride ชนะ · ปิดเคลมเฉพาะ `claimable=false`) · `toggleVatClaim` ถามเซิร์ฟเวอร์แทน `vehicleKw/truckKw` (dealer ไม่ถูกถาม) · **ลิสต์คำรถใน JS = 0** | `Controllers/InputVatScreenController.cs` · `documents.html` |
| C-5 | ชั้น `VehicleVatVerdict.UnclearCheck` (**เตือน ไม่ปิดเคลม**) ใน `InputVatVehicleRule` ตัวเดียว: ยี่ห้อรถ/คำว่ารถยนต์ + งานรถ · ผู้ขายปั๊ม/บัตรน้ำมันทั่วไป · "fuel" ที่ไม่ตามด้วย surcharge/adjustment/levy/charge · ยกเว้นมอเตอร์ไซค์/เครื่องปั่นไฟ · ไม่ใส่ "mg"/"service" | `Helpers/InputVatVehicleRule.cs` |
| C-9 | คอลัมน์ `CompanySettings.VatStatusConfirmedAt` (migration เติมแถวเดิมครั้งเดียวแล้ว `DROP DEFAULT`) · `NewFor(company, vatStatusConfirmed)` บังคับส่ง (วิซาร์ด=ยืนยัน · สมัคร/SSO/lazy=ยังไม่) · ยืนยันเมื่อบันทึกช่อง VAT หน้าตั้งค่า/บริษัท · `/settings` ส่ง `vatStatusConfirmed` · `layout.js` แถบ "ยังไม่ได้ระบุว่าบริษัทจด VAT หรือไม่ — ตั้งค่า" บน documents/dashboard · ค่าเริ่มต้นยังเป็น "ไม่จด" | `CompanySettings.cs` · `DatabaseMigrationHelper.cs` · `CompanySettingsFactory.cs` · `SettingsService.cs` · `CompanyService.cs` · `AuthService.cs` · `layout.js` |
| P-8 | `settings.html` ถอด `checked` ของ "จด VAT" · hydrate `=== true` · ส่งช่อง VAT เฉพาะเมื่อโหลดค่าจริงสำเร็จ | `settings.html` |
| ออกซ้ำพร้อมกัน | `GenerateAsync` `SELECT … FOR UPDATE` แถวเอกสารในธุรกรรม + ตรวจซ้ำ · hook ที่ล้มเพราะอีกตัวออกก่อน = ไม่ถือว่าล้ม | `EtaxInvoiceService.cs` · `IssuedDocumentHooks.cs` |
| save ทั้ง context | `StampFailureAsync` ใช้ `ExecuteUpdateAsync` แถวเดียว (อ่านค่าล่าสุดก่อนต่อท้าย · ตั้ง OriginalValue กันเขียนซ้ำ) | `IssuedDocumentHooks.cs` |
| ป้ายไม่มีผู้อ่าน/ไม่ถูกล้าง | `Helpers/EtaxAutoFailedNote` (Append แทนป้ายเดิม · Has · Clear เฉพาะย่อหน้าป้าย) · `DocumentResponse.EtaxAutoFailed` · แถบแดงหน้าเอกสาร · ล้างเมื่อ `GenerateAsync` สำเร็จ · hook คืน `IssuedDocumentHookResult` → Integration TIV/CN/DN ต่อข้อความเตือนในคำตอบ = ดังครบ 3 ที่ | `EtaxAutoFailedNote.cs` · `DocumentDtos.cs` · `documents.html` · `IntegrationService.cs` |
| P-6/P-7 | CMS lead/booking/commerce อ่าน VAT ผ่าน `CompanyVatStatus` ตัวเดียวกับ §90/2 | `CmsLeadService.cs` · `CmsBookingService.cs` · `CmsCommerceService.cs` |

เทสต์ (สองทิศ · ยังไม่ได้รัน C#): `EtaxAutoIssueScopeTests` +2F/+3T · `InputVatVehicleRuleTests` +2F/+2T (5 แบบของฝ่ายค้าน = UnclearCheck ·
ค่าซ่อมแอร์/น้ำมันพืช/fuel surcharge/Fuel Adjustment/service charge/เครื่องปั่นไฟ Honda/10 mg = เงียบ) · `EtaxAutoFailedNoteTests` ใหม่ 4F/1T ·
`CompanyVatStatusTests` +2F · จำลอง `InputVatVehicleRule` ด้วย Python ที่อ่านลิสต์จากไฟล์ C# จริง: 14/14 ตรง และเคสเดิมไม่เปลี่ยน ·
`check_all`: checker 49 ✅ · sim 5 ✅ · ❌ เดียว TEST_PLAN §0 (ห้ามแตะ)

ยังเหลือ: CN คืนมัดจำ (L2 · baseline) · JS ยังมีกติกา §82/5 ที่ไม่ใช่รถ 2 อย่าง (ค่ารับรองแบบกว้าง · บิลเงินสด) — ย้ายเข้าเซิร์ฟเวอร์ต้องมีเทสต์กระดาษจริง
เพราะเปลี่ยนผล OCR ด้วย · ป้าย `[ETAX-AUTO-FAILED]` ที่ติดใบ walk-in/by-law-false ไปแล้วก่อนรอบนี้ยังค้าง (ไม่แก้หลังบ้าน — ล้างเองเมื่อออก e-Tax สำเร็จ ·
เสนอรายงานอ่านอย่างเดียวไล่) · ช่องจด VAT ฝั่งหน้าข้อมูลบริษัทยังไม่มี guard โหลดล้ม (เสี่ยงต่ำ) · P-1 รอเจ้าของ · diff รอบนี้แตะภาษี ควรส่งฝ่ายค้านอีกรอบ

---

## หลังฝ่ายค้านรอบสอง (review193-r2-sec-tax.md)

merge `claude/erp-system-review-team-660mev` ก่อน · verify ทุกข้อจากโค้ดตรงจุด — **ทุกข้อจริง** · รายละเอียดที่พบเพิ่มระหว่าง verify:
R2-C7 — `ComputeDocumentTitle` ของ TaxInvoice ที่ VAT = 0 และผู้ซื้อไม่ใช่ walk-in/ไม่ปฏิเสธ คงหัว "ใบกำกับภาษี" (ไม่ดูอัตรา) ⇒ ใบ 0% จาก API
**กระดาษเป็นใบกำกับแต่ธงบอกไม่ใช่** (คอมเมนต์เดิมใน `IntegrationService` ที่ว่า "VAT=0 หัวพิมพ์ใบเสร็จ" จริงเฉพาะ walk-in — แก้คอมเมนต์แล้ว)

| ข้อ | ทำอะไร | ที่ไหน |
|---|---|---|
| R2-C6 | แยก "ข้ามโดยเจตนา" ออกจาก "ข้อมูลไม่ครบ": `TaxService.NotFullTaxInvoiceByDesign` = ไม่ใช่เต็มรูป **และ** (ผู้ซื้อไม่ประสงค์รับ · walk-in · ผู้ซื้อบุคคลธรรมดา) · hook ใช้ตัวนี้ ⇒ นิติบุคคลที่ §86/4 ไม่ครบ (และใบที่ไม่มีผู้ติดต่อ) ไม่ถูกข้ามเงียบอีก — ไป `GenerateAsync` ซึ่งปฏิเสธพร้อมชื่อช่องที่ขาด ⇒ ป้าย `[ETAX-AUTO-FAILED]` บนเอกสาร + ข้อความใน response ของ Integration (`EtaxHookSuffix`) | `TaxService.cs` · `IssuedDocumentHooks.cs` · `EtaxAutoIssueScope.cs` (doc) · `EtaxInvoiceService.cs` (คอมเมนต์) |
| R2-C7 | ตัวตัดสินตัวเดียว `TaxInvoiceSeriesPolicy.CarriesTaxInvoiceRole(doc, title, companyVatRegistered = false)` เพิ่มพื้นข้อสอง `IsZeroRatedFullTaxInvoice`: TaxInvoice · VAT 0 · มีบรรทัด (ไม่ถูกลบ) อัตรา **0** อย่างน้อยหนึ่ง · ผู้ซื้อไม่ปฏิเสธ/ไม่ใช่ walk-in (ตรงกับหัวกระดาษ) · ใช้เมื่อผู้เรียกยืนยันว่าจด VAT · ยกเว้น §81 ล้วน (`-1`) = ไม่ใช่ · Integration ส่ง `Lines`+`Contact`+`vatRegistered` เข้า probe ⇒ ใบ 0% ได้ธง true + เลขชุด TIV · POS ไม่ส่ง = พฤติกรรมเดิม · เส้นเว็บตัดสินจากหัวอยู่แล้ว | `Helpers/TaxInvoiceSeriesPolicy.cs` · `IntegrationService.cs` ~966-980 |
| R2-C7 ใบเดิม | **ไม่แก้หลังบ้าน** — รายงานอ่านอย่างเดียว `GET /api/companies/{id}/vat-flag-consistency/zero-rated-tax-invoices` (สิทธิ์ `CompanySettings.Edit` · กรอง `CompanyId` · เกณฑ์เดียวกับตัวตรึงธง · ≤200 ใบ + ธง truncated · บริษัทไม่จด = ไม่ค้น) (กรอง DB หยาบแล้วตัดสินรายใบด้วย `IsZeroRatedFullTaxInvoice` ตัวเดียวกัน — ไม่มีเกณฑ์สำเนาเป็น LINQ) | `Controllers/VatFlagConsistencyController.cs` |
| R2-C8 | ประทับ `VatStatusConfirmedAt` เฉพาะเมื่อ payload มีช่อง VAT **และ** `confirmVatStatus: true` (ช่องใหม่ท้าย `UpdateCompanySettingsRequest`/`UpdateCompanyRequest`) · หน้า: ส่งช่อง VAT + ธงยืนยันเฉพาะเมื่อผู้ใช้แตะช่องจด VAT (onchange ของช่องใดช่องหนึ่ง — สองช่องสะท้อนกัน) หรือกดปุ่มใหม่ "ยืนยันสถานะ VAT ตามที่เลือกอยู่" (โชว์เฉพาะ `vatStatusConfirmed === false` ทั้งสองแท็บ · ซ่อนหลังบันทึกสำเร็จ) · บันทึกเรื่องอื่นไม่แตะธง VAT และไม่ประทับ | `SettingsService.cs` · `CompanyService.cs` · `SettingsDtos.cs` · `CompanyDtos.cs` · `settings.html` |
| P-8 (แท็บบริษัท) | hydrate `c.isVatRegistered === true` · `_companyVatLoaded`/`_companyLoaded` — โหลดล้ม = ไม่ส่ง `isVatRegistered`/`vatRate` · `isVehicleDealer` ส่งเฉพาะเมื่อโหลดค่าตั้งได้ (`_vehicleDealerLoaded`) | `settings.html` |
| C-4 ส่วนที่เหลือ | ย้าย regex ค่ารับรอง/บิลเงินสดออกจาก JS → `Helpers/ManualInputVatLineRule` (เรียกจาก `InputVatScreenController` **หลัง**ตัวคัดกรองกลาง · **ไม่ใช้กับเส้น OCR** ⇒ ผล OCR ไม่เปลี่ยนโดยโครงสร้าง จึงไม่ต้องรันชุดกระดาษก่อน-หลัง — เหตุที่ไม่รวมเข้า `ProhibitedInputVatScreener`: ตัวนั้นกินข้อความทั้งหน้าสแกน คำอย่าง "กอล์ฟ"/"กระเช้า"/"บิลเงินสด" ในโฆษณาท้ายใบจะปิดเคลมทั้งใบ = เปลี่ยนผล OCR โดยไม่มีกระดาษยืนยัน) · แคบลงจาก regex เดิม: ไม่มี "รับรอง"/"เลี้ยง" เดี่ยว (เดิมปิดเคลม "หนังสือรับรองบริษัท" · "ตรวจรับรอง ISO" · "อาหารเลี้ยงสัตว์" · "เลี้ยงพนักงาน") · **กติกา §82/5 ใน JS ของฟอร์ม = 0** | `Helpers/ManualInputVatLineRule.cs` · `InputVatScreenController.cs` · `documents.html` |
| autoSrc | ปิดเคลมอัตโนมัติติดป้าย `dataset.autoSrc='screen'` · คำอธิบายเปลี่ยนจนไม่เข้าข่าย/ลบคำอธิบาย ⇒ คืนเคลมเฉพาะที่ระบบปิดเอง (ไม่ค้าง) · บรรทัด 0%/ยกเว้น ล้างแค่เหตุผล · ผู้ใช้กดไอคอน/ติ๊ก "มีใบกำกับ" ⇒ ลบ autoSrc (ผู้ใช้ชนะ) · regex "เหตุผลถาวร" รู้จักรหัส `RD-82/5(n)` ของเซิร์ฟเวอร์ด้วย (เดิมเช็กแค่ `§82/5(n)` ของ regex ที่ถอดไปแล้ว — ค่ารับรอง/รถรอดเพราะคำไทยในเหตุผลเท่านั้น) | `documents.html` |
| 414 | endpoint คัดกรองเป็น `POST` body `{lines, vendorName}` (อ่านอย่างเดียว · คอนโทรลเลอร์ไม่อยู่ใน WATCHED ของ `write_permission_gate_check`) · ทั้ง `suggestVatClaim` และ `toggleVatClaim` | `InputVatScreenController.cs` · `documents.html` |
| R2-C12 | `approved_status_writer_check` กติกาที่สอง: การเรียก hook ที่มี `BeginTransactionAsync` (บล็อกยังครอบ hook) ก่อนหน้า ต้องมี `CommitAsync` บน**เส้นเดียวกับ** hook ระหว่างนั้น — commit ในทางออกก่อนกำหนด (`if (…) { commit; return; }`) ไม่นับ · ออกจาก try/using/lambda ได้ · ไม่มี baseline · self-test +3 จับ (Pay · Twice ธุรกรรมที่สอง · EarlyReturn) / +3 ไม่ฟ้อง (หลัง commit · ไม่มีธุรกรรม · commit ใน try) · **negative test กับโค้ดจริง**: สลับ hook ขึ้นก่อน commit ใน `IssueReceiptForPaymentAsync`/`CreatePaymentAsync` ⇒ จับได้ทั้งคู่ · โค้ดปัจจุบัน 0 จุด (รุ่นแรกของกติกาที่นับ commit ใดก็ได้ **ไม่จับ** เคสจริงเพราะ early-return commit — จึงเพิ่มการดูเส้นทาง) | `tools/approved_status_writer_check.py` |

เทสต์ใหม่ (ยังไม่ได้รัน C# — ไม่มี SDK · สองครึ่งทุกหัวข้อ): `EtaxSkipIntentAndZeroRatedTests` 18F (R2-C6 8 · R2-C7 10) ·
`ManualInputVatLineRuleTests` 3T/15 InlineData (คำเดิมที่ต้องยังจับ + คำที่ regex เดิมจับผิดต้องไม่แตะ)

F3 7–12 (ย่อ): 7) `CarriesTaxInvoiceRole` ผู้เรียก 3 (POS ไม่ส่ง = เดิม · Integration ส่ง · Approve ใช้หัว) · `NotFullTaxInvoice` เดิมยังใช้ใน `GenerateAsync`/PDF ·
hook ใช้ ByDesign 1 จุด · ผู้ประทับ `VatStatusConfirmedAt` 2 จุด (+ factory) ทั้งคู่ต้องมีธง · `input-vat/screen` GET เหลือ 0 จุด · regex §82/5 ใน JS ของฟอร์ม 0 ·
8) ทางเข้าอื่น: POS ไม่ส่งสถานะจด (POS ไม่ขาย 0%) · CMS ไม่ผ่าน `CarriesTaxInvoiceRole` · เส้นเว็บตัดสินจากหัว (0% = หัวใบกำกับอยู่แล้ว) · API/มือถือที่ส่ง
`vatRegistered` โดยไม่มีธงยืนยัน = บันทึกค่าได้แต่ไม่ประทับ (ทิศปลอดภัย: แถบเตือนยังอยู่) · 9) เข้มขึ้น: นิติบุคคล §86/4 ไม่ครบได้ป้ายล้ม e-Tax
(ทางไปต่อ: เติมข้อมูลผู้ซื้อแล้วกดออก e-Tax — ป้ายถูกล้างเมื่อสำเร็จ) เทสต์ทิศตรงข้าม `ผู้ซื้อไม่ประสงค์รับ_ข้ามโดยเจตนา` ·
`ลูกค้าเงินสด_walk_in_ข้ามโดยเจตนา` · `บุคคลธรรมดาข้อมูลไม่ครบ_ข้ามโดยเจตนา_หัวเป็นใบย่อ` · ใบยกเว้นล้วนไม่ถูกดึง `ยกเว้น_81_ล้วน_ไม่ใช่ใบกำกับ` ·
10) persisted: ใบ 0% เดิมที่ธง false → รายงานอ่านอย่างเดียว (ไม่แก้หลังบ้าน) · `VatStatusConfirmedAt` ที่ถูกประทับจากการบันทึกเรื่องอื่นก่อนแก้ — แยกไม่ได้ว่า
ผู้ใช้ตอบจริงหรือไม่ (ไม่มีประวัติ) ⇒ **ไม่ล้าง** (ล้าง = แถบเตือนกลับมาหาคนที่ตอบแล้ว) · จดไว้ให้เจ้าของตัดสิน · 11) แตะภาษี → ควรส่งฝ่ายค้านรอบสาม ·
12) DOCUMENT_FLOW (ขอบเขต e-Tax อัตโนมัติ · ธง by-law ของ 0%) + TEST_PLAN §0 ต้องขยับ — main agent (ทีมนี้ห้ามแตะ)

ยังเหลือ / ต้องถามเจ้าของ:
1. หัวกระดาษของ TaxInvoice ที่ **ยกเว้น §81 ล้วน** ยังพิมพ์ "ใบกำกับภาษี" (`ComputeDocumentTitle` ไม่ดูอัตรา) ขณะที่ธงของ Integration = ไม่ใช่ใบกำกับ ⇒ กระดาษกับธงยังขัดกันในเคสนี้ (กฎ #2 D: Exempt ห้ามออกใบกำกับ) — แก้ต้องแตะ renderer ×2 + ด่านอนุมัติ ไม่ได้ทำรอบนี้
2. `VatStatusConfirmedAt` ของบริษัทที่ถูกประทับจากการบันทึกเรื่องอื่นระหว่าง 7601891 → รอบนี้ (ข้อ 10 ข้างบน)
3. หน้าเว็บของรายงาน `zero-rated-tax-invoices` / `vat-flag-consistency` (ยัง endpoint อย่างเดียว)

`check_all` (รอบสอง): checker ทุกตัว ✅ · sim 5 ✅ · brace/node/U+FFFD 19 ไฟล์ ✅ · ❌ เดียว TEST_PLAN §0 (ห้ามแตะ) — แถวใหม่ให้ main agent วางทับ:
`**326 ไฟล์ · 2,802 [Fact] + 434 [Theory] (1,888 InlineData)** ณ 2026-09-24` · ยังไม่ได้คอมไพล์ (ไม่มี dotnet SDK)

---

## หลังฝ่ายค้านรอบสาม (review193-r3.md)

merge `claude/erp-system-review-team-660mev` ก่อน · verify จากโค้ดตรงจุด — R3-4 จริง · B6 จริง **และพบเพิ่ม 1 ข้อที่หนักกว่า**:
`DocumentLineVatConvention.SplitLine` (ตัวคำนวณกลางของ integration ทั้งฝั่งขาย/ซื้อ) กับอัตรา `-1` ไหลลงสูตร `net × rate / 100`
⇒ **VAT ติดลบ 1% ของยอด** — ถ้าเขียนสัญญาให้คู่ค้าส่ง `-1` สำหรับของยกเว้นโดยไม่แก้ตรงนี้ = สั่งให้คู่ค้าสร้าง VAT ติดลบ

| ข้อ | ทำอะไร | ที่ไหน |
|---|---|---|
| R3-4 | baseline จาก **`git show 7601891:Accounting/wwwroot/pages/documents.html`** (คอมมิตสุดท้ายที่ JS ตัดสินเอง · บรรทัด 7816-7817) ไม่ใช่ความจำ · เปลี่ยนรูปกติกาจาก "รายการคำบวกแคบ ๆ" เป็น "**ความกว้างของ regex เดิม ลบบริบทที่ระบุชื่อ**": "รับรอง" ที่เหลือหลังตัดบริบทออกหนังสือ/ตรวจรับรอง (หนังสือรับรอง · ใบรับรอง · ตรวจรับรอง · รับรองมาตรฐาน/คุณภาพ/ระบบ/สำเนา/เอกสาร/ลายมือชื่อ/การแปล/งบ) = ค่ารับรอง · "เลี้ยง" ที่เหลือหลังตัดบริบทสัตว์/เพาะเชื้อ/เบี้ยเลี้ยง/เลี้ยงดู = ค่ารับรอง ยกเว้นบรรทัดที่มีคำว่าพนักงาน/ลูกจ้าง/บุคลากร **และไม่มี** ลูกค้า/แขก/คู่ค้า · คำที่เป็นค่ารับรองเสมอ: กระเช้า · ของขวัญลูกค้า · กอล์ฟ · พาลูกค้า | `Helpers/ManualInputVatLineRule.cs` |
| R3-4 เทสต์ | regex เดิมสองตัว**คัดลอกตรงตัวจาก git** ลงเทสต์ · สามกลุ่ม: (1) ค่ารับรอง 18 ข้อความรวม 4 ข้อความของฝ่ายค้าน — ยืนยันว่า baseline ปิดจริง **และ** ใหม่ยังปิด · บิลเงินสด 2 · (2) ตั้งใจปล่อย 10 (หนังสือรับรองบริษัท · ตรวจรับรอง ISO · ใบรับรองแพทย์ · รับรองสำเนา · รับรองงบการเงิน · อาหารเลี้ยงสัตว์/ปลา · เลี้ยงสังสรรค์พนักงาน · งานเลี้ยงปีใหม่พนักงาน · เบี้ยเลี้ยงพนักงาน) — ยืนยันว่า baseline ปิด (= เปลี่ยนโดยเจตนา) และใหม่ไม่ปิด · (3) คำทั่วไป 4 · จำลองด้วย Python ที่อ่าน regex จาก `git show` + ลิสต์จากไฟล์ C# จริง: 33/33 ตรงตามที่ตั้งใจ | `Accounting.Tests/ManualInputVatLineRuleTests.cs` |
| B6 (โค้ดที่ต้องแก้ก่อนเขียนสัญญา) | `SplitLine`: อัตรา ≤ 0 → VAT 0 (ยอด VAT ที่คู่ค้าคำนวณมาเองยังชนะ) · เทสต์ −1/0 × รวม/ไม่รวม VAT + 7% ไม่ถูกแตะ + explicit VAT | `Helpers/DocumentLineVatConvention.cs` · `IntegrationVatSplitTests.cs` |
| B6 สัญญา API | เขียนใน doc ส่งมอบคู่ค้าที่มีอยู่ `INTEGRATION_RESYNC.md` §11 (ตาราง null/7/0/−1 · **ห้ามส่ง 0 แทนยกเว้น** · ใบเดิม → รายงาน zero-rated) + คอมเมนต์สัญญาบน `InboundInvoiceLineRequest.VatRate`/`InboundInvoiceRequest.VatRate` · **ไม่แตะ ACCOUNT_STRUCTURE** | `INTEGRATION_RESYNC.md` · `Models/DTOs/Integration/IntegrationDtos.cs` |

ข้อเสนอ ACCOUNT_STRUCTURE (main agent/เจ้าของ — ทีมนี้ห้ามแตะ): ในส่วน Connected API/Integration เพิ่มบรรทัด "อัตรา VAT ต่อบรรทัด:
`0` = อัตราศูนย์ §80/1 (ใบกำกับอัตรา 0) · `-1` = ยกเว้น §81 (ไม่ใช่ใบกำกับ) — รายละเอียด `INTEGRATION_RESYNC.md` §11"

B6 ยังเป็นคำถามเจ้าของ (ไม่ได้ตัดสินแทน): เอกสารบอกให้คู่ค้าส่ง −1 แต่**คู่ค้าที่ต่อไว้แล้ว**และส่ง 0 ในความหมาย "ยกเว้น" จะได้ใบกำกับ 0%
จนกว่าจะแก้ต้นทาง · ทางเลือกที่เข้มกว่า (รอเจ้าของ): ให้ใบ 0% ต้องมีธงส่งออก/ต่างประเทศใน payload ก่อนนับเป็นใบกำกับ

F3 7–12 (ย่อ): 7) `ManualInputVatLineRule.Judge` ผู้เรียก 1 (`InputVatScreenController`) · regex ค่ารับรองใน JS ของฟอร์ม 0 ·
`SplitLine` ผู้เรียก 2 (Integration ขาย `:827` · ซื้อ `:2051`) ทุกเส้น integration ที่คิด VAT จากอัตราผ่านตัวนี้ (grep สูตร `Rate / 100` อื่น = 0 จุด นอกจาก WHT) ·
8) OCR ไม่ผ่าน `ManualInputVatLineRule` (ตั้งใจ — ตัวคัดกรองกลางยังเป็นชุดเดิม) · ฝั่งเว็บคีย์มือคิด VAT ใน `DocumentService.ComputeLineAmounts` (ไม่ใช่ตัวนี้) ·
9) เข้มขึ้น: 4 ข้อความของฝ่ายค้านกลับไปปิดเคลม — ผู้ใช้กดไอคอนเปิดเคลมคืนได้ (ค่าผู้ใช้ชนะ) · ทิศตรงข้าม `คำที่_baseline_ปิดผิด_ตั้งใจปล่อย` · `อัตรา_7_ไม่ถูกแตะโดยด่านอัตราศูนย์` ·
10) ใบที่ออกผ่าน API ด้วย −1 ก่อนแก้มี VAT ติดลบอยู่ใน DB — **ไม่แก้หลังบ้าน**; เสนอรายงานอ่านอย่างเดียว (บรรทัด `VatRate = -1` และ `VatAmount < 0`) ในรอบถัดไป ·
11) แตะภาษี — ควรส่งฝ่ายค้านอีกรอบ · 12) TEST_PLAN §0 ต้องขยับ (main agent) — `**329 ไฟล์ · 2,846 [Fact] + 445 [Theory] (1,947 InlineData)** ณ 2026-09-24`

`check_all` (รอบสาม): checker ทุกตัว ✅ · sim 5 ✅ · brace/node/U+FFFD ✅ · ❌ เดียว TEST_PLAN §0 (ห้ามแตะ) · ยังไม่ได้คอมไพล์ (ไม่มี dotnet SDK)
