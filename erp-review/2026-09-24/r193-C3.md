# รอบ 193 · ทีม C3 — ปิดช่องที่ฝ่ายค้านพบใน "คีย์เลขภาษี + สาขา" (คำตัดสินเจ้าของข้อ 20)

> ต่อจากทีม O2 (`abd33a7` · `r193-O2.md`) · ยังไม่ได้คอมไพล์ในเครื่องนี้ (ไม่มี .NET SDK) — รบกวน rebuild/CI
> `bash tools/check_all.sh`: checker 44 ตัว + self-test + sim + brace/node ผ่านทั้งหมด · **ล้มข้อเดียว = `test_inventory --check`**
> (TEST_PLAN §0 ต้องขยับเพราะเพิ่มเทสต์ — ห้ามแตะ TEST_PLAN ในรอบนี้ ให้ main agent วาง `--row` ทับหลัง merge)

## 1. ผล verify ข้อที่ฝ่ายค้านยก (ทุกข้อเปิดโค้ดจริงก่อนแก้)

| ข้อ | ฝ่ายค้านว่า | verify | หมายเหตุ |
| --- | --- | --- | --- |
| 1 | `DocumentsV1Controller.ResolveContactAsync` ส่ง `branchCode: null` ทั้งที่ `ContactsV1` สัญญาว่า "จะสร้างผู้ติดต่อของสาขานี้แยกเมื่อสร้างเอกสาร" | **จริง** | `CreateRequest` ไม่มีช่องสาขาเลย · และเมื่อเลขมีอยู่แล้วคนละสาขา โค้ดยังถอยไปเทียบชื่อ (ได้แถวสาขาอื่นกลับมา) · แถวใหม่ตั้ง `IsSupplier` อย่างเดียว ⇒ ใบฝั่งขายของแถวใหม่ตกด่านบทบาทใน `CreateDocumentAsync` |
| 2 | แถวสาขาว่างถูก payload สาขา 00008 อ้างได้ (`UnspecifiedBranchRow`) แล้ว `ProcessCustomerAsync` เขียน 00008 + ที่อยู่ทับ | **จริง** | ทางเข้าที่ได้รับผลจากกติกาเดียวกัน: integration ลูกค้า (เขียนทับ) · นำเข้าผู้ติดต่อ (เขียนทับสาขา) · POS ใบกำกับเต็มรูป (เติมสาขาเมื่อว่าง = ทับแถวว่าง) · API v1 resolve (ตอบว่าตรง) · ที่พักส่ง "00000" จึงไม่โดน |
| 3 | `CmsCustomerService` ~346 · `CrossTenantWorkflowService` ~384, ~472 · `PlatformBillingDocumentIssuer` ~292 ยังจับด้วยเลขอย่างเดียว | **จริงทั้ง 4** | พบเพิ่มอีก 11 จุด (ดู §3) — รวม **2 จุดที่พังแรงกว่าที่ฝ่ายค้านบอก**: `ToDictionaryAsync(c => c.TaxId)` ใน `CompetitorImportFramework` ×2 และหน้าพรีวิวนำเข้า `ImportExportService` ⇒ `ArgumentException` ทั้งไฟล์ทันทีที่เลขเดียวมีสองสาขา |
| 5 | Lodging ไม่ดู `TaxIdExists` แล้วตกไป email/phone | **จริง** | ดู §5 (ไม่แก้ — ทีม L2 ถือไฟล์) |

ไม่มีข้อที่ verify แล้วไม่จริง

พบเพิ่มระหว่างทาง (จริง · แก้แล้ว):
- **รหัสสาขาผิดรูปทับแถว สนญ.** — `"8A"`/`"สาขา 8"` ถูกตัวจับคู่ถือว่า "ไม่รู้" (`TaxBranchCode.TryNormalize` ไม่ผ่าน ⇒ ได้แถว สนญ.)
  แต่ `ContactTypeResolver.NormalizeBranchCode` ดึงเลขออกเป็น `00008` ⇒ `ProcessCustomerAsync`/`ImportContactAsync` เขียน 00008 ลงแถว สนญ.
  (ช่องเดียวกับข้อ 2 ผ่านอีกประตู) · payload `branchCode: ""` ก็ล้างสาขาของแถวที่ได้มาได้
- **`/api/v1/contacts/sync` บล็อกสาขาของนิติบุคคลเดียวกัน** — ด่าน `CONTACT-TAXID-OWNED` เทียบเลขอย่างเดียว ⇒ ERP ที่ sync สนญ. (รหัส A) แล้ว
  สาขา 8 (รหัส B) ถูกปฏิเสธที่ B เสมอ ขัดคำตัดสินข้อ 20 ตรง ๆ
- **CMS AutoLink จับอีเมลก่อนเลขภาษี** และเมื่อลูกค้าเว็บไม่มีอีเมล EF แปล `c.Email == null` เป็น `IS NULL` ⇒ ผูกผู้ติดต่อรายแรกที่ไม่มีอีเมล
- **ข้ามบริษัท (CrossTenant)** เมื่อบริษัทคู่ค้าไม่มีเลขภาษี `c.TaxId == supplier.TaxId` แปลเป็น `IS NULL` ⇒ ผูกผู้ติดต่อไม่มีเลขรายไหนก็ได้ · แถวที่สร้างไม่ตั้งสาขา/ชนิด
- **DuplicateDetector** รายงาน "ชื่อตรงกัน (เลขผู้เสียภาษีต่างกัน)" กับแถวสาขาอื่นของ**เลขเดียวกัน** (ข้อความเท็จ) และแถวสาขา 8 ถูกเสนอให้ "เขียนทับ" แถว สนญ.

## 2. ทำอะไรไปแล้ว (file:line)

### ตัวจับคู่กลาง `Accounting/Helpers/ContactTaxBranchKey.cs`
- `:95` แถวที่ไม่เคยระบุสาขา (≡ 00000) อ้างได้**เฉพาะ payload สำนักงานใหญ่** — payload สาขาอื่นได้ "ไม่พบ + `TaxIdExists`" ⇒ ผู้เรียกสร้างแถวของสาขานั้น ·
  ถอด `ContactKeyBasis.UnspecifiedBranchRow` (ไม่มีผู้เรียกนอกเทสต์ — `callers.py` = 0)
- `:39` `ContactKeyMatch.MayOverwriteBranch` — เขียนสาขาของ payload ลงแถวที่ได้มาได้เฉพาะเมื่อ `ExactBranch` / ไม่ได้จับด้วยเลข / เลขไม่ใช่ 13 หลัก
- `:134` `PickContact(rows, taxId, branch)` + `:145` `LoadByTaxIdsAsync(...)` — ตัวจับคู่เดียวกันสำหรับเส้นนำเข้าเป็นชุด/change tracker (แทน `ToDictionary(c => c.TaxId)`)

### ข้อ 1 — API v1
- `Controllers/V1/DocumentsV1Controller.cs:62` `CreateRequest.ContactBranchCode` (optional) · `:95` รหัสผิดรูป = 400 ชี้ช่อง ·
  `:313` ส่งสาขาเข้า `ContactTaxBranchKey.FindAsync` · เลขมีแล้วคนละสาขา ⇒ **ไม่เทียบชื่อ** · สร้างแถวสาขาใหม่โดยชื่อ (ถ้าไม่ส่งมา) + บทบาทลูกค้า/ผู้จำหน่ายตามแถว สนญ.
  ของเลขเดียวกัน + ฝั่งของเอกสาร (`Helpers/DocumentSide.IsSales`) · `:368` สาขาของแถวใหม่ผ่าน `ContactTypeResolver.ResolveWithBranch`
- `Controllers/V1/ContactsV1Controller.cs:388` ข้อความ resolve ตรงพฤติกรรมจริง ("ส่ง contactTaxId พร้อม contactBranchCode … ถ้าไม่ส่ง = ผูกสำนักงานใหญ่") + `taxIdExists: true` ·
  `:362` รหัสสาขาผิดรูป = 400 · `:170` ด่าน `CONTACT-TAXID-OWNED` เทียบ **เลข + สาขา** ผ่าน `ContactTaxBranchKey.Pick`
- `wwwroot/connect/workbench.html` ช่อง "รหัสสาขาผู้ขาย" (เติมจาก `vendorBranchCode` ของสแกน) ส่งทั้ง resolve และ create · แสดงข้อความ `reason` จากเซิร์ฟเวอร์เมื่อไม่พบ

### ข้อ 2 — แถวสาขาว่าง / เขียนทับสาขา
- ตัวจับคู่ (ข้างบน) ปิดทุกทางเข้าในครั้งเดียว: integration ลูกค้า · นำเข้าผู้ติดต่อ · POS · API v1 resolve/documents · ที่พัก (ส่ง 00000 — พฤติกรรมเดิม)
- `Services/Implementations/IntegrationService.cs:650` เขียนสาขาเฉพาะ `MayOverwriteBranch`
- `Services/Implementations/ImportExportService.cs:560` เหมือนกัน

### ข้อ 3 — ทางเข้าที่ยังจับด้วยเลขภาษีอย่างเดียว (**ก่อน 19 จุด/13 ไฟล์ → หลัง 7 จุด/4 ไฟล์** — นับด้วย checker ใหม่)
| ไฟล์ | เดิม | ตอนนี้ |
| --- | --- | --- |
| `CmsCustomerService.cs:343` | อีเมลก่อน แล้ว `c.TaxId ==` | เลข + `SiteCustomer.BranchCode` ก่อน · `TaxIdExists` ⇒ ไม่ถอยไปอีเมล · อีเมลว่างไม่จับ (แก้เฉพาะส่วนจับคู่ — ไม่แตะ VAT/e-Tax ของทีม V) |
| `CrossTenantWorkflowService.cs:385,510` (+ `:456` `FindPartnerContactAsync` · `:477` `NewPartnerContact`) | `c.TaxId == company.TaxId` | เลข + `Company.BranchCode` · ไม่มีเลข ⇒ ชื่อตรงตัว + บทบาท · แถวใหม่ได้สาขา/ชนิด/ที่อยู่จากตัวตัดสินกลาง |
| `PlatformBillingDocumentIssuer.cs:297` | `c.TaxId == taxId` | เลข + `buyer.BranchCode` (ว่าง = 00000) |
| `Import/DuplicateDetector.cs:125` | `c.TaxId == taxId` | เลข + คอลัมน์ `BranchCode` · คนละสาขา ⇒ ไม่ใช่ "ซ้ำ" และไม่ตกไปเทียบชื่อ |
| `ImportExportService.cs:140` (พรีวิว) | `ToDictionaryAsync(c => c.TaxId)` (**โยน** เมื่อสองสาขา) | `LoadByTaxIdsAsync` + `PickContact` (สาขาจากไฟล์) |
| `ImportExportService.cs:1035` (ยอดยกมา change tracker) | `c.TaxId == contactTaxId` | `PickContact(..., null)` |
| `Migration/CompetitorImportFramework.cs:157,244` | `ToDictionaryAsync(c => c.TaxId)` ×2 (**โยน**) | `LoadByTaxIdsAsync` + `PickContact(..., null)` |
| `Controllers/V1/ContactsV1Controller.cs:122` | โหลด owners ด้วยเลขอย่างเดียว | โหลด `Id`+`BranchCode` แล้วตัดสินด้วย `Pick` (ข้างบน) |
| `Services/Ai/AdvancedAiAugmenter.cs:113` | `c.TaxId ==` → ประวัติผู้ขายจากแถวไหนก็ได้ | `FindAsync(..., null)` (แถว สนญ. ก่อน) |
| `Controllers/AiSuggestionController.cs` fuzzy-match | 5 แถวของเลขเดียวกัน หน้าตาเหมือนกันทุกตัวอักษร | **ตั้งใจคืนทุกสาขา** (รายการให้คนเลือก) — เพิ่ม `branchCode` + ป้ายสาขาใน `reason` (เซิร์ฟเวอร์คำนวณ หน้าแสดง) |
| `Services/Ai/DocumentAiAugmenter.cs` ×2 | ประวัติ WHT/ยอดสะสม/ค่าเฉลี่ย | **ตั้งใจรวมทุกสาขา** (ผู้มีเงินได้หนึ่งราย · บริบทให้ AI) — ใส่คอมเมนต์เหตุผล · อยู่ใน baseline |

ที่เหลือใน baseline (ไฟล์ของทีมอื่น — **ห้ามแตะรอบนี้**):
- `CmsLeadService.cs:217` (ทีม V) — `c.TaxId == lead.CustomerTaxId` แล้วถอยไปอีเมล → ใช้ `FindAsync(branchCode: null)` + ห้ามถอยไปอีเมลเมื่อ `TaxIdExists`
- `OcrService.cs:5974` (สร้างผู้ติดต่อจากสแกนในเส้น re-process) · `:9512` `ResolveContactIdByTaxIdAsync` (ทีม O1) — ใช้ `FindAsync` + สาขาจากสแกน
  (`VendorBranchCode`/`BuyerBranchCode`) · (`:2512` ใช้ `OcrVendorBranchContact.Decide` ที่ดูสาขาอยู่แล้ว — ไม่ฟ้อง)
- `PayrollService.cs:3378,3383` (ทีม M2) — ผู้รับเงินเป็นบุคคลธรรมดา (ไม่มีโครงสาขา) ผลจึงไม่ผิดวันนี้ แต่ควรผ่านตัวจับคู่ตัวเดียว `FindAsync(branchCode: null)`

### ข้อ 4 — checker
- `tools/contact_taxid_only_match_check.py` — ฟ้อง**ประโยค**ที่แหล่งเป็นผู้ติดต่อ (`.Contacts` · `Set<Contact>()` · `Entries<Contact>()` · ตัวแปร IQueryable ที่ประกาศจาก
  `.Contacts` ในไฟล์ · navigation `.Contact.TaxId`) **และ**เทียบเลขภาษี (`.TaxId ==` · `== x.TaxId` · `.Contains(c.TaxId)` · `.TaxId.Equals(` · `Normalize(c.TaxId) ==`)
  โดยไม่มี `BranchCode`/`ContactTaxBranchKey` ในประโยคเดียวกัน · ไม่ฟ้อง `== null`/`== ""` · `VendorTaxId` · `Companies` · คอมเมนต์ · เจ้าของกติกา ·
  ข้าม `bin` `obj` `.claude` `node_modules` `wwwroot`
- baseline `tools/contact_taxid_only_match_baseline.txt` (7 จุด/4 ไฟล์ พร้อมเหตุผลรายไฟล์) · ratchet เดินทางเดียว (ล้มเมื่อจำนวนต่อไฟล์เพิ่ม)
- negative test `--self-test` 16 เคส: จับ 6 รูปที่ผิด · ไม่ฟ้อง 7 รูปที่ถูก · ไม่สแกน `bin`/`obj`/`.claude` — และลองกับโค้ดจริง: `git show HEAD:CmsCustomerService.cs`
  ถูกฟ้องที่ `:346` · ไฟล์หลังแก้ = 0
- `tools/check_all.sh` ขั้น 1a รัน `--self-test` ของ checker นี้ (ตัว checker เองถูกกวาดโดย glob `tools/*_check.py` อยู่แล้ว)

## 3. เทสต์ที่เพิ่ม (`Accounting.Tests/ContactTaxBranchKeyTests.cs`)
สองครึ่งตาม CLAUDE.md §H:
- ครึ่งที่พัง: `LegacyRowWithoutBranch_NotClaimedByBranch8Payload_CallerCreatesBranch8Row` · `…_EvenWhenOnlyDigitsGiven` ·
  `Sequence_Branch8ThenHq_HqStillOwnsLegacyRow` (ลำดับจริงที่ฝ่ายค้านเล่า) · `MayOverwriteBranch_False_WhenMalformedBranchFellBackToHq` ·
  `PickContact_TwoBranchesSameTaxId_NoThrow_ImportWithoutBranchGetsHq` (สาธิต `ToDictionary` โยนจริง) · `SyncOwnerCheck_Branch8OfSameEntity_IsNotOwnedByHqCode`
- ครึ่งที่ต้องยังถูก: `LegacyRowWithoutBranch_StillClaimedByHqPayload_AnySpelling` (Theory 00000/0/000) · `MayOverwriteBranch_True_ForExactBranch_And_NonTaxMatch` ·
  `PickContact_NoRowForBranch_ReturnsNull_SoCallerCreatesBranchRow` (เลขแถวเดียว/มีขีด ได้แถวเดิม) · `SyncOwnerCheck_SameBranchDifferentCode_StillOwned`
- เทสต์เดิม `LegacyRowWithoutBranch_ClaimedByBranchPayload_NoDuplicate` ถูกแทน (ล็อกพฤติกรรมที่ฝ่ายค้านพิสูจน์ว่าผิด)
- ไม่มีเทสต์ระดับ controller/service (เรพไม่มีโครง DB-backed test) — ตรรกะที่ตัดสินอยู่ใน helper ที่มีเทสต์ทั้งหมด

## 4. เอกสารที่ main agent ต้องขยับ (ห้ามแตะรอบนี้)
- **TEST_PLAN.md §0** — `python3 tools/test_inventory.py --row` วางทับ (ตัวเลขเปลี่ยนเพราะเทสต์ใหม่) · เพิ่มแถวกลุ่ม ContactTaxBranchKey (C3)
- **DOCUMENT_FLOW.md** (ทางเข้า §entry points) — `/api/v1/documents` รับ `contactBranchCode` · เลขมีแล้วคนละสาขา ⇒ สร้างผู้ติดต่อสาขาใหม่ (ไม่เทียบชื่อ) ·
  บทบาทแถวใหม่ตามฝั่งเอกสาร · ข้ามบริษัท/ใบค่าบริการแพลตฟอร์มผูกผู้ติดต่อด้วยเลข + สาขาของ `Company`
- **ACCOUNT_STRUCTURE.md** (Connected `/api/v1`) — `POST /documents.contactBranchCode` · `POST /contacts/resolve` ตอบ `taxIdExists` + 400 เมื่อสาขาผิดรูป ·
  `/contacts/sync` ด่าน `CONTACT-TAXID-OWNED` เทียบเลข + สาขา
- **หน้าเอกสาร API (`wwwroot/pages/api-developer.html`)** — ไม่มีส่วน request ของ `POST /api/v1/documents` ให้เติม (ไม่ได้แตะ) · ถ้ามีเอกสาร partner แยก ให้เพิ่ม `contactBranchCode`
- **CLAUDE.md** — checker ตัวใหม่ในลิสต์หมวด F (`python3 tools/contact_taxid_only_match_check.py # query ผู้ติดต่อด้วยเลขภาษีอย่างเดียว …`)
- **CHANGELOG.md** — ประวัติรอบ

## 5. Lodging (ไม่แก้ — ส่งต่อทีม L2)
`Services/Implementations/Lodging/LodgingService.Reservations.cs` `FindOrCreateContactAsync` (~413–439):
- `:420-424` หาเลข + `"00000"` ถูกแล้ว แต่**ทิ้ง `taxKey.TaxIdExists`** ⇒ `:426-427` ถอยไปจับ email/phone ⇒ เลขนี้มีแค่แถวสาขา 8 (หรือแถวของคนละเลขที่ใช้อีเมลเดียวกัน)
  ก็ได้แถวนั้นกลับมา = ใบเสร็จ/ใบกำกับของแขกนิติบุคคลออกในนามสาขาผิด หรือคนละนิติบุคคล
- วิธีแก้: ยก `taxKey` ออกนอก `if` แล้วเพิ่มเงื่อนไข `!taxKey.TaxIdExists` ให้บรรทัด email และ phone (แบบเดียวกับ `IntegrationService.ProcessCustomerAsync:541`) ·
  และเมื่อ `taxId != null` แต่เลขไม่มีในระบบเลย การจับด้วย email/phone ควรจำกัดที่ `x.TaxId == null || x.TaxId == ""` (ห้ามได้ผู้ติดต่อที่ถือเลขอื่น) —
  ข้อนี้ต้องใส่เทสต์สองทิศ (แขกบุคคลไม่มีเลข จับอีเมลได้เหมือนเดิม)
- ผลของ C3 ต่อ Lodging: ไม่เปลี่ยน (ส่ง 00000 ⇒ ยังอ้างแถวสาขาว่างได้เหมือนเดิม)

## 6. F3 ข้อ 7–12 (สรุป — ฉบับเต็มในข้อความคอมมิต)
- 7: `c.TaxId ==`/`ToDictionary(TaxId)` บน Contacts ทั้งเรพ ก่อน 19 → หลัง 7 (ตั้งใจ 2 · ไฟล์ทีมอื่น 5) · `UnspecifiedBranchRow` เหลือ 0
- 8: ทางเข้าชุดเดียวกัน — integration ลูกค้า/ใบขาย/ผู้ขาย · นำเข้า 4 เส้น · POS · ที่พัก · API v1 ×3 · CMS · ข้ามบริษัท · แพลตฟอร์ม เดินตัวจับคู่เดียว ·
  ที่ยังไม่: CMS lead · OCR ×2 · payroll · ที่พัก (ถอยไปอีเมล)
- 9: เข้มขึ้น 2 จุด — รหัสสาขาผิดรูปใน API v1 = 400 (ทางไปต่อ: ข้อความบอกรูปแบบ 5 หลัก / ไม่ส่งช่องก็ได้) · payload สาขาอื่นไม่ได้แถวว่าง (ทางไปต่อ: ได้แถวสาขาใหม่อัตโนมัติ ·
  เทสต์ทิศตรงข้าม `LegacyRowWithoutBranch_StillClaimedByHqPayload_AnySpelling`)
- 10: แถวที่ถูกเขียนทับไปแล้ว (สาขาว่าง → 00008) **แยกไม่ได้แบบกลไก** ว่าแถวไหนเดิมเป็น สนญ. — ไม่มี migration · ใช้รายงานผู้ติดต่อข้อมูลเสียของ O2
  + ประวัติ `UpdatedBy = api/integration` ให้คนตัดสิน (**ต้องให้เจ้าของตัดสินว่าจะเขียน data-fix หรือไม่**)
- 11: แตะภาษี (§86/4 สาขา) — agent นี้ส่ง subagent ฝ่ายค้านไม่ได้ **main agent ต้องส่ง diff ให้ฝ่ายค้าน 1 รอบ**
- 12: ต้องขยับ DOCUMENT_FLOW · ACCOUNT_STRUCTURE · TEST_PLAN (ดู §4)

## 7. สิ่งที่ยังเหลือ
- ไฟล์ทีมอื่นใน baseline 5 จุด (§2 ท้าย) + Lodging (§5)
- `InboundInvoiceRequest` (integration ใบขาย) **ไม่มีช่องสาขาผู้ซื้อ** ⇒ `IntegrationService.ResolveContactAsync` ส่ง null = แถว สนญ. เสมอ (ซื่อสัตย์ แต่ใบกำกับของผู้ซื้อสาขา
  ยังออกในนาม สนญ. ถ้าพาร์ตเนอร์ไม่ sync ลูกค้าแยกสาขาแล้วอ้าง `customerExternalId`) — ต้องเพิ่ม `CustomerBranchCode` ในสัญญา (เรื่องของเจ้าของสัญญา integration)
- POS (ทีม M2) เติมสาขาด้วยค่าดิบ `buyerBranch` เมื่อแถวว่าง: รหัสผิดรูป `"8A"` ได้แถว สนญ./ว่างแล้วเขียน `"8A"` ดิบลงไป — ควรใช้ `MayOverwriteBranch` + `TaxBranchCode.TryNormalize`
- `IntegrationService.ProcessCustomerAsync` ยังเขียน**ที่อยู่**ของ payload ลงแถวที่ได้จากกติกา "ไม่ระบุสาขา" (รหัสผิดรูป) — ควรตีกลับรหัสผิดรูปที่ต้นทางแทน (ต้องให้เจ้าของเลือก: 400 หรือบันทึกพร้อมคำเตือน)
- `ImportContactAsync` / `PreviewConflictsAsync` ใช้คีย์ resolution = เลขภาษี (ไม่รวมสาขา) ⇒ สองแถวสาขาในไฟล์เดียวกันแชร์การตัดสิน Skip/Overwrite ตัวเดียว
- `PlatformBillingDocumentIssuer` ยังเดาชนิดผู้ติดต่อเอง (`StartsWith('0')`) แทน `ContactTypeResolver` — นอกโจทย์ C3 จึงไม่แตะ

---

## หลังฝ่ายค้าน (รอบ 2 · ตอบ `review193-V-C3.md`)

> merge `claude/erp-system-review-team-660mev` (a217cc1) ก่อนแก้ · เปิดไฟล์ยืนยันทุกข้อ: **C-6 จริง · C-8 จริง · P-5 จริง · P-6 จริง ·
> จุดบอด/ฟ้องผิดของ checker จริงทั้ง 4** (probe ด้วยโค้ดสังเคราะห์ตามรายงาน) · C-7 ไม่ทำ (ส่ง L2 แล้ว) · ยังไม่ได้คอมไพล์ (ไม่มี SDK)

### บันทึกคำกล่าวอ้างที่ผิด
- คอมมิต de97a95 (main agent · `SoftMatchScope` ที่พัก) ข้อ 7 เขียนว่า "Integration ใช้ `!TaxIdExists` อยู่แล้ว · **0 จุดค้าง**" — **ไม่จริง**:
  `!TaxIdExists` กันแค่ "เลขนี้มีแล้วคนละสาขา" ไม่กัน "เลขใหม่ + ชื่อ/อีเมลตรงกับนิติบุคคลอื่น" · ณ a217cc1 ยังถอยไปจับชื่อ/อีเมลนอก `SoftMatchScope` **6 เมธอด**
  (Integration ลูกค้า/ใบขาย/ผู้ขาย · CMS AutoLink · นำเข้าผู้ติดต่อ · API v1) + CMS lead + ยอดยกมา/นำเข้าเอกสาร + ตัวตรวจซ้ำ
- คอมมิต 4fcd06b (C3 รอบ 1) §2 เขียนว่า PlatformBilling/CrossTenant "ปิดแล้ว" — ช่อง "-" (C-8) ยังเปิดอยู่ในทั้งสองเมธอด (ไม่ใช่การถดถอย แต่คำกล่าวอ้างเกินจริง)

### แก้แล้ว (file:line หลังแก้)
- **ตัวจับคู่กลาง** `Helpers/ContactTaxBranchKey.cs`
  - `:84` `Pick` · `FindAsync` · `LoadByTaxIdsAsync` · `SoftMatchScope`: ค่าที่ไม่มีตัวเลขเลย ("-" ของ `AuthService` · "N/A") = **ไม่มีเลข** (C-8) · `:226` `HasTaxId`
  - `:234` **`SoftScope(IQueryable, companyId, payloadTaxId, taxKey)`** — ชุดที่ถอยไปจับชื่อ/อีเมล/เบอร์ได้ (null = ห้าม · เลขใหม่ = เฉพาะแถวที่ยังไม่มีเลข/"-" ·
    ไม่มีเลข = ทุกแถว) + overload ในหน่วยความจำ `:244` (change tracker) — ทุกทางเข้าเรียกตัวนี้ตัวเดียว
  - `:256` **`MayWriteTaxId(existing, incoming)`** — ห้ามเขียนทับเลขของนิติบุคคลอื่น / ห้ามล้างเลขจริงด้วย "-"
  - `:169` `LoadByTaxIdsAsync` ดึงแถวที่เก็บเลขแบบมีขีด/ช่องว่างด้วย (P-6)
  - `:195` **`AllBranchIdsAsync`** — ทางที่ "ประกาศว่าตั้งใจรวมทุกสาขา" (ให้ checker แยก "ตั้งใจ" ออกจาก "ลืมสาขา")
- **C-6 — SoftScope ทุกทางเข้า**: `IntegrationService.cs:555` (ลูกค้า) + `:646` `MayWriteTaxId` แทน `contact.TaxId = request.TaxId` · `:1614` (ใบขาย) · `:1708` (ผู้ขาย —
  ฝ่ายค้านไม่ได้ยก แต่คลาสเดียวกัน) · `CmsCustomerService.cs:351` · `ImportExportService.cs:522` (ผู้ติดต่อ) · `:1031`/`:1040` (ยอดยกมา DB + change tracker) ·
  `:1594` (นำเข้าเอกสาร) · `DocumentsV1Controller.cs:354` (ผู้สมัครเทียบชื่อข้ามภาษามาจากชุด SoftScope) · `CmsLeadService.cs:245` (แก้เฉพาะการจับคู่ — เลขผ่าน
  `FindAsync` + อีเมลบน SoftScope) · `Import/DuplicateDetector.cs:142` (ชื่อซ้ำเสนอเฉพาะแถวที่ยังไม่มีเลข · ข้อความ "เลขต่างกัน" ที่เคยเท็จถูกเปลี่ยน) ·
  `CrossTenantWorkflowService.cs:471` · `PlatformBillingDocumentIssuer.cs:311`
- **C-8**: `PlatformBillingDocumentIssuer.cs:291` "-" = ไม่มีเลข ⇒ กุญแจแรก `ExternalSystem="NextAccTenant" + ExternalId = buyer.Id` → ชื่อ (ไม่เอาแถวที่ผูกกับ tenant อื่น) ·
  แถวใหม่เก็บ `TaxId = null` แทน "-" · `CrossTenantWorkflowService.cs:460,492` เหมือนกัน
- **P-5**: `DocumentsV1Controller.cs:167` เอกสารฝั่งขายตอบ `contact.missingBuyerFields` (ตัวตรวจเดียวกับด่านอนุมัติ `TaxInvoiceCompletenessChecker.MissingBuyerFields`) +
  `branchCode` · ข้อความ response บอกว่าต้องเติมอะไรก่อน `/approve`
- **ทางที่ตั้งใจรวมสาขา** ย้ายไป `AllBranchIdsAsync`: `DocumentAiAugmenter.cs:281` (ประวัติ WHT) · `:514` (ค่าเฉลี่ย — และแก้ของแถม: เดิม `d.Contact.TaxId == null`
  เป็น IS NULL = เฉลี่ยทุกผู้ติดต่อที่ไม่มีเลข) · `AiSuggestionController.cs:955` (fuzzy-match) · `ContactsV1Controller` โหลด owners ผ่าน `LoadByTaxIdsAsync`

### checker (`tools/contact_taxid_only_match_check.py`)
- กติกา 1 ปิดจุดบอด: list ที่ `await …ToListAsync()` แล้ว · `string.Equals(c.TaxId, x)` · ยกเว้นเฉพาะเมื่อสาขาถูก**เทียบ** หรือป้อนตัวตัดสินที่ดูสาขา
  (`ContactKeyCandidate` · `OcrVendorBranchContact`) — แค่ `.Select(new { c.BranchCode })` ไม่นับแล้ว · navigation นับเฉพาะพารามิเตอร์ lambda
  ⇒ `doc.Contact.TaxId == company.TaxId` ไม่ถูกฟ้องแล้ว
- **กติกา 2 ใหม่ (key `ไฟล์#soft`)**: หลัง `ContactTaxBranchKey.FindAsync` ในเมธอดเดียวกัน การจับผู้ติดต่อด้วย Name/Email/Phone บน `.Contacts`/`Set<Contact>()` ตรง ๆ
  ต้องผ่าน `SoftScope` — pattern แคบ (หน้าต่าง = จาก FindAsync ถึงประกาศเมธอดถัดไป)
- self-test 24 เคส (จับ 11 รูปที่ผิด · ไม่ฟ้อง 10 รูปที่ถูก · ข้าม 3 โฟลเดอร์) + ทดลองกับโค้ดจริงก่อนแก้: `IntegrationService` (a217cc1) ถูกฟ้องกติกา 2 ที่ :554/:1613/:1708 ·
  `AiSuggestionController` กติกา 1 ที่ :956 · หลังแก้ = 0
- baseline: **กติกา 1 = 4 จุด/2 ไฟล์** (OcrService ×2 ทีม O1 · PayrollService ×2 ทีม M2) · **กติกา 2 = 1** (PosService ทีม M2) — ตัวเลขลดจาก 7 (ไม่มีแถวที่ "ตั้งใจ" เหลือแล้ว
  เพราะย้ายไป `AllBranchIdsAsync`) · OcrService :2549 (`OcrVendorBranchContact.Decide`) ดูสาขาอยู่แล้ว — ไม่ฟ้อง

### ล็อกจุดเรียก (`tools/required_call_site_check.py` ของทีม M2 — เพิ่ม 12 กติกา)
ฝ่ายค้านพบว่าถอด `MayOverwriteBranch`/สาขาใน v1 ออกแล้วเทสต์ยังเขียว ⇒ ล็อกรายเมธอด (negative test ในตัวรันทุกครั้ง: ลบ/คอมเมนต์/สลับแล้วต้องฟ้อง):
`IntegrationService.ProcessCustomerAsync` (FindAsync ด้วยสาขาของ payload · SoftScope หลัง FindAsync · `MayOverwriteBranch` · `MayWriteTaxId`) ·
`ResolveContactAsync` · `ResolveSupplierAsync` · `ImportExportService.ImportContactAsync` · `DocumentsV1Controller.Create` (400 สาขาผิดรูป · `MissingBuyerFields`) ·
`DocumentsV1Controller.ResolveContactAsync` (`req.ContactBranchCode` ทั้งหา+สร้าง · SoftScope) · `ContactsV1Controller.SyncCoreAsync` (`Pick` + สาขาที่มีผล) ·
`CmsCustomerService.AutoLinkToErpContactAsync` · `CmsLeadService.EnsureContactLinkedAsync` · `PlatformBillingDocumentIssuer.EnsureContactAsync` (`HasTaxId`) ·
`CrossTenantWorkflowService.FindPartnerContactAsync` · `DuplicateDetector.DetectContactAsync` — ผล: 23 กติกาผ่าน

### เทสต์ที่เพิ่ม (`ContactTaxBranchKeyTests.cs`)
`Placeholder_WithoutDigits_IsNoTaxId_NeverMatches` (Theory "-"/" - "/"N/A") · `Placeholder_DoesNotBreakRealTaxIds` (ทิศตรงข้าม) ·
`SoftScope_NewTaxId_ExcludesRowsHoldingAnotherTaxId` · `SoftScope_NoTaxIdInPayload_AllRows_UnchangedBehaviour` (ทิศตรงข้าม) · `SoftScope_TaxIdExistsOtherBranch_Empty` ·
`MayWriteTaxId_NeverOverwritesAnotherEntitysTaxId` (Theory 6 เคสสองทิศ)

### เอกสารที่ main agent ต้องขยับเพิ่ม
- TEST_PLAN §0 (`test_inventory --row` — ข้อเดียวที่ `check_all` ล้ม)
- DOCUMENT_FLOW / ACCOUNT_STRUCTURE: `/api/v1/documents` ตอบ `contact.missingBuyerFields` + `branchCode` · ทุกทางเข้าถอยไปจับชื่อ/อีเมลบน `SoftScope` ·
  ใบค่าบริการแพลตฟอร์มผูกลูกค้าด้วย `ExternalId = tenant id` เมื่อยังไม่มีเลข และไม่เก็บ "-" เป็นเลขภาษีอีก
- CLAUDE.md หมวด F: บรรทัด checker ตัวนี้ (กติกา 2 `#soft`)

### ยังเหลือ
- **ข้อมูลที่ persist แล้ว**: ผู้ติดต่อที่เก็บ `TaxId = "-"` (จาก PlatformBilling/CrossTenant/อื่น ๆ) ยังอยู่ — โค้ดใหม่ถือเป็น "ไม่มีเลข" ถูกต้องแล้ว แต่ควรมี migration
  `UPDATE Contacts SET TaxId = NULL WHERE btrim(TaxId) IN ('-','--','N/A','n/a','NA','ไม่มี')` (**ให้เจ้าของตัดสิน** — ไม่ได้ใส่เพราะแตะข้อมูลทุก tenant) ·
  แถวที่ integration เคยเขียนเลขทับเลขของรายอื่น แยกด้วยกลไกไม่ได้ (ต้องดูจาก `SyncLog`/`UpdatedBy`)
- ที่พัก (L2): ยังเขียนตัวกรอง `RowsWithoutTaxId` เองที่ `LodgingService.Reservations.cs:434` — ควรเรียก `ContactTaxBranchKey.SoftScope(...)` ตัวเดียว (ซึ่งรู้จัก "-" แล้ว) · C-7
- POS (M2): ชื่อหลัง FindAsync ไม่ผ่าน SoftScope (baseline `#soft`) · OCR (O1) ×2 · Payroll (M2) ×2
- ความต่างเล็กที่รู้อยู่: `SoftScope` ฝั่ง SQL รู้จัก placeholder เป็นรายการ ("-", "N/A" ฯลฯ) ขณะที่ฝั่งหน่วยความจำใช้ "ไม่มีตัวเลขเลย" — ค่าแปลก ๆ อื่น (เช่น "abc") ฝั่ง SQL จะถือว่ามีเลข (ทิศปลอดภัย: ไม่ถอยไปจับ)

---

## หลังฝ่ายค้านรอบสอง (ตอบ `review193-r2-sec-tax.md`)

> merge `claude/erp-system-review-team-660mev` (f9f1556 · รวมที่ main แก้ CS0103 `rawTaxId` ใน `DocumentAiAugmenter` ที่หลุดจาก 6b2e7fb)
> · verify ทุกข้อจากโค้ดก่อนแก้: **R2-C5 จริง** (4 ทางเข้าจับแถวไม่มีเลขแล้วไม่เติม) · **R2-C9 จริง** · **R2-C10 จริง** (probe C4 ยืนยันด้วย self-test ใหม่) ·
> probe C2 (NameEn) · C3 (fuzzy จากทุกแถว) · C5 (`BranchCode != null`) **จริง** · C7 (PlatformBilling ถอดกุญแจ ExternalId ไม่มีกติกาล็อก) **จริง** · ยังไม่ได้คอมไพล์

### แก้แล้ว
- **R2-C5 + C-6 ฝั่งเติม** — ตัวช่วยเดียว `ContactTaxBranchKey.AdoptTaxId(row, taxId, branch)` (`Helpers/ContactTaxBranchKey.cs`): เติมเฉพาะแถวที่ยังไม่มีเลข
  (null/ว่าง/"-") ด้วยเลขจริงของ payload (`MayWriteTaxId`) · ชนิดผ่าน `ContactTypeResolver.ApplyToExisting` · สาขาเฉพาะเมื่อแถวยังไม่ระบุ ผ่าน `BranchCodeFor`
  (บุคคลธรรมดาไม่ได้ 00000) · ผู้เรียก 11 จุด: Integration ลูกค้า (แทน `MayWriteTaxId` ตรง) / ใบขาย / ผู้ขาย (แทน `IsNullOrWhiteSpace(TaxId)` ที่ไม่รู้จัก "-") ·
  CMS AutoLink · CMS lead · นำเข้าผู้ติดต่อ (หลังด่าน Skip) / ยอดยกมา (หลัง fallback change tracker) / เอกสาร · API v1 (โหลดแถวแบบ tracked แล้วบันทึก) ·
  PlatformBilling · CrossTenant
- **R2-C9** — `PlatformBillingDocumentIssuer.EnsureContactAsync` และ `CrossTenantWorkflowService.FindPartnerContactAsync`: ลำดับกุญแจ
  (1) `ExternalSystem="NextAccTenant" + ExternalId = Id บริษัท` **เสมอ** (ข้ามเฉพาะเมื่อแถวนั้นถือเลข/สาขาอื่นแล้ว — ลูกค้าเปลี่ยนสาขาต้องได้ผู้ติดต่อสาขาใหม่) →
  (2) เลข + สาขา → (3) ชื่อบน SoftScope (ไม่เอาแถวที่ผูกกับบริษัทอื่น) → แถวที่ได้รับเลขด้วย `AdoptTaxId` + ผูกกุญแจถ้ายังว่าง · CrossTenant แถวใหม่ประทับกุญแจแล้ว
  (เดิมไม่มี)
- **probe C3 (ของจริง)** — `ContactsV1Controller.Resolve` ถอยไปเทียบชื่อข้ามภาษาบนทุกแถว ⇒ เลขใหม่ + ชื่อคล้ายตอบ `matched` กับนิติบุคคลที่ถือเลขอื่น (พาร์ตเนอร์ผูกรหัสผิด) —
  เปลี่ยนเป็นผู้สมัครจากชุด SoftScope (checker กติกา 2 ใหม่จับได้เองระหว่างทาง)
- **R2-C10 checker** — ยกเว้นเฉพาะการเรียก `ContactTaxBranchKey.(FindAsync|Pick|PickContact|LoadByTaxIdsAsync|AllBranchIdsAsync|SoftScope)` (ไม่ใช่ `HasTaxId`) ·
  ตัดหัวบล็อก `if (…) {` ที่ติดมากับประโยคแรก (เริ่มพิจารณาที่บรรทัดของแหล่งผู้ติดต่อ) · `BranchCode != null` ไม่นับเป็นเทียบสาขา · กติกา 2 รู้จัก `NameEn`/`ContactPerson`
  และผู้สมัคร fuzzy (`Select(new { … Name })` + `CounterpartyNameMatcher` ในเมธอดเดียวกัน · ไม่นับ query ที่ดึงแถวเดียวด้วย `Id ==`) · self-test 31 เคส
  (จับ 15 · ไม่ฟ้อง 13 · ข้าม 3) · baseline คงเดิม 5 (ไม่มีจุดจริงซ่อนอยู่เพิ่ม — ตรงกับที่ฝ่ายค้านสแกน)
- **required_call_site_check** — เพิ่ม/ขยายกติกา: ทุกเมธอดที่จับแบบ soft ต้องเรียก `AdoptTaxId` · PlatformBilling/CrossTenant ต้องมีกุญแจ ExternalId **ก่อน** `FindAsync`
  (ปิด probe C7) · `ContactsV1.Resolve` · `ImportOpeningSubledgerAsync` · `ImportDocumentAsync` — ผ่านพร้อม negative test ในตัว
- เทสต์: `AdoptTaxId_RowWithoutTaxId_GetsPayloadTaxIdAndBranch` · `AdoptTaxId_PlaceholderRow_IsFilled_BranchDefaultsToHq` (Theory) ·
  `AdoptTaxId_NeverTouchesRowHoldingAnotherOrSameTaxId` (ทิศตรงข้าม) · `AdoptTaxId_Individual_DoesNotGetHeadOfficeSuffix` (ทิศตรงข้าม) · `MayWriteTaxId` เป็น private (dead_helper_check: ผู้เรียกภายนอกเหลือทางเดียวคือ `AdoptTaxId`) ⇒ เทสต์ Theory เดิมเปลี่ยนชื่อเป็น `AdoptTaxId_NeverOverwritesAnotherEntitysTaxId` (เลขเดียวกันต่างรูปแบบ = ไม่แตะ)

### ไม่ได้แก้ (นอกไฟล์ที่แตะ)
- **Q6 V1 confirm** — อยู่ที่ `OcrV1Controller.Confirm` → `AiFeedbackRecorder.RecordUserChoiceAsync` (ค้นด้วย `f.Id == feedbackId` อย่างเดียว) — **ไม่ใช่** ContactsV1/DocumentsV1
  จึงไม่แตะตามคำสั่ง · verify แล้วว่าจริงตามที่ฝ่ายค้านเขียน (ไม่มีกรอง `CompanyId`) · แนวแก้: ส่ง `companyId` เข้า `RecordUserChoiceAsync` แล้วกรอง `f.CompanyId == companyId`
  (ต้องไล่ผู้เรียกทุกจุดของ recorder — `callers.py RecordUserChoiceAsync`)
- ที่พัก (L2): ยังเขียนการเติมเลขเอง (`LodgingService.Reservations.cs:445-452` · `c.BranchCode ??= "00000"` ไม่ดูชนิด) — ควรเรียก `AdoptTaxId` ตัวเดียว
- POS/Payroll (M2) · OCR (O1) — baseline เดิม

### F3 7–12 (ย่อ — ฉบับเต็มในข้อความคอมมิต)
- 7: จับ soft แล้วไม่เติมเลข: ก่อน 9 เมธอด (Integration ใบขาย/ผู้ขาย · CMS ×2 · นำเข้า ×3 · API v1 · PlatformBilling/CrossTenant) → 0 นอกไฟล์ทีมอื่น ·
  ผู้สมัคร fuzzy นอก SoftScope: ก่อน 2 (API v1 แก้รอบก่อน · Resolve) → 0 · baseline checker 5 เท่าเดิม
- 9: เข้มขึ้น — resolve ไม่ตอบ matched ข้ามเลขอีก (ทางไปต่อ: สร้างเอกสารแล้วระบบสร้างผู้ติดต่อใหม่ · หรือ `/contacts/map` ผูกเอง)
- 10: แถวที่ถูกจับไปแล้วแต่ไม่มีเลข (จาก 4 ทางเข้าเดิม) จะได้เลขเมื่อทางเข้านั้นจับอีกครั้ง — ไม่มี migration (ไม่รู้ว่าเลขไหนเป็นของแถวไหนจากฐานอย่างเดียว) ·
  แถว PlatformBilling ที่ซ้ำแล้ว (ExternalId เดียวกันสองแถว) ต้องรวมด้วยมือ — **ให้เจ้าของตัดสิน**

---

## หลังฝ่ายค้านรอบสาม (ตอบ `review193-r3.md`)

> merge `claude/erp-system-review-team-660mev` (613866d) ก่อนแก้ · verify: **R3-2 จริง** (`DocumentsV1Controller` เติมเลขหลัง `CounterpartyNameMatcher`
> · `ImportDocumentAsync` เติมเลขหลัง `Name.Contains` ที่ไม่มี OrderBy) · **B8 จริง** (`AdoptTaxId` ไม่ตรวจ mod-11 และไม่กัน `IsWalkInCustomer`) · ยังไม่ได้คอมไพล์

### คำตัดสินที่ทำตาม (main agent)
จับแบบ fuzzy/substring ได้ต่อ (ไม่ถดถอยเคสที่ไม่มีเลข) แต่เติมเลขได้เฉพาะเมื่อจับด้วยชื่อตรงตัวหลัง normalize · payload มีเลขจริง + จับได้แค่ fuzzy ⇒ ไม่ใช้แถวนั้น

### แก้แล้ว
- **`ContactTaxBranchKey.AdoptTaxId(row, taxId, branch, ContactMatchKind matchedBy)` → `ContactAdoptOutcome { Keep, Adopted, Reject }`** — ตัดสินเองจากชนิดการจับ
  (ผู้เรียกลืมไม่ได้เพราะเป็นพารามิเตอร์บังคับ):
  - `FuzzyName` + เลขใช้ได้ ⇒ **Reject** (ผู้เรียกสร้างแถวใหม่/ไม่ผูก) · `FuzzyName` + ไม่มีเลข/เลขใช้ไม่ได้ ⇒ Keep (ผูกตามเดิม)
  - เลขที่ใช้ได้ = มีตัวเลข · ไม่ใช่ศูนย์ล้วน · 13 หลักต้องผ่าน `ThaiTaxId.IsValid` (mod-11 · ตัวตรวจ canonical) — `0000000000000`/checksum ผิดไม่ถูกเติม
  - แถว `IsWalkInCustomer` ไม่รับเลขใด ๆ — ผู้ซื้อที่มีเลขจริง ⇒ Reject (ไม่ใช้แถวกลาง) · ไม่มีเลข ⇒ Keep
- **`ContactTaxBranchKey.NameMatchKind(given, stored)`** — ExactName เมื่อชื่อเท่ากันหลัง normalize (ตัวพิมพ์ · เว้นวรรค/เครื่องหมาย · "บริษัท/จำกัด/มหาชน/บมจ/บจก/หจก/
  ห้างหุ้นส่วน…" · "Co./Ltd./Company Limited/PLC/Inc.") · อื่น ๆ (substring/ชื่อถูกตัด/ข้ามภาษา) = FuzzyName (กฎ #4 H: superstring ไม่ใช่ fuzzy)
- ผู้เรียกทั้ง 11 จุดส่งชนิดการจับจริงและจัดการ Reject:
  - `DocumentsV1Controller.ResolveContactAsync` — `NameMatchKind(req.ContactName, matched.Name)` · Reject ⇒ สร้างผู้ติดต่อใหม่ (ไม่คืนแถว fuzzy)
  - `ImportExportService.ImportDocumentAsync` — ชื่อตรงตัวก่อน แล้ว substring **เรียงแน่นอน** (`Name.Length` → `CreatedAt` → `Id`) · `NameMatchKind` ·
    Reject ⇒ `KeyNotFoundException` ข้อความไทยบอกเหตุและทางไปต่อ (สร้าง/นำเข้าผู้ติดต่อก่อน) — ไม่ผูกเอกสารกับแถวที่ชื่อตรงแค่บางส่วน
  - Integration ลูกค้า (ย้ายการเติมเลขมาไว้ก่อนตัดสินสร้าง/อัปเดต) / ใบขาย / ผู้จำหน่าย · CMS AutoLink · CMS lead · นำเข้าผู้ติดต่อ (แยกบล็อกให้ Reject ตกไปสร้างแถวใหม่ —
    `action` ยกออกนอกบล็อก) / ยอดยกมา · PlatformBilling · CrossTenant — ชนิดการจับ = TaxKey / ExternalKey / ExactName / Email ตามทางที่จับได้
- รูปเดิม `AdoptTaxId(row, taxId, branch)` (bool) คงไว้แบบ `[Obsolete]` **เฉพาะให้ที่พัก (L2) คอมไพล์ได้** — ถือเป็น Email · Reject ยุบเป็น false ⇒ ที่พักยังใช้แถว walk-in ได้
  **L2 ต้องย้ายไปรูปใหม่แล้วลบรูปเดิม**
- **required_call_site_check**: ทุกกติกาที่บังคับ `AdoptTaxId` บังคับ `ContactAdoptOutcome.Reject` ด้วย (ผู้เรียกต้องจัดการการปฏิเสธ) · `DocumentsV1.ResolveContactAsync` และ
  `ImportDocumentAsync` ต้องเรียก `NameMatchKind(` · `ImportDocumentAsync` ต้องมี `OrderBy(c => c.Name.Length)` — ผ่านพร้อม negative test ในตัว (ไม่แตะกติกาของ L2)
- เทสต์: `AdoptTaxId_FuzzyNameWithRealTaxId_Rejects_RowUntouched` · `AdoptTaxId_FuzzyNameWithoutTaxId_KeepsRow_UnchangedBehaviour` (ทิศตรงข้าม) ·
  `NameMatchKind_SameNameAfterNormalize_IsExact` (Theory 4 · รวม "หจก. แอม แฮปปี้เนส") · `NameMatchKind_PartialOrCrossLanguage_IsFuzzy` (Theory 4 · รวมชื่อถูกตัด
  "แอม แฮปปี้") · `AdoptTaxId_InvalidChecksumOrZeros_NotAdopted` · `AdoptTaxId_WalkInRow_NeverTakesTaxId` · `AdoptTaxId_ExactNameWithRealTaxId_StillAdopts` (ทิศตรงข้าม) ·
  เทสต์ AdoptTaxId เดิมย้ายไปรูปใหม่ (ผลเดิมทุกข้อ)

### F3 7–12 (ย่อ)
- 7: เติมเลขหลังจับแบบ fuzzy/substring: ก่อน 2 จุด → 0 · `Name.Contains` ไม่มี OrderBy ในเส้นผู้ติดต่อ: ก่อน 1 → 0 · ผู้เรียก AdoptTaxId รูปเดิม: 1 (ที่พัก · L2)
- 9: เข้มขึ้น — (ก) API v1: เลขจริง + ชื่อคล้าย ⇒ สร้างผู้ติดต่อใหม่แทนผูกแถวเดิม (ทางไปต่อ: `/contacts/map` หรือรวมในหน้าผู้ติดต่อ) · (ข) นำเข้าเอกสาร: ชื่อตรงบางส่วน +
  เลขใหม่ ⇒ แถวนั้นล้มพร้อมข้อความ (ทางไปต่อ: นำเข้าผู้ติดต่อก่อน) · (ค) walk-in + เลขจริง ⇒ แถวใหม่ · เทสต์ทิศตรงข้าม `…FuzzyNameWithoutTaxId_KeepsRow…`,
  `…ExactNameWithRealTaxId_StillAdopts`
- 10: แถวที่ถูกเติมเลขผิด (fuzzy) ไปแล้วระหว่าง 69ccf37 → คอมมิตนี้ แยกด้วยกลไกไม่ได้ (ไม่มีร่องรอยว่าเลขมาจาก payload ไหน) — ช่วงเวลาสั้น (ยังไม่ merge ขึ้น main) ·
  ถ้า deploy ไปแล้วให้ตรวจแถวที่ `UpdatedAt` อยู่ในช่วงนั้นและ `TaxId` ไม่ตรงชื่อทะเบียน — **ให้เจ้าของตัดสิน**
- 12: DOCUMENT_FLOW/ACCOUNT_STRUCTURE — API v1 สร้างผู้ติดต่อใหม่เมื่อเลขจริง + ชื่อคล้าย · นำเข้าเอกสารปฏิเสธชื่อตรงบางส่วน + เลขใหม่ · TEST_PLAN §0
