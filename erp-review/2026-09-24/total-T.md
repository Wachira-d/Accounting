# ทีม T — ลงมือ Total-first (รอบ 192): "หายอดรวมทั้งสิ้นก่อน แล้วให้ตัวเลขอื่นอธิบายยอดนี้"

> โจทย์: `BRIEF-TOTAL.md` · นิยาม: `total-A.md` · ผลไล่โค้ด: `total-B.md` · แบบ: `total-C.md` · ของเดิมรอบ 190: `team-M.md`
> **ยังไม่ได้คอมไพล์** (เครื่องนี้ไม่มี .NET SDK) — `bash tools/check_all.sh` ผ่านทุกด่าน **ยกเว้น** `test_inventory --check`
> (เทสต์เพิ่มขึ้น · ห้ามแตะ `TEST_PLAN.md`) · ตรรกะของ helper ใหม่ทุกตัวถูกจำลองด้วย JS ที่**อ่าน regex ตรงจากไฟล์ `.cs`**
> แล้วรันกับข้อความกระดาษชุดเดียวกับเทสต์ (ดู §6) — แต่ชนิด/overload ของ C# ยืนยันได้ด้วยคอมไพเลอร์เท่านั้น

---

## 1. สร้างอะไร (เพิ่มวิธีคิด ไม่รื้อ — ไม่ลบโค้ดเดิมบรรทัดเดียว ยกเว้นสูตร gross ที่ย้ายเข้า helper)

| ชิ้น | ไฟล์ | หน้าที่ |
|---|---|---|
| `OcrPaperAmounts` (ใหม่) | `Accounting/Helpers/OcrPaperAmounts.cs` | ตัวอ่าน "ตัวเลขที่พิมพ์บนกระดาษ" ตัวเดียวของขั้นยึดยอด: ยอดเงินต่อบรรทัด · ยอด VAT ที่พิมพ์ (แถวป้าย VAT + VAT ของตารางกลุ่ม · ไม่รวมแถว "รวมภาษี/ไม่รวม/ก่อนภาษี/ยกเว้น/Included/Excluded VAT") · แถวส่วนลด/มัดจำที่มีเงิน (แถว 0 ไม่ใช่หลักฐาน RG-02) |
| `OcrLineVatMarks.ReadGroups` (เมธอดใหม่ — `Read`/`Assign` เดิมไม่แตะ) + `OcrVatGroupKind`/`OcrVatGroup`/`OcrVatGroupTable` | `Accounting/Helpers/OcrLineVatMarks.cs` | ตารางสรุปตามกลุ่มภาษี: แบบห้าง `จำนวนชิ้น รหัส(ตัวเลข) ฐาน VAT รวม` + แถว `รวม` + คำอธิบาย `1=…ยกเว้น… · 2=…ต้องเสียภาษี…` และแบบเดิม `V 7 ฐาน VAT รวม` · ความหมายรหัสจากคำอธิบายบนกระดาษก่อน → ไม่มีคำอธิบาย = พิสูจน์ด้วยตัวเลขแถว (VAT 0 / 7% ของฐาน) · รหัสที่คำอธิบายไม่บอก หรือคำอธิบายขัดตัวเลข = `Unknown` · แถว ฐาน+VAT≠รวม / Σ แถว≠แถว "รวม" / ตารางรหัสตัวเลขไม่มีแถว "รวม" ⇒ ไม่ใช้ทั้งตาราง |
| `OcrTotalAnchor` (ใหม่) | `Accounting/Helpers/OcrTotalAnchor.cs` | **ขั้น 1** ยอดไหนคือยอดรวมทั้งสิ้น: ผู้สมัคร = ป้ายยอดรวม/สุทธิ/ชำระ · แถวชำระที่ไม่ใช่เงินสด · ตัวอักษร · แถว "รวม" ตารางกลุ่ม · ค่าของ engine (+`AmountDue`) · นับ**ชั้นอิสระ** 4 ชั้น (VAT · ตัวอักษร · ป้าย · แถวชำระ) · **Proven = ชั้น VAT + ≥1 ชั้น และค่า engine ถูกอธิบายบทบาทด้วยตัวเลขที่พิมพ์** (ยอดก่อนลด / ยอดที่ชำระหลังส่วนลด-ปรับ / หลังหัก ณ ที่จ่าย / ยอดก่อน VAT) · อธิบายไม่ได้ = `Conflict` · ไม่มีชั้น VAT แต่ตัวอักษร+ป้ายชี้ยอดอื่น = `Unsure` · ใบหักมัดจำที่มีเงิน = ไม่ตัดสิน (`Unknown` — วงจรมัดจำเป็นเจ้าของ) · แถวเงินสดรับ/เงินทอนไม่ใช่หลักฐาน |
| `OcrTotalDecomposer` (ใหม่) | `Accounting/Helpers/OcrTotalDecomposer.cs` | **ขั้น 2** ส่วนลดอยู่ตรงไหน — ให้ตัวเลขที่พิมพ์เป็นกรรมการ (±0.02): `PreVat` (ยอดก่อนลดที่พิมพ์ − ส่วนลด = ฐานที่ VAT ปิดยอด) · `InclVat` (ยอดก่อนลดที่พิมพ์ − ส่วนลด = ยอดรวม) · `PostInvoice` (ยอดรวม − ส่วนลด = ยอดที่พิมพ์ · VAT ปิดยอดรวม**ก่อนหัก** · แถวส่วนลดอยู่หลังยอดรวม) · ลงตัว 0 หรือ >1 แบบ = `Unknown` (ส่งส่วนลดเดิมต่อ) · **เปลี่ยนพฤติกรรมเดียว: `PostInvoice` ⇒ `DiscountToSpread = 0` + `[PAY≠TOTAL]`** · `LineGross` (ยอดพิมพ์ชนะ ราคา×จำนวน เมื่อต่างแค่เศษปัดราคาต่อหน่วย) · `SummaryGroupLines` (บรรทัดสรุปต่อกลุ่มเมื่อไม่มีรายการ) |
| `OcrPageSet` (ใหม่) | `Accounting/Helpers/OcrPageSet.cs` | "หน้า N จาก M / Page N of M" ที่กระดาษพิมพ์ ⇒ หน้าที่ขาด ⇒ `[PAGES-PARTIAL]` (ไม่บล็อก) · ไม่พิมพ์/พิมพ์ขัดกัน = ไม่รู้ = ไม่เตือน |
| `VatBackCalcGuard.PrintedVatContradicts` + พารามิเตอร์ `totalAmount` ของ `Decide` (optional — ไม่ส่ง = ด่านเดิมทุกประการ) | `Accounting/Helpers/VatBackCalcGuard.cs` | แก้บั๊ก "VAT แต่งขึ้น" (ทีม B #3/RC-5): back-calc 7/107 **ทั้งสองชุด** (`OcrService.ParseThaiDocument` ผ่าน `Decide` · `SmartFieldExtractor.ApplyAmountMath` ที่เดิมไม่ผ่านด่านใดเลย) ห้ามแต่งเมื่อกระดาษพิมพ์ VAT ไว้แล้วและไม่ใช่ 7/107 ของยอดรวม |
| แท็กห้ามอนุมัติเอง | `Accounting/Helpers/OcrPostingReadiness.cs:39-42` | `[TOTAL-CONFLICT]` · `[PAY≠TOTAL]` (อ้างค่าคงที่ของ helper — ไม่พิมพ์สตริงซ้ำ) |

## 2. จุดต่อสาย (OcrService = wiring อย่างเดียว)

| # | ที่ | ทำอะไร |
|---|---|---|
| W0 | `OcrService.cs:3484` + ช่องใหม่ `OcrExtractedData.EngineAmountDue` | เก็บ `azure.AmountDue` (เดิมทิ้งเมื่อมี InvoiceTotal) เป็นผู้สมัครของ anchor — `TotalAmount = InvoiceTotal ?? AmountDue` **ไม่เปลี่ยน** |
| W1 | `OcrService.cs:620` → `ApplyTotalFirst` (private static · ต่อจาก `ResolveScanDebitAccountIdAsync`) | หลังตรวจวันที่ **ก่อน**ด่านคณิต · ข้าม e-Tax XML · `Proven` ⇒ เขียน `TotalAmount` conf 0.95 + `Note(PaperLabel)` + ถ้า Sub+VAT ไม่ปิดยอดใหม่ ใช้คู่ฐาน/VAT ที่**พิมพ์**ซึ่งปิดยอดนี้ · `Conflict/Unsure` ⇒ ไม่แตะค่า · conf ≤ 0.80 · แท็ก · ทุกคำตัดสิน ⇒ trace `[TOTAL]` · แตกยอด ⇒ `[PAY≠TOTAL]` · หน้าไม่ครบ ⇒ `[PAGES-PARTIAL]` |
| W1b | `OcrService.cs:681` (ด่านคณิต `documentDiscount`) · `:769` (หลัง `EnrichFromRawText`) | ส่วนลด `PostInvoice` ไม่ถูกส่งให้ข้อ 0 ของด่าน และไม่ถูกเก็บเป็น `ExtractedDiscountAmount` (ตัวเลขอยู่ใน `[PAY≠TOTAL]`) |
| W2 | `BuildScanLinesAsync` `OcrService.cs:6604` (gross) · `:6798` (ส่วนลดรายบรรทัด) · `:6842` (สาขาไม่มีรายการ) | ยอดก่อนลดต่อบรรทัด = `LineGross` ตัวเดียวทั้งกระทบยอด/กระจาย/ส่วนลดบรรทัด · ไม่มีรายการ + ตารางกลุ่มพิสูจน์แล้ว (≥2 กลุ่ม · รู้ชนิดทุกกลุ่ม · รวม=ยอดรวม · VAT=VAT หัว · ฐาน=ฐานหัว · ไม่มี WHT) ⇒ บรรทัดสรุป**ต่อกลุ่ม** แล้วเดินด่าน `OcrAmountIntegrity` เดิม · ไม่เข้าเงื่อนไข ⇒ บรรทัดเดียวแบบเดิม |
| W3 | `OcrService.cs:6037` (สร้าง) · `:7019` (พรีวิว) · `:7234` (repopulate) → `ScanBillDiscount` | ส่วนลดที่ตัวสร้างบรรทัด/`ResolveHeaderSubTotal` ใช้ = `OcrTotalDecomposer.ForScan` ตัวเดียว (อ่านจากข้อความดิบตอนสร้าง ⇒ สแกนเก่าที่ persist 98 ไว้ก็ได้ 0 **และได้แท็ก `[PAY≠TOTAL]` ต่อท้ายหมายเหตุสแกน** — รอบฝ่ายค้าน P1) |
| back-calc | `OcrService.cs:5499` (`Decide(..., totalAmount:)`) · `SmartFieldExtractor.cs:519-526` | ห้ามแต่ง VAT ที่ขัดกับ VAT ที่พิมพ์ · ปัด `AwayFromZero` (พิสูจน์ได้ว่าไม่มีค่าไหนเปลี่ยน: T/1.07 ของยอด 2 ตำแหน่งไม่มีวันลงที่ .xx5 พอดี) |
| หน้าเว็บ | `document-scan.html:1527-1530` `parseScanNotes` | `pick()` 4 บรรทัด (`TOTAL-CONFLICT` · `PAY≠TOTAL` · `TOTAL-UNSURE` · `PAGES-PARTIAL`) — ข้อความ/ตัวเลขมาจากเซิร์ฟเวอร์ |
| LINE | `LineBotService.cs:729-732` `OcrScanWarnings` | แท็กชุดเดียวกับหน้าเว็บ (คอมเมนต์ของเมธอดเขียนไว้เองว่าต้องเป็นชุดเดียวกัน — F2 ข้อ 1 คู่สมมาตร) |

## 3. ก่อน/หลัง — ใบจริง 3 ใบ (ก่อน = ผลของทีม B · หลัง = จำลอง regex จริง + ไล่มือตามโค้ดที่แก้)

| ใบ / เส้น | ก่อน: Total · ฐาน · VAT · บรรทัด · แท็ก | หลัง: Total · ฐาน · VAT · บรรทัด · แท็ก |
|---|---|---|
| **M** ข้อความ (Tesseract) | **24,110.00** · 22,532.71 · **1,577.29 (แต่ง)** · 1 บรรทัด 7% · **ไม่มีแท็ก ⇒ อนุมัติเองได้** | back-calc ถูกห้าม (พิมพ์ 1,148.28 ≠ 1,577.29) · anchor **Proven 23,812.25** (VAT+ตัวอักษร+ป้าย AMOUNT/NET AMOUNT+แถวชำระ = 4 ชั้น · 24,110 = ยอดก่อนหักส่วนลด 297.75) · ฐาน/VAT จากแถว "รวม" = **22,663.97 / 1,148.28** · สร้าง: `NetSubTotal` = 22,663.97 · **2 บรรทัด**: ยกเว้น 6,260.00 (−1) · 7% 16,403.97 VAT 1,148.28 · `OcrAmountIntegrity` ผ่าน · `[TOTAL]` (ข้อสังเกต) + `[PAGES-PARTIAL]` ขาดหน้า 1, 2 |
| **M** Azure (InvoiceTotal 24,110) | 24,110 · 22,961.72 · 1,148.28 · 1 บรรทัด 7% · `[MATH]`+`[Σ-GAP]` | **23,812.25** · 22,663.97 · 1,148.28 · 2 บรรทัดกลุ่ม · `[TOTAL]` + `[PAGES-PARTIAL]` (ด่านคณิตลงตัว — ใบผสมเป็นข้อสังเกต) |
| **U** ข้อความ/Azure (536) | 536 · 500.93 · 35.07 · ส่วนลด 98 **อ่านแล้วทิ้งเงียบ** · ไม่มีแท็ก | 536 (Confirmed) · 500.93 · 35.07 · ส่วนลดของสแกน = ว่าง · **`[PAY≠TOTAL]` "ยอดตามใบกำกับ 536.00 · ยอดที่ชำระจริง 438.00 (ต่าง −98.00 · ค่าจัดส่ง +37.00 · Shopee Voucher −135.00)" — ห้ามอนุมัติเอง** · บรรทัด Azure 2×268 = เคส A |
| **U** Azure หยิบ 438 | **438** · 402.93 · 35.07 · เคส **E** (ลงตัวแต่ผิดเรื่อง) · `[Σ-GAP]` คำใบ้ผิดทิศ | anchor **Proven 536** (438 = ยอดที่ชำระหลังส่วนลด/ปรับ 98 นอกใบกำกับ) · ฐาน 500.93 · ส่วนลดที่ใช้ได้ 0 ⇒ เคส A · `[TOTAL]` + `[PAY≠TOTAL]` |
| **S** ข้อความ/Azure | 5,024 · 4,695.33 · 328.67 · (Azure) ส่วนลดบรรทัด 216.83 | 5,024 (Confirmed) · เท่าเดิม · `PreVat` ส่งส่วนลด 216.82 เท่าเดิม · (Azure) gross = **4,912.15** (ยอดพิมพ์) ⇒ ส่วนลดบรรทัด **216.82** ตรงกระดาษ · ไม่มีแท็กใหม่ |

## 4. ก่อน/หลัง — ใบที่ต้อง "ยังถูก" (กฎเหล็ก #4 H)

| ใบ | anchor | ยอดรวม | ส่วนลดที่ส่งต่อ | แท็กใหม่ | เปลี่ยน? |
|---|---|---|---|---|---|
| Wine Pro 3,593 | Confirmed (ป้าย Total · แถว Card · ตาราง V 7) | 3,593.00 | 0 (None) · ตารางกลุ่มเดียว ⇒ ไม่แยกบรรทัด | – | ไม่ |
| ร้านวัสดุ 1,418.02 | Confirmed (ตัวอักษร · ป้าย · 1,325.25+92.77) | เดิม | 69.75 (PreVat) ⇒ เคส C เดิม | – | ไม่ |
| ซูเปอร์ 735.30 | Confirmed (ยอดสุทธิ · 687.20+48.10) | เดิม | 38.70 (InclVat) ⇒ เคส E เดิม | – | ไม่ |
| ค้าส่งผสม V/N 792 | Unknown (ไม่มีชั้น VAT ทั้งใบ) | เดิม | 0 | – | ไม่ (`OcrLineVatMarks.Assign` ยังติดอัตรา) |
| Makro 951/49/1,000 | Confirmed | 1,000.00 | 0 | – | ไม่ |
| ลักกี้เวย์ (หลังสลับ 1,070 / ก่อนสลับ 1,000) | Unknown ทั้งคู่ (ชั้นนี้ไม่สลับเอง — `OcrHeaderAmounts.Normalize` ตัวเดิม) | เดิม | 0 | – | ไม่ |
| IKEA 1,396 ราคารวม VAT | Confirmed | เดิม | 0 | – | ไม่ |
| หัก ณ ที่จ่ายที่พิมพ์ (10,700 / ยอดชำระสุทธิ 10,400) | Unknown — 10,400 มีป้ายชั้นเดียว **ไม่เคยชนะ** | 10,700.00 | 0 | – | ไม่ |
| มัดจำแถว 0.00 / ใบมัดจำจริง | Unknown (ใบมัดจำจริงไม่ถูกตัดสินเลย) | เดิม | 0 | – | ไม่ |
| ส่งออก 0% | Unknown (VAT 0 ไม่มีชั้น VAT) | เดิม | 0 | – | ไม่ |
| แถวเงินสด 1,000 / เงินทอน 49 | Confirmed 951 · 1,000/49 ไม่เป็นผู้สมัคร | เดิม | 0 | – | ไม่ |

ล็อกด้วย `OcrReplayGoldenTests.ใบเดิมทุกใบในชุด_ยอดรวมและฐานหลังขั้นยึดยอด_เท่าเดิมทุกใบ` (ทุกใบเดิมใน corpus: `AnchorVerdict ∈ {Confirmed, Unknown}` · `AnchorTotal = HeaderTotal` ·
`AnchoredNetSubTotal = NetSubTotal` · `DiscountToSpread = BillDiscount`) — ช่องเดิมของ replay **ไม่เปลี่ยน** (ช่องใหม่เป็นแถวใหม่เท่านั้น)

## 5. เทสต์ที่เพิ่ม (สองครึ่งทุกไฟล์ · ข้อความกระดาษ M/U/S ใน `OcrPaperSamples.MakroPage3of3/UptoyouShopee/ScommerceLazada`)

| ไฟล์ | จำนวน | พัง→ถูก | ห้ามแตะ |
|---|---|---|---|
| `OcrTotalAnchorTests` (ใหม่) | 15 Fact + 2 Theory (10 InlineData) | M 24,110→23,812.25 (role ก่อนลด · คู่ 22,663.97/1,148.28 · 4 ชั้น) · M engine ว่าง ⇒ เติม · U 438→536 (AmountSettled) · Conflict 1,250 vs 1,520 ⇒ แท็กหยุดอนุมัติจริง | Wine Pro/ร้านวัสดุ/ซูเปอร์ Confirmed ยอดเดิม · ค้าส่ง · ลักกี้เวย์ ×2 · มัดจำ ×2 · หัก ณ ที่จ่าย · ส่งออก · Makro 951 · IKEA · เงินทอน · ป้ายเดียวไม่มี VAT · ข้อความว่าง · `[TOTAL]/[TOTAL-UNSURE]/[PAGES-PARTIAL]` **ไม่บล็อก** |
| `OcrTotalDecomposerTests` (ใหม่) | 12 Fact + 1 Theory (4) | U PostInvoice · หมายเหตุมีตัวเลข +37/−135 · ส่วนลดที่ใช้ได้ 0 ⇒ เคส A · ล็อกบั๊กเดิม (438+98 ⇒ เคส E) · S ส่วนลด 216.82 ไม่ใช่แถว (0.00) · S gross 4,912.15 ⇒ เคส C ถึงสตางค์ · M สองกลุ่มผ่าน `OcrAmountIntegrity` (บรรทัดเดียว 7% ไม่ผ่าน) | M ยอด 24,110 ⇒ ไม่แยกกลุ่ม · ร้านวัสดุ C · ซูเปอร์ E · Wine Pro None · ส่วนลดอธิบายไม่ได้ ⇒ Unknown ส่งต่อเท่าเดิม · `LineGross` ส่วนลดรายบรรทัด ⇒ สูตรเดิม |
| `OcrVatGroupTableTests` (ใหม่) | 13 Fact + 2 Theory (7) | ReadGroups Makro (ยกเว้น/มี VAT จากคำอธิบาย) · ไม่มีคำอธิบาย ⇒ พิสูจน์ด้วยตัวเลข · VAT พิมพ์ 1,148.28 ห้ามแต่ง 1,577.29 (`Decide(..., totalAmount:)` = false) · หน้า 3/3 ขาด 1, 2 | ตาราง Σ≠รวม / ไม่มีแถวรวม / แถวไม่ลงตัว / ไม่มีตาราง ⇒ ว่าง · รหัส 4 ไม่มีความหมาย ⇒ Unknown · คำอธิบายขัดตัวเลข ⇒ Unknown · `Read` เดิมไม่เห็นตาราง Makro · Wine Pro แยก VAT ได้ตามเดิม · ไม่มี VAT บนกระดาษ ⇒ ด่านเดิม · ไฟล์ครบ/ไม่รู้ ⇒ ไม่เตือน |
| `OcrReplayHarness` + `OcrReplayGoldenTests` | +3 ใบ · +5 ช่อง · +4 Fact | M/U/S | ทุกใบเดิม (ข้างบน) |

`python3 tools/test_inventory.py --row` (main agent วางทับ TEST_PLAN §0):
`**280 ไฟล์ · 2,327 [Fact] + 344 [Theory] (1,554 InlineData)**` (ก่อนรอบนี้ 277 · 2,283 · 339 · 1,533)

## 6. สิ่งที่ยืนยันไม่ได้โดยไม่มีคอมไพเลอร์ (ต้องดู CI)

- ตรรกะ: จำลองด้วย `scratchpad/totalsim.js` + `run.js` ที่**ดึง regex จากไฟล์ .cs จริง**ตอนรัน (กัน drift ระหว่างสำเนา) ครอบ ReadGroups · Anchor · Decomposer ·
  PageSet · PrintedVatContradicts กับ M/U/S + 13 ใบเดิม — ผลตรงกับทุก assert ในเทสต์ · ข้อต่าง .NET/JS ที่รู้: `\d` ของ .NET รับเลขไทย (JS ไม่รับ) —
  ข้อความตัวอย่างไม่มีเลขไทย · `ThaiAmountInWords`/`PaperWhtReader` ใน sim เป็นสำเนาย่อ
- จุดที่เสี่ยงชนิด/overload (เขียนเลี่ยงไว้แล้วแต่ยังไม่ผ่านคอมไพเลอร์): `Assert.Equal` ระหว่าง tuple ที่มี `int?` (ใส่ `(int?)17`) · `Assert.Equal(array, list)` (ใส่ `.ToArray()`) ·
  pattern `x is var g && g.Count > 0` ใน `else if` · target-typed conditional `cond ? null : a ?? b` · `is not decimal x ||` + ใช้ `x` หลัง `if`
- ผลบนสแกนจริง: ยังไม่มี ReasoningTrace ของ Azure กับสามใบ ⇒ แถว "Azure" ใน §3 เป็นค่าสมมติแบบเดียวกับทีม B · ข้อความ M เป็นการถอดจากภาพ ไม่ใช่ output Tesseract จริง
  (ถ้า Tesseract เรียงตารางรหัส ภ.พ. ผิดคอลัมน์ `ReadGroups` จะคืนว่าง ⇒ ถอยไปพฤติกรรมเดิม + ด่านยอดฟ้อง — ไม่แต่งตัวเลข)

## 7. Doc ที่ main agent ต้องขยับ (ทีมห้ามแตะ)

- **DOCUMENT_FLOW.md** ทางเข้า OCR: (1) ขั้นใหม่ "ยึดยอดรวมทั้งสิ้น" ก่อนด่านคณิต (`OcrTotalAnchor` · เขียนทับเฉพาะ Proven) (2) นิยาม `Document.TotalAmount` = ยอดใบกำกับ (ยอดที่ VAT ถูกคิด) ไม่ใช่ยอดที่จ่าย
  (3) แท็กใหม่ `[TOTAL]` · `[TOTAL-CONFLICT]` (บล็อก) · `[TOTAL-UNSURE]` · `[PAY≠TOTAL]` (บล็อก) · `[PAGES-PARTIAL]` (4) ส่วนลดหลังยอดใบกำกับไม่ลดฐาน/VAT และไม่ถูกเก็บเป็น `ExtractedDiscountAmount`
  (5) ไม่มีรายการ + ตารางกลุ่มภาษี ⇒ บรรทัดสรุปต่อกลุ่ม (6) back-calc 7/107 ทั้งสองชุดห้ามขัดกับ VAT ที่พิมพ์ (7) ยอดบรรทัดที่พิมพ์ชนะเศษปัดราคาต่อหน่วย
- **CLAUDE.md กฎเหล็ก #3 ข้อ 2** fallback chain ขั้น "สกัดจากข้อความ": เติม `OcrTotalAnchor`/`OcrTotalDecomposer`/`OcrLineVatMarks.ReadGroups` (แตะเฉพาะเมื่อ main agent เห็นว่าเปลี่ยนหลักการ) · ส่วนรายชื่อ helper ใน §OCR_PIPELINE_REVIEW ของ CLAUDE.md
- **TEST_PLAN.md** §0 แถว §5 + ไฟล์เทสต์ใหม่ 3 ไฟล์ + ช่อง replay ใหม่ 5 ช่อง
- **OCR_PIPELINE_REVIEW / DECISION_DOCTRINE backlog**: `OcrLineSplitGuard` ไม่รู้จักส่วนลด (S เส้นข้อความยังได้บรรทัดสรุปเดียว) · `ThaiVatTypeRule` "นม" ⇒ นมผงสูตรเด็กเป็นยกเว้น (S เส้น Azure ที่ไม่คืน TaxRate — ตอนนี้ดังด้วย `[Σ-GAP]` ไม่เงียบ) ·
  ตัวอ่านค่าขนส่ง `EnrichFromRawText` ไม่กระทบยอด (U รอดเพราะ `+` คั่น) · SummaryMarkers ขาด `รวม`/`รวมเป็นเงิน`/`ยอดเงินรวม`/ช่องทางชำระ · `PreviousUnpaidBalance` dead field · ส่วนลดรายบรรทัดของบรรทัดที่ไม่มีราคาต่อหน่วย
  (สูตรเดิม `Amount_หลังกระจาย × Qty − Amount` = 0 แต่ `DiscountPercent` > 0 — **ไม่ได้แก้รอบนี้** เพื่อไม่เปลี่ยนพฤติกรรมเกินโจทย์)
- **docs/lessons/ocr-pipeline.md**: *"ลงตัวทางคณิตไม่พอ ต้องถามว่า VAT ถูกคิดจากยอดไหน"* (U เข้าข้อ 0 ของด่านและเคส E ได้ทั้งที่ผิดเรื่อง) · *"ป้าย TOTAL ภาษาอังกฤษไม่ใช่ยอดรวมทั้งสิ้นเสมอ — นับชั้นหลักฐานอิสระ + บังคับอธิบายผู้แพ้"*

## 8. คำถามเจ้าของ (ไม่เดาแทน)

1. **ส่วนต่าง 98 ของใบ U ลงบัญชีอย่างไร** — (ก) คูปองแพลตฟอร์ม 135 = รายได้อื่น/ส่วนลดรับ (ไม่ลดฐาน VAT) · ค่าจัดส่ง 37 = ค่าใช้จ่ายไม่มีใบกำกับ (ข) ตัดสุทธิ −98 บัญชีเดียว (ค) ให้ผู้ใช้เลือกทุกครั้ง ·
   วันนี้: เอกสารลง 536 · `[PAY≠TOTAL]` บล็อกอนุมัติเอง · ยอดจ่าย 438 อยู่ในหมายเหตุเท่านั้น (ไม่มีคอลัมน์ `AmountSettled` — phase 2 ต้อง migration + DTO echo ตาม #4 B) ·
   ⚠️ ถ้าเอกสารปลายทางเป็นชนิด "จ่ายแล้ว" (PV/Receipt) `PaidAmount` = 536 ≠ เงินออก 438 — ผู้ใช้ต้องแก้ตอนตรวจ (ด่านบล็อกไว้แล้ว)
2. **`[PAGES-PARTIAL]` ควรบล็อกการอนุมัติเองไหม** — ยอด/VAT พิสูจน์ได้จากหน้าสรุป แต่ต้นฉบับ ม.87/3 ไม่ครบชุด (วันนี้ = ข้อสังเกต ไม่บล็อก)
3. **บรรทัดสรุปรายกลุ่ม VAT** ("… — สินค้ายกเว้น VAT (รหัส 1 บนกระดาษ · 17 ชิ้น)") ยอมรับเป็นรายการในเอกสารซื้อไหม — ถ้าไม่ ผู้ใช้ต้องแยกเองจากหน้า 1–2
4. `[TOTAL-UNSURE]` (ตัวอักษร+ป้ายชี้ยอดอื่น แต่ไม่มีชั้น VAT — ใบไม่มี VAT) ควรบล็อกไหม — วันนี้ไม่บล็อก (conf ≤ 0.80 = เหลือง)

## 9. F3 ข้อ 7–12 (ตอบในข้อความคอมมิตด้วย)

7. รูปแบบเดิมที่แก้: `ExtractedDiscountAmount ?? 0` ที่ตัวสร้าง **3 จุด → 0** (ทุกจุดผ่าน `ScanBillDiscount`) · สูตร gross `UnitPrice*Qty : Amount` ใน `BuildScanLinesAsync` **2 จุด → 0** (`LineGross`) ·
   back-calc 7/107 **2 จุด** — ทั้งคู่ถามด่านเดียวกัน · `ExtractedTotalAmount ?? 0` ผู้บริโภค T13 ไม่แตะ (ได้ยอดที่ยึดแล้วจากต้นทาง)
8. ทางเข้า: สแกนเว็บ (ทุก engine ยกเว้น e-Tax XML ที่ลงนาม) ✓ · สร้าง/พรีวิว/repopulate ✓ (W3) · LINE การ์ด ✓ (`OcrScanWarnings` + `OcrPostingReadiness`) · LINE postback ✓ (readiness ตัวเดียว) ·
   ค่าพาร์ทเนอร์ `ApplyExternalAmountOverrides` รันหลัง ⇒ ชนะ anchor (ตั้งใจ) · มือถือ/API/อนุมัติมือ = คนตัดสิน
9. เข้มขึ้น: `[TOTAL-CONFLICT]` · `[PAY≠TOTAL]` หยุดอนุมัติเอง — ทางไปต่อ: เปิด Draft ตรวจยอดกับกระดาษแล้วกดอนุมัติเอง · ทิศตรงข้าม: `ข้อสังเกต_TOTAL_และ_TOTAL_UNSURE_และ_PAGES_PARTIAL_ไม่บล็อก` ·
   `ใบในชุดreplayเดิม_ไม่มีคำตัดสินที่แตะค่า` · `ใบเดิมทุกใบในชุด_ยอดรวมและฐานหลังขั้นยึดยอด_เท่าเดิมทุกใบ` · `ร้านวัสดุ…เคสCเท่าเดิม` · `ซูเปอร์…เคสEเท่าเดิม`
10. ค่าที่ persist: สแกนเก่าที่เก็บยอด 24,110 ยังผิดอยู่ (ขั้นยึดยอดรันตอนสแกน) — ตอนสร้างเอกสาร ด่านยอด `[Σ-GAP]` ฟ้อง (ดัง ไม่เงียบ) · ทางแก้: สแกนใหม่ · **ไม่มี migration** ·
    สแกนเก่าที่เก็บส่วนลด 98 ของใบ U ได้ผลใหม่ทันที (W3 อ่านจากข้อความดิบ) · ⚠️ dedup (เลขที่+ยอด) ของสแกนใหม่จะไม่จับสแกนเก่าที่เก็บยอดผิดไว้
11. ไม่มีเครื่องมือเรียก subagent ฝ่ายค้านใน session นี้ — ตอบ 3 คำถามเอง: ทางเข้าอื่น (ข้อ 8) · ทิศตรงข้าม (ข้อ 9) · สถานะปลายทางประทับเอง = **ไม่มี** (เขียนแค่ยอด/แท็ก ไม่ประทับสถานะ ·
    เขียนทับยอดเฉพาะเมื่อพิสูจน์ด้วยตัวเลขที่พิมพ์ ≥2 ชั้นรวม VAT และอธิบายค่าเดิมได้) · kill-switch: ไม่เรียก AI เลย
12. DOCUMENT_FLOW / TEST_PLAN ต้องขยับ — §7

---

## 10. รอบฝ่ายค้าน (main agent ส่งผลตรวจ read-only + Python regex mirror มา · verify กับโค้ดทุกข้อก่อนแก้)

ยืนยันด้วย JS mirror ที่ดึง regex จากไฟล์ .cs จริง (แก้ตัว mirror ให้ข้ามคอมเมนต์ `//` และแทนค่าคงที่ `OriginalDocWords` ด้วย — ไม่งั้น
mirror ผิดเอง) · ทุกข้อ **ยืนยันว่าจริง** — ไม่มีข้อที่ไม่เห็นด้วย มีแต่ 2 จุดที่เลือกทางแก้ต่างจากข้อเสนอ (หมายเหตุท้ายตาราง)

| ข้อ | ยืนยัน | แก้อย่างไร (ไฟล์) | เทสต์ (พัง→ถูก / ห้ามแตะ) |
|---|---|---|---|
| C1 ใบปัดเศษ ⇒ `[TOTAL-CONFLICT]` ปลอม | ✅ 1,234.00 × 7/107 = 80.73 ≈ 80.75 (±0.02) ⇒ สองยอด "พิสูจน์ได้" | (1) ชั้น VAT แยก **ตรงเป๊ะ** (ฐานพิมพ์+VAT · ตารางกลุ่ม) กับ **อัตราส่วน** (`VatRatio` ±0.01) — มีผู้สมัครตัวใดตรงเป๊ะ ⇒ อัตราส่วนไม่นับเป็นชั้น VAT ของตัวอื่น (2) บทบาทใหม่ `RoundedPayable` เมื่อส่วนต่าง < 1 บาท = แถว "ปัดเศษ" ที่พิมพ์ (ตัวอ่านเดิม `ThaiBillSurcharge`) (3) สองตัวที่พิสูจน์ได้แต่อธิบายกันได้ ≠ ขัดกัน · `OcrTotalAnchor.cs` | `C1_ใบปัดเศษ…ไม่มีConflict` · `C1_engineหยิบยอดชำระหลังปัดเศษ…` (Proven 1,234.25) / `C1_ทิศตรงข้าม_…ไม่มีแถวปัดเศษ…ยังดัง` |
| C2 สองสกุลเงิน · ใบลดหนี้อังกฤษ | ✅ ทั้งสองแบบ Conflict · และ `excl(?:uded\|usive)` พลาด "excluding"/"exclusive of" | `OcrPaperAmounts`: ตัดแถว VAT "before VAT/excluding/exclusive of/incl…of VAT" · แถวของใบเดิม (`original/previous/prior/เดิม` — ค่าคงที่ `OriginalDocWords` ตัวเดียวใช้ทั้งแถว VAT และป้ายยอดรวม) · `HasForeignCurrency` (รหัส/สัญลักษณ์สกุลต่างประเทศ · "exchange rate/อัตราแลกเปลี่ยน" · แถวยอดบางแถวติด THB แต่แถวยอด/VAT อื่นไม่ติด) ⇒ anchor/decomposer = **Unknown** (ไม่ใช่ Conflict) | `C2_ใบสองสกุลเงิน_ไม่รู้_ไม่ใช่ขัดกัน` ×3 · `C2_ใบลดหนี้…Confirmed` ×2 (อังกฤษ/ไทย) · `แถวที่ไม่ใช่ยอดVATของใบนี้…` · `ใบหลายสกุลเงิน_ตรวจเจอ_ใบไทยทั่วไปไม่เข้า` ×4 · `C2_ใบสองสกุลเงิน_ส่วนลดไม่ถูกเดาทิศ` |
| C3 อัปไฟล์ซ้ำทิ้งแท็กใหม่ | ✅ และพบว่า**ของเดิม** `[MATH]`/`[DATE-UNSURE]`/`[FX-UNKNOWN]` ก็หลุดด้วย (ลิสต์มือแยกจาก BlockingTags) | `OcrScanSnapshot.DecisionNoteTags` = ลิสต์เดิม ∪ **ทุก** `OcrPostingReadiness.BlockingTags` ∪ `[TOTAL-UNSURE]`/`[PAGES-PARTIAL]` (ตัวตั้งตัวเดียว) · `[TOTAL]` ไม่ยก (ค่าที่ยึดถูกคัดลอกแล้ว และคำเดียวกันอยู่ในบรรทัด `[Reasoning]`) | `แท็กห้ามอนุมัติทุกตัว_ติดไปกับสำเนา…` / `ข้อสังเกตยึดยอด_TOTAL_ไม่ถูกยกไป…` + เทสต์เดิม 3 ตัวผ่านเหมือนเดิม |
| C4 ฐานคำนวณถูกประทับ PaperLabel 0.95 | ✅ | ทาง 7/107 ไม่ใส่ `PrintedBase` · ตัวต่อสายเขียนตาม `OcrTotalAnchor.Plan` (ใหม่ — แยกออกจาก `ApplyTotalFirst` ให้เทสต์ได้): ฐานพิมพ์ = PaperLabel 0.95 · ฐานคำนวณ = **Rule 0.80** (เหลือง) | `C4_ฐานที่คำนวณ…ไม่ถูกประทับ…` · `Plan_Makroเส้นข้อความ…PaperLabel` / `Plan_ทิศตรงข้าม…ไม่แตะฐาน_Confirmedไม่เขียนอะไร` |
| C5 แถวเดียวนับสองชั้น | ✅ | หลักฐานทุกชิ้นมี `LineNo` · นับชั้นแบบ "บรรทัดที่ยังไม่ถูกชั้นอื่นใช้" (ตัวอักษร = −1 ไม่ผูกบรรทัด) | `C5_แถวรวมของตารางแถวเดียว_นับเป็นหลักฐานชั้นเดียว` / M ยังได้ 4 ชั้น (`Makro_ป้ายTOTAL…`) |
| P1 สแกนเก่าไม่มี `[PAY≠TOTAL]` | ✅ (เส้นสร้าง/repopulate อ่านข้อความดิบได้ส่วนลด 0 แต่ไม่มีแท็ก ⇒ LINE/เว็บอนุมัติเองได้) | `OcrTotalDecomposer.ForScan` (แทน `EffectiveBillDiscount`) คืน ส่วนลดที่ใช้ได้ + หมายเหตุที่สแกนยังขาด · `ScanBillDiscount` ต่อท้ายลง `result.ProcessingNotes` (เส้นสร้าง tracked ⇒ บันทึก · พรีวิว AsNoTracking ⇒ โชว์ใน delta ไม่บันทึก) | `P1_สแกนเก่าไม่มีแท็ก…` / `P1_ทิศตรงข้าม_มีแท็กแล้วไม่ซ้ำ…` |
| P2 อัตรา 0 ⇒ ยกเว้นเสมอ | ✅ | `ReadGroups`: แถวอัตรา 0 ใช้คำอธิบายรหัสบนกระดาษ (อ่าน `LegendRow` เดิม) — ยกเว้น/0% ตามที่พิมพ์ · ไม่มีคำอธิบาย = Unknown · ตารางรหัสตัวเลขไม่มีคำอธิบาย + VAT 0 = Unknown (เดิม Exempt) | `แถวอัตราศูนย์_ตัดสินจากคำอธิบาย…` ×3 · `ไม่มีคำอธิบายรหัส_VAT7พิสูจน์ได้_แต่VATศูนย์ไม่รู้…` (เปลี่ยนคำตอบจาก Exempt — ตั้งใจ) · Makro (มีคำอธิบาย) ยัง Exempt/Standard7 |
| P3 "VAT RATE 7.00" | ✅ | แถวที่มีคำว่า rate/อัตรา และตัวเลขท้ายแถว = 7.00 ไม่ใช่ยอด VAT | `แถวอัตราVAT_ไม่ทำให้แยกVATที่ถูกต้องถูกปฏิเสธ` |
| P4 คอมเมนต์ผิด | ✅ | doc ของ `OcrTotalAnchor` บอกตรงตามโค้ด: ป้ายสลับซ่อมใน `SmartFieldExtractor` (`IsSwapped`) ของ engine ที่ผ่าน Enrich — ไม่ใช่ "Normalize รันก่อนเสมอ" | – |
| P5 `LineGross` กลืนส่วนลดที่จำนวนมาก | ✅ | เพดานคงที่ `LinePrintedTol` = 0.01 (ใบ S ต่าง 0.01 พอดีจึงยังได้ 4,912.15) | `[InlineData(100, 10, 999.50 → 1000)]` / `(3, 33.33, 100.00 → 100)` · S เดิมผ่าน |

**จุดที่เลือกทางแก้ต่างจากข้อเสนอ (ไม่ใช่ไม่เห็นด้วย):**
- **C1 ไม่ใส่ `[PAY≠TOTAL]` ให้ใบปัดเศษ** — ส่วนต่าง < 1 บาทที่กระดาษพิมพ์แถว "ปัดเศษ" เป็นการปัดเงินสดปกติของร้านค้าปลีก ไม่ใช่ส่วนลด/คูปองนอกใบกำกับ
  (ไม่มีคำถามเรื่องบัญชีปลายทางที่ต้องรอเจ้าของ) · ถ้าบล็อกจะฟ้องใบเงินสดถูกต้องแทบทุกใบ = ปิดด่านโดยไม่ตั้งใจ (F2 ข้อ 8) ·
  ส่วนต่างที่**ไม่มี**แถวปัดเศษ + engine หยิบยอดที่ไม่ใช่ยอด VAT ⇒ ยัง Conflict (ดัง) — เทสต์ทิศตรงข้ามล็อกไว้
- **C2 ใบสองสกุล = Unknown ทั้งใบ** แทน "เลือกเฉพาะแถว THB" — การเลือกแถวต้องเดาว่าแถวที่ไม่ติดป้ายเป็นสกุลไหน (ตัวอย่างของฝ่ายค้านเองมี `Total 1,070.00`
  ไม่ติดป้าย) · Unknown = พฤติกรรมเดิม + ด่าน `[FX-UNKNOWN]` เดิม · ข้อเสีย: ใบไทยที่พิมพ์ "Total Amount (THB) …" บนแถวเดียวแต่แถว VAT ไม่ติดป้าย จะไม่ได้ประโยชน์จากขั้นยึดยอด
  (ถอยเป็นเดิม ไม่ผิด)

**ผลต่อใบที่ต้อง "ยังถูก"**: รันชุดเดิม 21 สถานการณ์ใน mirror ซ้ำ — คำตัดสินของ M/U/S/Wine Pro/ร้านวัสดุ/ซูเปอร์/ค้าส่ง/ลักกี้เวย์ ×2/Makro 951/มัดจำ ×2/
หัก ณ ที่จ่าย ×2/ส่งออก/IKEA/เงินทอน/Conflict **เหมือนรอบแรกทุกตัว** (ใบเงินทอนเปลี่ยนแค่ `PrintedBase` เป็น null เพราะฐาน 888.79 ไม่ได้พิมพ์ = C4)
· replay golden เดิมไม่ต้องแก้

**ยังตรวจไม่ได้โดยไม่มีคอมไพเลอร์**: target-typed tuple `(null, v.Amount)` ลง `Dictionary<int,(decimal?,decimal)>` · pattern `out var` ใน ternary ของ `ReadGroups` ·
`Assert.Equal(double, double?)` · static initializer ข้ามคลาส `OcrScanSnapshot.DecisionNoteTags` ← `OcrPostingReadiness.BlockingTags` (ไม่มีวงวน)

`python3 tools/test_inventory.py --row` หลังรอบนี้: `**280 ไฟล์ · 2,341 [Fact] + 348 [Theory] (1,568 InlineData)**`
