# ตัวตนคู่ค้าจาก OCR — ผลตรวจรอบ 173 + แผนชั้นป้องกัน

> ที่มา: ใบของ "บริษัท ดีแคทลอน (ประเทศไทย) จำกัด" (เลขภาษี 0115558007311)
> ระบบได้ชื่อผู้ขาย = **"DECATHLON"** (ข้อความโลโก้) แล้วเอาไปเป็น "ผู้รับเงิน" บนใบสำคัญจ่าย
>
> รอบนี้ตั้งทีม 3 ด้าน (ฝ่ายค้าน · กฎหมายภาษี · สถาปนิกชั้นป้องกัน) แล้ว **main agent เปิดไฟล์ยืนยันเองทุกข้อที่นำมาเขียน**
> ข้อที่ยืนยันไม่ได้ ระบุไว้ใน §6

---

## 1. สิ่งที่ผมเสนอไปก่อนหน้านี้ — และทำไมมันใช้ไม่ได้

**ข้อเสนอเดิม:** เทียบชื่อ OCR กับ **ชื่ออังกฤษ** ของทะเบียนด้วย (วันนี้เทียบเฉพาะ `dbd.NameTh`)
วัดแล้วได้ผลสวย: เทียบชื่อไทย = **0.000** · เทียบชื่ออังกฤษ = **0.779** ผ่านเกณฑ์ 0.45 สบาย

**แต่ฝ่ายค้านทุบถูกจุด และผมยืนยันแล้วว่าจริง:**
`DbdLookupService.LookupRdVatAsync` คืน **`NameEn: null` แบบฮาร์ดโค้ด** (`DbdLookupService.cs:369`)
และผู้เรียกที่ `:107-111` **คืนค่าทันที**เมื่อได้ `NameTh` โดยไม่แตะแหล่งอื่น
⇒ บริษัทที่จด VAT (เกือบทุกนิติบุคคล รวมดีแคทลอน) จะได้ `NameEn == null` **เสมอ**
⇒ **ข้อเสนอของผมเป็น no-op บนเคสที่ยกมาเอง**

> บทเรียน: ผมวัดคะแนนกับ "ชื่ออังกฤษที่สมมติว่ามี" แทนที่จะเช็คก่อนว่าแหล่งข้อมูลจริง**ส่งช่องนั้นมาไหม**
> — ตรงกับกติกาของโปรเจกต์เอง "ก่อนเชื่อว่ามีของ ให้ `grep` ว่าใครเป็นคนเขียนค่านั้น"

---

## 2. และผมยังรายงานผิดอีกข้อ — ต้องแก้

ผมบอกไปว่า "จอแสดง 94% พิสูจน์ว่าการค้นทะเบียนไม่เคยได้ข้อมูลกลับมา" — **ข้อสรุปนั้นผิด**

`FieldConfidenceJson` ถูก serialize **ครั้งเดียวที่บรรทัด 1231** ส่วน DBD ทำงานที่บรรทัด **2159**
⇒ ค่าความมั่นใจที่ `EnrichFromDbdAsync` ตั้ง (ทั้ง 0.30/0.50 ตอนเตือน และ 1.0 ตอนทะเบียนชนะ)
**ไม่เคยถูกบันทึกลงฐานเลย** โผล่แค่ใน response ของการสแกนสดวินาทีเดียว พอรีเฟรชก็กลับเป็นค่าก่อน DBD

⇒ ตัวเลข 94% บนจอ **บอกอะไรเกี่ยวกับ DBD ไม่ได้เลย** · ตัวที่ใช้วินิจฉัยได้จริงคือบรรทัด `[DBD]` ใน Processing Notes

---

## 3. ต้นเหตุจริง — เราถามผิดคำถาม

ระบบพยายามตอบว่า **"ชื่อสองอันนี้คล้ายกันพอจะเป็นบริษัทเดียวกันไหม"**
แต่คำถามที่ตอบได้จริงคือ **"กุญแจที่ใช้ค้นทะเบียน (เลขผู้เสียภาษี) เชื่อถือได้ไหม"**

เพราะเมื่อกระดาษพิมพ์ชื่อคนละสคริปต์กับทะเบียน (โลโก้ละติน ↔ ชื่อนิติบุคคลไทย)
คะแนนความคล้ายจะเป็น **0.000 ซึ่งเท่ากับกลุ่ม "คนละบริษัท" เป๊ะ** (doc ของด่านเองระบุกลุ่มนั้นไว้ที่ 0.000–0.087)
⇒ **ช่องว่างระหว่างสองกลุ่มยุบเป็นศูนย์ ไม่มีเกณฑ์ตัวเลขใดแยกได้อีกต่อไป**

เทสต์ `DbdNameGuardTests` ล็อกเกณฑ์ 0.45 ไว้ด้วยคู่ทดสอบ **ไทย-ไทยล้วนทั้ง 8 คู่** ไม่มีคู่ข้ามสคริปต์สักคู่
และ docstring ของไฟล์นั้น**เขียนเตือนบทเรียนนี้ไว้เอง** ว่า "ถ้าวันหนึ่งสองกลุ่มทับกัน แปลว่าเกณฑ์ใช้ไม่ได้แล้ว"
— วันนั้นคือวันนี้

---

## 4. ทางที่ควรไป — ยืนยัน "กุญแจ" แทนการเดา "ชื่อ"

ให้ทะเบียนชนะได้เมื่อเข้าเงื่อนไขทั้งสอง แทนที่จะให้ similarity ตัดสิน:

1. `ThaiTaxId.IsJuristic(VendorTaxId)` — checksum ผ่าน **และ** ขึ้นต้นด้วย 0 (`Helpers/ThaiTaxId.cs:69`)
   (วันนี้ `EnrichFromDbdAsync:4465` ตรวจแค่ `Length != 13` ⇒ เลขบัตรประชาชน/บาร์โค้ดก็ยิงออกเน็ต)
2. **เลขนั้นปรากฏบนกระดาษจริง** ด้วย `RdComplianceValidator.RawTextHasTaxId` (`:434`)
   — เส้นผู้ซื้อใช้ตัวนี้อยู่แล้วที่ `OcrService.cs:2141` จึงไม่ต้องสร้างของใหม่

เมื่อกุญแจถูกยืนยัน ชื่อทะเบียนชนะได้ **แม้กระดาษจะมีแค่โลโก้** โดยไม่ต้องพึ่งการเทียบชื่อข้ามภาษาเลย
⇒ เคสดีแคทลอนจบ โดย **ไม่เปิดรูให้ชื่อสั้น** อย่าง "CP" / "SCG" / "AIS"

> รูนั้นมีจริงและยืนยันแล้ว: `DbdIdentityGuard.cs:79-85` ใช้ `Contains` ตัดสินเป็น `ExactMatch`
> **โดยไม่มีความยาวขั้นต่ำ** ⇒ ข้ามเกณฑ์ 0.45 ทั้งดุ้นทุกครั้งที่ชื่อบนกระดาษเป็นคำสั้น
> (พี่น้องในเรพมีด่านนี้แล้ว: `Helpers/OcrPartyName.cs:29` บังคับความยาว ≥ 3)

---

## 5. ชั้นป้องกันที่ควรเพิ่ม (เรียงตามผลต่อความเสี่ยง)

| # | ชั้น | ตอบคำถามที่ชั้นอื่นตอบไม่ได้ | สถานะวันนี้ |
| --- | --- | --- | --- |
| 1 | **ยืนยันกุญแจก่อนให้ทะเบียนชนะ** (§4) | "เลขที่ใช้ค้น เชื่อได้ไหม" | ไม่มี — ตรวจแค่ความยาว |
| 2 | **เก็บชื่อบนกระดาษไว้เสมอ** ห้ามทับ | "ใบใบนี้พิมพ์ว่าอะไร" (รายงาน §87 ต้องใช้ค่านี้) | **ถูกทับถาวร** `OcrService.cs:4563` → `:4569` |
| 3 | **แยกช่อง ชื่อนิติบุคคล / ชื่อทางการค้า** | "ชื่อไหนใช้ยื่นแบบ ชื่อไหนใช้แสดง" | `Contact` มี `Name` + `NameEn` เท่านั้น |
| 4 | **ตรึงชื่อคู่สัญญาลงเอกสารตอนอนุมัติ** | "ใบที่ออกไปแล้วเขียนชื่ออะไร" | ไม่มี — `Document` เก็บแค่ `ContactId` ดึงสดทุกครั้ง |
| 5 | **บันทึกผลค้นทะเบียนลงฐาน** | "เคยค้นไหม ได้อะไร ชนะไหม" | ไม่มีคอลัมน์ DBD สักช่องบน `OcrScanResult` |
| 6 | **ด่าน "ชื่อนี้ไม่ใช่ชื่อนิติบุคคล"** | "เลขขึ้นต้น 0 แต่ชื่อไม่มี บริษัท/หจก./Co.,Ltd." | ไม่มี — ด่าน §65 ตรี ตรวจแค่ว่าง/ไม่ว่าง |
| 7 | **ให้ผลทะเบียนเดินผ่าน arbiter** | "ค่านี้มาจากไหน" ต้องตรงกับความจริง | ข้าม arbiter + สมุดที่มาถูกปิดที่ `:1242` ก่อน DBD ที่ `:2159` |
| 8 | **แยก "ค้นไม่เจอ" ออกจาก "ค้นไม่ได้"** | "เน็ตล่ม หรือ ทะเบียนไม่มีจริง" | แยกไม่ได้ — exception ถูกกลืนเป็น null เหมือนกัน |

---

## 6. ข้อที่ทีมเสนอแต่ผมยืนยันไม่ได้ / ไม่เห็นด้วย

| ข้อ | ท่าที |
| --- | --- |
| ทีมกฎหมายอ้างว่าเลขอนุมาตราที่ระบบ log ผิด (`RD-65ter(11)(18)` ควรเป็น `(18)` · `(13)` ควรเป็น `(10)`) | **ไม่แก้** — ขัดกับตาราง §65 ตรี ที่เขียนไว้ใน `CLAUDE.md` เอง สองแหล่งไม่ตรงกัน ต้องให้นักบัญชียืนยันก่อน ห้ามเดา |
| ข้อเสนอ "ลด confidence เมื่อค้นทะเบียนไม่เจอ" (ของผมเอง) | **ถอน** — เน็ตล่มจะทำให้ทุกใบติดเตือน ซึ่งเข้าข้อห้าม "คำเตือนที่ฟ้องใบถูกทุกใบ = ปิดด่านโดยไม่ตั้งใจ" และค่าที่ตั้งก็ไม่ถูกบันทึกอยู่ดี (§2) |
| ข้อเสนอ "ยุบเกณฑ์ 0.45 ไปใช้ `DbdIdentityGuard`" | **ทำได้แต่ไม่ใช่ refactor เปล่า** — ตัว normalize ของสองที่ไม่เหมือนกัน การยุบจะพลิกคำตอบของคู่ที่เทสต์ล็อกไว้เอง โดยเทสต์ไม่ฟ้อง (assert แค่ตัวเลข similarity ไม่ assert ผลลัพธ์) ⇒ ต้องรันชุดกระดาษจริงก่อน/หลัง ตามกฎเหล็ก #4 H |

---

## 7. ลำดับที่แนะนำ

```
เฟส 1 ทำให้เห็นความจริงก่อน (ไม่เปลี่ยนพฤติกรรม)
  • re-serialize FieldConfidenceJson หลังขั้น DBD  • เก็บผลค้นทะเบียนลงฐาน (ชั้น 5)
เฟส 2 ยืนยันกุญแจ (ชั้น 1) + หยุดทับชื่อบนกระดาษ (ชั้น 2)   ← ปิดเคสดีแคทลอน
เฟส 3 แยกช่องชื่อ + ตรึงชื่อลงเอกสาร (ชั้น 3, 4)             ← ปิดความเสี่ยง 50 ทวิ / ภ.ง.ด.3/53
เฟส 4 ด่านรูปชื่อนิติบุคคล (ชั้น 6) + ให้ทะเบียนเดินผ่าน arbiter (ชั้น 7)
```

เฟส 1 ไม่เปลี่ยนคำตอบของใบใด ๆ แต่ทำให้วัดได้ว่าเฟสถัดไปได้ผลจริงไหม — ควรทำก่อนเสมอ

---

---

## 8. ข้อที่ลึกที่สุดของรอบนี้ — วงจรเรียนรู้ไม่ปิด (ยืนยันเองทุกข้อ)

คำถามที่ควรถามไม่ใช่ "จะเทียบชื่อให้แม่นขึ้นยังไง" แต่คือ
**"ทำไมใบที่สองของผู้ขังรายเดิม ยังโง่เท่าใบแรก"**

| กลไก | ฝั่งเขียน | ฝั่งอ่าน | ใช้ได้จริงไหม |
| --- | --- | --- | --- |
| `VendorKnownGoodValues` (SellerName) | **เฉพาะ** `AzureDiPatternLearner.cs:184` | `VendorKnownGoodCorrector` | **ไม่** — ฝั่งอ่านถูกเรียกแค่ `OcrService.cs:430` (python) และ `:526` (tesseract) **ไม่เคยถูกเรียกบนเส้น Azure ซึ่งเป็น engine หลัก** |
| การแก้ชื่อของผู้ใช้ → known-good | — | — | **ไม่มี** — `VendorKnownGoodValues` ปรากฏใน `OcrService.cs` **0 ครั้ง** ⇒ ผู้ใช้แก้ชื่อกี่ครั้งระบบก็ไม่จำ |
| Historical lookup จาก `Contact` | — | — | **ไม่มีเลย** — ไม่มีบรรทัดไหนเอา `Contact.Name` มาเติม `VendorName` ทั้งไฟล์ |
| ด่านกรองคำตอบ AI ของชื่อผู้ขาย | — | `Helpers/OcrReviewGuard.cs:101` | **ไม่มีด่าน** — `Take("vendor_name", _ => (true, ""))` รับทุกสตริงที่ AI ตอบ |

> ⚠️ ข้อที่สามขัดกับ **CLAUDE.md กฎเหล็ก #3 ข้อ 2.6** ซึ่งเขียนว่า "ถ้า `SellerTaxId` เคยมีในระบบ →
> autofill `SellerName/Address/BranchCode` จาก `Contact` ล่าสุดของ vendor นั้น" — **ของนั้นไม่มีอยู่จริง**
> ⇒ ต้องแก้ doc หรือสร้างของให้ตรง doc (โค้ดเป็น ground truth)
>
> ⚠️ ข้อที่สี่ขัดกับ **กฎเหล็ก #1** ที่ห้ามใช้คำตอบ AI โดยไม่ validate กับ candidate set
> — และช่องนี้คือช่องที่กำหนดว่าเอกสารเป็นของใคร

**Litmus test ของงานนี้:** ปิด AI ทุกตัว + ปิดการค้นทะเบียน แล้วสแกน **ใบที่สอง**ของผู้ขายที่ผู้ใช้เคยแก้ชื่อไว้แล้ว
→ ต้องได้ชื่อที่ผู้ใช้ยืนยันไว้ จากประวัติของ tenant เอง
**วันนี้ตอบว่า ไม่ผ่าน** และนี่คือชั้นที่ขาดจริง ๆ ไม่ใช่สูตรเทียบชื่อ

---

## 9. ลำดับที่แนะนำ (ปรับหลังได้ผลทีมครบ)

```
ชุด A — เล็ก วัดผลได้ทันที ไม่แตะ schema
  A1 ยืนยันกุญแจก่อนให้ทะเบียนชนะ (§4)          ← ปิดเคสดีแคทลอน
  A2 ด่านชื่อผู้ขายใน OcrReviewGuard              ← ปิดรูที่ AI ตอบอะไรก็รับ
  A3 เรียกตัวซ่อมจากประวัติบนเส้น Azure ด้วย      ← ของมีอยู่แล้ว แค่ไม่ถูกเรียก
  A4 ผู้ใช้แก้ชื่อ → เขียน known-good             ← ปิดวงจรเรียนรู้
ชุด B — ต้อง migration
  B1 แยกช่อง ชื่อบนกระดาษ / ชื่อทะเบียน + เลิกทับ
  B2 เก็บผลค้นทะเบียนลงฐาน + re-serialize confidence หลังขั้น DBD
ชุด C — ต้องรันชุดกระดาษจริงก่อน/หลัง (กฎเหล็ก #4 H)
  C1 ให้ทะเบียนเดินผ่าน arbiter + ย้ายจุดถ่ายรูปสมุดที่มาไปหลัง DBD
```

ชุด A ทั้งสี่ข้อรวมกันเล็กกว่าที่คิด เพราะสามในสี่เป็น "ของมีอยู่แล้วแต่ไม่ถูกเรียก"
ไม่ใช่การสร้างของใหม่

---

## 10. ลงมือแล้ว — รอบ 174 (`<pending>`)

| ข้อ | ทำอะไร | ไฟล์ |
| --- | --- | --- |
| ✅ A1 | `Helpers/OcrVendorKeyEvidence` (ใหม่ · ฟังก์ชันบริสุทธิ์) ตัดสินว่า "กุญแจถูก" จาก **กระดาษ**: checksum + ไม่ใช่บาร์โค้ด (`ThaiTaxId.IsPlausibleFromScan`) · มีป้าย "เลขประจำตัวผู้เสียภาษี" กำกับ (`SmartFieldExtractor.ExtractTaxIdCandidates`) · ไม่ใช่เลขผู้ซื้อ/เลขบริษัทเรา · ไม่ได้อยู่ใต้ป้ายฝั่งผู้ซื้อ (`OcrPartyLabels.FindAll`) → ส่งเข้า `DbdIdentityGuard.Judge(..., keyProven:)` ซึ่งได้ verdict ใหม่ `KeyVerifiedNameDiffers` ⇒ **ทะเบียนชนะแม้ชื่อคล้าย 0.000** | `Helpers/OcrVendorKeyEvidence.cs` · `Helpers/DbdIdentityGuard.cs` · `OcrService.EnrichFromDbdAsync` |
| ✅ A1b | **ถอดสำเนา inline** — `EnrichFromDbdAsync` เคยมีตรรกะ "ทะเบียนชนะไหม" เขียนซ้ำอยู่เอง (`NormalizeCompanyName` + เกณฑ์ 0.45 ของตัวเอง) ทั้งที่ doc ของ `DbdIdentityGuard` เขียนว่า "ยุบมาแล้ว" ⇒ สองสำเนาที่ drift ได้ทุกเมื่อ. ตอนนี้เหลือตัวตัดสินตัวเดียวทั้งเส้น OCR และเส้น integration | `OcrService.cs` (ลบ `NormalizeCompanyName`) |
| ✅ A1c | ด่านก่อนยิงทะเบียนเปลี่ยนจาก `Length != 13` → `ThaiTaxId.IsPlausibleFromScan` (checksum + ไม่ใช่บาร์โค้ด GS1) · ไม่ยิง = เขียนเหตุผลลง `ReasoningTrace` ("ไม่รู้ = บอกว่าไม่รู้") | `OcrService.cs` |
| ✅ A1d | `DbdIdentityGuard.MinSubstringLength = 4` — เส้นทาง "ชื่อหนึ่งเป็นส่วนหนึ่งของอีกชื่อ" ไม่มีขั้นต่ำมาก่อน ⇒ "ปตท" (3 ตัวอักษรหลัง normalize) เป็นส่วนหนึ่งของ "ปตท น้ำมันและการค้าปลีก" ได้ `ExactMatch` แล้ว**ข้ามด่าน 0.45 ทั้งด่าน** | `Helpers/DbdIdentityGuard.cs` |
| ✅ A2 | `OcrReviewGuard` — `vendor_name` / `buyer_name` / `document_number` เดิมเป็น `_ => (true, "")` (รับทุกสตริง) → ต้อง**ปรากฏบนข้อความที่อ่านจากกระดาษ** (เทียบแบบตัดช่องว่าง/เครื่องหมาย เพราะ OCR ไทยแทรกช่องว่างกลางคำ) · ตกด่าน ≠ ทิ้ง — ไปอยู่ใน `Rejected` ที่ UI แสดงเป็นคำแนะนำ | `Helpers/OcrReviewGuard.cs` · `AiSuggestionController.cs` |
| ✅ A3 | เรียก `VendorKnownGoodCorrector.ApplyAsync` บนเส้น **Azure** ด้วย (เดิม tier 2/3 เท่านั้น) — จำเป็นเมื่อคลังมีค่าที่ผู้ใช้แก้เอง ซึ่ง `BestMatch` จัดให้ชนะ Azure | `OcrService.cs` |
| ✅ A4 | `VendorKnownGoodCorrector.RememberUserCorrectionAsync` (ใหม่) + เรียกจาก `SubmitCorrectionAsync` ⇒ คลัง `VendorKnownGoodValues` มีแถว `Source="UserCorrection"` เป็นครั้งแรก. **ก่อนหน้านี้ผู้เขียนมีเจ้าเดียวคือ `AzureDiPatternLearner`** ⇒ ตัวจัดอันดับที่เขียนว่า "UserCorrection ชนะ AzureDI" ไม่มีวันได้ทำงาน (หลักการข้อ 2 "มี ≠ ถูกเรียก") | `Ocr/VendorKnownGoodCorrector.cs` · `OcrService.cs` |
| ✅ B2-half | ย้ายการ serialize `FieldConfidenceJson` ไป**ท้ายไปป์ไลน์** (นอก try/catch) — เดิมอยู่กลางทาง ⇒ ค่าที่ตั้งหลังจุดนั้นทั้งหมด (`[DBD]` 0.30/0.50 · `DebitAccount` · `DocumentNumber` · `ExpenseCategory` · VAT/SubTotal ที่คำนวณย้อน) **ไม่เคยลงฐาน** ⇒ เปิดหน้าทบทวนแล้วเห็นป้ายเขียวบนช่องที่ระบบเองยังไม่มั่นใจ (กฎเหล็ก #3 ข้อ 3 สั่งให้ไฮไลต์เหลือง — ไฮไลต์นั้นไม่เคยขึ้น) | `OcrService.cs` |
| ✅ doc | CLAUDE.md กฎเหล็ก #3 ข้อ 2.6 เคยเขียนว่า "autofill `SellerName/Address/BranchCode` จาก `Contact` ล่าสุด" — **ไม่มีในโค้ด**: บล็อก `[Enrich]` ไหลทางเดียว (สแกน → เติมช่องว่างของ Contact) ไม่เคยอ่านชื่อ Contact กลับมาทับ `VendorName`. แก้ doc ให้ตรงโค้ด (โค้ดเป็น ground truth) | `CLAUDE.md` |

**เทสต์ที่ล็อกไว้** — `Accounting.Tests/VendorKeyEvidenceTests.cs` (ใหม่ 16 เคส) แบ่งสองครึ่งตามกฎเหล็ก #4 H:
ครึ่งแรกพิสูจน์ว่าใบแบรนด์ละตินกลับมาถูก · ครึ่งหลังพิสูจน์ว่า**เคสที่ด่าน 0.45 ถูกสร้างมากันยังกันอยู่**
(เลขผู้ซื้อถูกหยิบมาเป็นผู้ขาย · เลขบริษัทเราเอง · บาร์โค้ด EAN-13 ที่ผ่าน mod-11 ไทย · checksum ไม่ผ่าน ·
เลขอยู่ใต้ป้ายผู้ซื้อ) + `OcrReviewGuardTests` เพิ่ม 5 เคสสองทิศ (รวมทิศ "ไม่มีข้อความให้เทียบ ห้ามบล็อกทุกใบ")

**ยังไม่ทำ (ต้องให้เจ้าของตัดสิน/ต้องรันกระดาษจริงก่อน):** ชุด B1 (แยกช่องชื่อบนกระดาษ ↔ ชื่อทะเบียน) ·
B2 ส่วนที่เหลือ (เก็บผลค้นทะเบียนเป็นคอลัมน์) · ชุด C1 (ย้ายตัวตัดสินเข้า `OcrFieldArbiter`) ·
ข้อโต้แย้งเลขมาตรา §65 ตรี ของทีมกฎหมาย (§6) ที่ขัดกับตารางใน CLAUDE.md เอง
