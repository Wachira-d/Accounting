# WHT_CREDIT_PLAN.md — ภาษีถูกหัก ณ ที่จ่าย → เครดิตใน ภ.ง.ด.50/51

เอกสารออกแบบ "ฝั่งที่เราถูกหัก" ให้ครบวงจร — ตั้งแต่ตอนลูกค้าจ่ายเงิน
จนถึงตอนใช้เครดิตในแบบ ภ.ง.ด.50 และล้างบัญชีปิดปี

> กฎการดูแลไฟล์นี้ = ชุดเดียวกับ `DOCUMENT_FLOW.md` — แตะ flow ที่ไฟล์นี้ครอบ
> ต้องอัปเดตในคอมมิตเดียวกัน พร้อมป้ายสถานะ (✅ มีจริง / 🔨 กำลังทำ / 📋 ออกแบบไว้)

---

## 0. สรุปคำตอบ: "ลงในใบสำคัญจ่ายพอไหม"

**ไม่พอ และคนละเรื่องกัน** — ใบสำคัญจ่ายคือตอน**เราหักคนอื่น** ส่วน ภ.ง.ด.50
ใช้เครดิตจาก**ภาษีที่เราถูกหัก** ซึ่งเกิดฝั่งขาย (ตอนลูกค้าจ่ายเงินเรา)

| | **เราหักคนอื่น** | **เราถูกหัก** |
| --- | --- | --- |
| เกิดตอน | เราจ่ายซัพพลายเออร์ (ใบสำคัญจ่าย) | ลูกค้าจ่ายเรา (ใบเสร็จ/ใบสำคัญรับ) |
| หนังสือรับรอง 50 ทวิ | **เราออกให้เขา** | **เขาออกให้เรา** |
| ลงบัญชี | Cr 21915/21916 (หนี้สิน — ต้องนำส่ง) | **Dr 11910** (สินทรัพย์ — เครดิตภาษี) |
| นำส่ง/ใช้ | ภ.ง.ด.3 / 53 รายเดือน (วันที่ 7/15) | **ใช้เครดิตใน ภ.ง.ด.51 และ ภ.ง.ด.50** |
| สถานะระบบ | ✅ ครบแล้ว | ⚠️ มีแค่ยอดใน GL — ยังใช้เครดิตไม่ได้ |

**สิ่งที่ยังขาด (ตรวจแล้ว ณ วันที่เขียน)**
1. ไม่มีทะเบียนหนังสือรับรองที่**ได้รับ** — `WithholdingTaxCert` มีแต่ใบที่เราออก
   (`PayeeContactId` = คนที่เราจ่าย, `WithholdingTaxCertType` = Withhold/PayAlways)
2. `GenerateCitReport` (ภ.ง.ด.50) คำนวณ รายได้ − รายจ่าย − ค่าเสื่อม → CIT
   แต่ **ไม่ได้หักเครดิตภาษีถูกหักเลย** — ผู้ใช้ต้องไปหักเองนอกระบบ
3. ไม่มีทางรู้ว่ายอด 11910 ก้อนไหน **ยังไม่ได้รับหนังสือรับรอง** (เครดิตไม่ได้)
4. ไม่มีการล้าง 11910 ตอนปิดปี → ยอดค้างสะสมข้ามปีไปเรื่อย ๆ

---

## 1. หลักกฎหมายที่ระบบต้องบังคับ

| ข้อ | กฎ | ผลต่อการออกแบบ |
| --- | --- | --- |
| L1 | เครดิตภาษีได้**เฉพาะเมื่อมีหนังสือรับรอง 50 ทวิ** จากผู้จ่าย | ต้องแยกสถานะ "มีใบแล้ว" กับ "รอใบ" — ยอดที่ยังไม่มีใบ **ห้าม**นับเป็นเครดิต |
| L2 | เครดิตต้องใช้ใน**ปีภาษีที่รับรู้เงินได้นั้น** | ทะเบียนต้องผูกปีภาษี + กระทบกับปีที่ลงรายได้ |
| L3 | ภ.ง.ด.51 (ครึ่งปี) ก็ใช้เครดิตได้ | ต้องแยกยอดที่ใช้ไปแล้วในครึ่งปี ไม่ให้ใช้ซ้ำตอนสิ้นปี |
| L4 | เครดิตเกินภาษีที่ต้องเสีย → **ขอคืน** หรือยกไปปีถัดไป | ต้องให้ผู้ใช้เลือก + ลงบัญชีต่างกัน |
| L5 | ขอคืนภาษีได้ภายใน **3 ปี** นับแต่วันสุดท้ายของกำหนดยื่น | ใบที่ได้รับช้าต้องยังใช้ได้ + เตือนใกล้หมดอายุ |
| L6 | เก็บหนังสือรับรอง **5 ปี** (พ.ร.บ.บัญชี ม.10) | ต้องแนบไฟล์ + retention ตามกฎเดียวกับเอกสารอื่น |
| L7 | อัตราหักต้องตรงประเภทเงินได้ (ท.ป.4/2528) | ตรวจอัตราที่ผู้จ่ายหักมา — ผิดอัตรา = เตือน (ผู้จ่ายอาจหักเกิน/ขาด) |

---

## 2. โครงข้อมูลที่เพิ่ม ✅

### 2.1 `WhtCreditReceived` — ทะเบียนหนังสือรับรองที่ได้รับ

```
Id, CompanyId
TaxYear                int          // ปีภาษีที่ใช้เครดิต (ตาม L2)
CertificateNumber      string?      // เลขที่บนหนังสือรับรองของผู้จ่าย
CertificateDate        DateTime?
PayerContactId         Guid?        // ลูกค้าที่หักเรา (nullable — อาจเป็นรายที่ไม่มีใน contact)
PayerName              string       // snapshot — ชื่อบนหนังสือ ณ วันนั้น
PayerTaxId             string?      // snapshot 13 หลัก
PayerFormType          enum         // แบบที่ผู้จ่ายยื่น: Pnd3 / Pnd53
IncomeTypeCode         string       // ม.40(2)/(3)/(4)/(5)/(6)/(7)/(8)
IncomeAmount           decimal      // ฐานเงินได้
WhtRate                decimal      // อัตราที่ถูกหักจริง
WhtAmount              decimal      // ภาษีที่ถูกหัก  ← ตัวที่ใช้เครดิต
Status                 enum         // Pending(รอใบ) / Received / Claimed / Expired
DocumentId             Guid?        // ใบเสร็จ/ใบแจ้งหนี้ที่ทำให้เกิด
PaymentId              Guid?
AttachmentId           Guid?        // สแกนหนังสือรับรอง (L6)
ClaimedInTaxReportId   Guid?        // ใช้เครดิตในแบบไหน (ภ.ง.ด.50/51) — กัน L3 ใช้ซ้ำ
Notes
```

**Index:** `(CompanyId, TaxYear, Status)`, `(CompanyId, DocumentId)`

**ทำไมไม่ใช้ `WithholdingTaxCert` เดิม:** เอกสารคนละชนิดทางกฎหมาย —
ของเดิมคือ "ใบที่เราออก" (มีเลขรันของเรา, ต้องพิมพ์, ผูก ภ.ง.ด.3/53 ที่เรานำส่ง)
ส่วนอันนี้คือ "ใบที่เขาออกให้เรา" (เลขเป็นของเขา, เราแค่บันทึก+เก็บ, ผูก ภ.ง.ด.50/51)
ถ้ายัดรวมเป็นตัวเดียวแล้วแยกด้วย flag จะเกิดปัญหาเดียวกับใบลดหนี้สองฝั่ง —
ทุก query/รายงานต้องจำใส่เงื่อนไขทิศทาง ลืมเมื่อไรตัวเลขผิดเงียบ ๆ

### 2.2 ต่อยอดจากของเดิม

- `Document.WithholdingTaxAmount` → ยังลง Dr 11910 เหมือนเดิม (ไม่แตะ)
- เพิ่ม `Document.WhtCreditReceivedId` (nullable) — ผูกกลับหาใบรับรองที่ได้รับ
- ตอน `WhtRecognitionBasis = Cash` ระบบสร้างแถว `WhtCreditReceived`
  สถานะ **Pending** ให้อัตโนมัติตอนรับเงิน (มียอด แต่ยังไม่มีใบ)

---

## 3. กระบวนการทั้งวงจร

```
1. ออกใบแจ้งหนี้/ใบกำกับ (มี WHT %)
        ↓
2. ลูกค้าจ่ายเงิน — หัก 3% ไว้     → Dr 11910 (ตามเกณฑ์ Cash/Accrual ที่ตั้งไว้)
        ↓                            + สร้าง WhtCreditReceived สถานะ "รอใบรับรอง"
3. ลูกค้าส่งหนังสือรับรอง 50 ทวิ    → บันทึกเลขที่/วันที่/แนบสแกน → "ได้รับแล้ว"
        ↓                            (ยังไม่มีใบ = ยังเครดิตไม่ได้ ตาม L1)
4. ครึ่งปี — ยื่น ภ.ง.ด.51          → ใช้เครดิตบางส่วน → stamp ClaimedInTaxReportId
        ↓
5. สิ้นปี — ยื่น ภ.ง.ด.50           → เครดิตที่เหลือ + หักภาษีที่ชำระตาม ภ.ง.ด.51
        ↓
6. ผลลัพธ์: ชำระเพิ่ม / ขอคืน / ยกไปปีหน้า  → ล้าง 11910 ตามจริง (ข้อ 5)
```

### 3.1 หน้าจอที่ต้องมี ✅ (หน้า `wht-credit.html`)

| หน้า | ทำอะไร |
| --- | --- |
| **ทะเบียนภาษีถูกหัก ณ ที่จ่าย** | ลิสต์ตามปีภาษี · filter สถานะ · ยอดรวมเครดิต |
| **รอรับหนังสือรับรอง** | ยอดที่ Dr 11910 แล้วแต่ยังไม่มีใบ — เรียงตามอายุ + ปุ่มทวงลูกค้า |
| **กระทบยอด 11910** | ยอดในทะเบียน ↔ ยอดคงเหลือบัญชี 11910 · ต่างเมื่อไร = มีรายการหลุด |
| **แนบใบ/แก้ไข** | อัปโหลดสแกน + กรอกเลขที่/วันที่/ประเภทเงินได้ |

### 3.2 จุดที่ต้องเตือน (กันพลาดจริง)

- ⚠️ **ก่อนปิดปี / ก่อนยื่น ภ.ง.ด.50** — ยังมีรายการ "รอใบรับรอง" คงค้าง
  → เครดิตไม่ได้ตาม L1 ต้องรีบทวงลูกค้า (แสดงยอดที่จะเสียไปถ้าไม่ได้ใบ)
- ⚠️ **อัตราที่ถูกหักไม่ตรงประเภทเงินได้** (L7) — เช่นค่าบริการหัก 5% แทน 3%
- ⚠️ **ใบรับรองข้ามปีภาษี** — วันที่บนใบอยู่คนละปีกับที่ลงรายได้ (L2)
- ⚠️ **ใกล้พ้น 3 ปี** (L5) — เครดิตที่ยังไม่ได้ใช้/ขอคืน

---

## 4. ภ.ง.ด.50 / 51 ต้องแก้อะไร

ปัจจุบัน `TaxService.GenerateCitReport` จบที่ "ภาษีที่ต้องเสีย" — ต้องต่อ:

```
ภาษีที่คำนวณได้ (CIT)
  − เครดิตภาษีถูกหัก ณ ที่จ่าย (เฉพาะสถานะ Received ของปีนั้น)   ← ใหม่
  − ภาษีที่ชำระแล้วตาม ภ.ง.ด.51                                  ← ใหม่
  = ต้องชำระเพิ่ม  หรือ  ชำระเกิน → ขอคืน / ยกไปปีถัดไป (L4)
```

- `TaxReport.TotalTaxWithheld` ตอนนี้ถูกใช้เก็บ "ยอด CIT" (reuse ผิดความหมาย)
  → ควรแยก field ให้ตรงชื่อ ก่อนต่อยอด ไม่งั้นสับสนถาวร
- ต้อง stamp `ClaimedInTaxReportId` ทุกใบที่ถูกใช้ → กันใช้ซ้ำระหว่าง 51 กับ 50 (L3)

---

## 5. การลงบัญชีตอนปิดปี ✅

| เหตุการณ์ | JE |
| --- | --- |
| ใช้เครดิตหักกับภาษีที่ต้องเสีย | Dr ภาษีเงินได้ค้างจ่าย (21810) / **Cr 11910** |
| ขอคืน (เครดิตเกิน) | Dr ลูกหนี้กรมสรรพากร (11920) / **Cr 11910** |
| ยกไปเครดิตปีถัดไป | คงไว้ที่ 11910 (ไม่ต้องล้าง) + หมายเหตุปีที่ยกมา |
| ใบที่ไม่ได้รับจนหมดอายุ | Dr ค่าใช้จ่ายภาษีที่เรียกคืนไม่ได้ / **Cr 11910** + เหตุผล |

> ถ้าไม่ทำข้อนี้ ยอด 11910 จะพองสะสมข้ามปีจนกระทบยอดกับแบบยื่นไม่ได้เลย

---

## 6. ลำดับการทำ (ถ้าอนุมัติให้สร้าง)

| เฟส | ได้อะไร | ขนาด |
| --- | --- | --- |
| **1** | entity `WhtCreditReceived` + migration + สร้างแถว Pending อัตโนมัติตอนรับเงิน | เล็ก |
| **2** | หน้าทะเบียน + แนบสแกน + เปลี่ยนสถานะเป็น Received | กลาง |
| **3** | ภ.ง.ด.50/51 หักเครดิตจริง + stamp กันใช้ซ้ำ | กลาง |
| **4** | กระทบยอด 11910 + หน้ารอรับใบ + แจ้งเตือนก่อนปิดปี | กลาง |
| **5** | JE ปิดปี (ใช้เครดิต / ขอคืน / ยกไป / ตัดสูญ) | เล็ก |

เฟส 1–2 ใช้งานได้จริงแล้ว (มีทะเบียน เก็บใบครบ ตรวจสอบได้)
เฟส 3 คือจุดที่เริ่มประหยัดเวลาจริงตอนยื่นแบบ

---

## 6.5 ทางเข้าจาก OCR ✅

สแกนหนังสือรับรอง 50 ทวิ → ระบบดูว่าเลขผู้เสียภาษีของบริษัทเราอยู่ช่องไหน:

| เราอยู่ช่อง | แปลว่า | ระบบทำอะไร |
| --- | --- | --- |
| **ผู้ถูกหักภาษี ณ ที่จ่าย** | ลูกค้าหักเรา | ลงทะเบียนเครดิตสถานะ **Received** + แนบไฟล์สแกน |
| **ผู้มีหน้าที่หักภาษี ณ ที่จ่าย** | เราหักเขา | ไม่แตะทะเบียนนี้ (เป็นหนี้ที่ต้องนำส่ง ภ.ง.ด.3/53) |
| แยกไม่ได้ / ไม่มีเลขเรา | — | ไม่เดา ไม่ลงทะเบียน |

`OcrDocumentRoleInferrer.InferWhtCertWeAreWithheld` — เทียบตำแหน่งเลขผู้เสียภาษี
ของเรากับตำแหน่งหัวข้อทั้งสองช่องบนกระดาษ

## 7. สถานะการสร้าง

| เฟส | สถานะ | ของจริงในโค้ด |
| --- | --- | --- |
| 1 entity + migration + สร้าง Pending อัตโนมัติ | ✅ | `WhtCreditReceived`, ตาราง `WhtCreditsReceived`, `DocumentService.SyncWhtCreditReceivedAsync` (อ่านจาก GL 11910 จริง) |
| 2 ทะเบียน + แนบสแกน + เปลี่ยนสถานะ | ✅ | `WhtCreditService`, `WhtCreditController`, หน้า `wht-credit.html` |
| 3 ภ.ง.ด.50 หักเครดิตจริง + กันใช้ซ้ำ | ✅ | `TaxService.GenerateCitReport` (นับเฉพาะ Received/Claimed) + `ClaimAsync`/`UnclaimAsync` |
| 4 กระทบยอด 11910 + เตือน | ✅ | `SummaryAsync` (ทะเบียน − GL) + แบนเนอร์เตือน 2 แบบบนหน้าจอ |
| 5 JE ปิดปี (ใช้เครดิต/ขอคืน/ยกไป/ตัดสูญ) | ✅ | `SettleYearEndAsync` + `POST /wht-credits/settle-year-end` — ลง JE ล้าง 11910 ตามที่ใช้จริง พร้อม guard ห้ามล้างเกินยอดคงเหลือ (ยกไปปีหน้า = ไม่ต้องเรียก) |

_Last verified against codebase: 2026-08-13 — สร้างครบทั้ง 5 เฟส + OCR 50 ทวิ อ่านประเภทเงินได้_
